/* ============================================================
   data-typescript.js — Contenu pédagogique TypeScript (approfondi)
   Suppose le module JavaScript déjà acquis (comparaisons JS vs TS).
   ============================================================ */
window.DEVDOCS = window.DEVDOCS || {};

DEVDOCS.ts = {
  id: 'ts',
  name: 'TypeScript',
  icon: 'code_blocks',
  tagline: 'JavaScript qui vérifie ses devoirs : types statiques, erreurs attrapées à la compilation, zéro coût au runtime.',
  heroTitle: 'TypeScript, le JavaScript qui relit ses copies',

  categories: [
    {
      id: 'fondamentaux',
      name: 'Fondamentaux',
      icon: 'tune',
      fiches: [
        {
          id: 'ts-pourquoi',
          title: 'Pourquoi TypeScript ?',
          icon: 'verified',
          level: 'Débutant',
          tagline: 'JavaScript ne dit rien… jusqu\'au crash en production. TypeScript parle AVANT, à la compilation.',
          intro: 'TypeScript est un **superset de JavaScript** : tout JS valide est du TS valide. Il ajoute une couche de **types statiques** vérifiée par un compilateur — puis entièrement effacée : ce qui tourne dans le navigateur reste du JavaScript pur, ni plus lourd ni plus lent. La promesse tient en une phrase : déplacer les bugs de « 3 h du matin en prod, chez un client de Cotonou » vers « souligné rouge dans ton éditeur, avant même de sauvegarder ». Cette fiche explique pourquoi cette promesse tient — et ce qu\'elle ne couvre pas.',
          blocks: [
            { t: 'h3', h: 'Le problème : JavaScript se tait jusqu\'au crash' },
            { t: 'p', h: 'Dans le module JS, tu as appris que le langage à typage dynamique garde le type dans la VALEUR, pas dans la variable : flexible… et silencieux. `total(500, "trois")` produit un `NaN` sans que personne ne bronche — jusqu\'à ce que ce NaN s\'affiche sur la facture d\'un client à Dantokpa, un vendredi soir à 22 h. Le bug n\'existe que quand la ligne s\'exécute, avec CES données-là. TypeScript pose une question renversante : et si on ANNONçAIT à l\'avance ce que chaque fonction attend, pour qu\'une machine vérifie l\'assemblage avant d\'appuyer sur « envoyer » ?' },
            { t: 'h3', h: 'Le genre de bug que JS laisse passer' },
            { t: 'code', lang: 'js', label: 'panier.js — JS pur', code:
'function total(prix, quantite) {\n  return prix * quantite;\n}\n\ntotal(500, "3");        // 1500 ? non : 500 * "3" → 1500… par chance\ntotal(500, "trois");    // NaN — silencieux, propagé partout\ntotal(500);             // NaN aussi, et personne ne prévient\n\n// Le bug n\'apparaît QUE quand la ligne s\'exécute,\n// avec CES données-là. En prod. Un vendredi soir.' },
            { t: 'h3', h: 'La même fonction, sous la loupe de TypeScript' },
            { t: 'code', lang: 'ts', label: 'panier.ts — la même, typée', code:
'function total(prix: number, quantite: number): number {\n  return prix * quantite;\n}\n\ntotal(500, "3");     // ✗ Erreur À L\'ÉCRITURE : "3" n\'est pas un number\ntotal(500);          // ✗ Erreur : il manque un argument\n\ntotal(500, 3);       // ✓ 1500 — et l\'éditeur complète, documente, navigue' },
            { t: 'p', h: 'Lis ce qui vient de se passer, c\'est tout le module : les annotations `: number` forment un CONTRAT (« mes deux entrées sont des nombres, ma sortie aussi »), et le compilateur vérifie chaque appel contre ce contrat — à la frappe, pas à l\'exécution. La valeur n\'a pas bougé ; c\'est le MOMENT où tu apprends l\'erreur qui change tout: avant que ton client ne la vive.' },
            { t: 'h3', h: 'Sous le capot : les types s\'effacent (type erasure)' },
            { t: 'p', h: 'La mécanique interne tient en trois temps : 1) tu écris du `.ts` — du JavaScript + des annotations de types ; 2) `tsc` (le compilateur) vérifie l\'ensemble des contrats ; 3) il émet du `.js` **dépouillé de toute trace de types**. Dans le navigateur, il n\'existe que du JavaScript classique : aucune case mémoire supplémentaire, aucune vérification au runtime, zéro coût. Conséquence en miroir, fondamentale : TypeScript NE PROTÈGE PAS à l\'exécution. Une donnée qui arrive du dehors (API, formulaire, localStorage) entre sans contrat — elle reste à valider explicitement (les fiches `unknown` et Type guards sont là pour ça).' },
            { t: 'h3', h: 'Ce que les types t\'achètent vraiment' },
            { t: 'ul', items: [
              '**Feedback immédiat** : la boucle « écrire → vérifier » se joue à la frappe, pas à l\'exécution. Le test « et si j\'envoie le mauvais type ? » devient gratuit.',
              '**Refactoring sans sueur** : renommer une propriété, changer une signature — le compilateur liste toutes les casses du projet, aucune n\'est oubliée. C\'est LUI qui rend les grandes bases de code gérables.',
              '**Documentation exécutable** : la signature d\'une fonction EST sa doc, toujours à jour (sinon, erreur de compilation), et l\'autocomplétion la sert au moment du besoin.',
              '**Contrats d\'équipe** : entre ton front et l\'API, entre modules de l\'équipe — les types remplacent les longs échanges « c\'est quoi déjà le format ? ».',
              '**Confiance de livraison** : quand tsc passe en strict, une famille entière de bugs (null, undefined, mauvais types) est statistiquement absente — pas toutes, mais les plus fréquentes.'
            ]},
            { t: 'h3', h: 'L\'éditeur devient copilote' },
            { t: 'p', h: 'Effet secondaire spectaculaire : une fois les types connus, ton éditeur cesse de deviner. Il propose toutes les propriétés d\'un objet après le point, affiche la signature d\'une fonction au survol, te téléporte à la définition (« Go to definition ») et renomme un symbole proprement dans tout le projet. Travailler en JS après ça fait l\'effet de taper avec des moufles — c\'est une des raisons pour lesquelles ceux qui goûtent TS reviennent rarement.' },
            { t: 'h3', h: 'Étude de cas : l\'appli de livraison zémidjan' },
            { t: 'code', lang: 'ts', code:
'// Le tarif dépend du quartier — au sens strict :\nfunction tarifCourse(quartier: string, express: boolean): number {\n  const base = quartier === "centre" ? 150 : 300;\n  return express ? base * 2 : base;\n}\n\ntarifCourse("Dantokpa", true);   // ✓ compile… mais « Dantokpa » avec un D majuscule ?\n// C\'est le sujet des LITERAL TYPES (fiche Unions) :\nfunction tarifCourse2(q: "centre" | "akpakpa" | "calavi", express: boolean): number {\n  /* … */ \n}\ntarifCourse2("Dantokpa", true);  // ✗ refusé À L\'ÉCRITURE : la faute de frappe est attrapée' },
            { t: 'h3', h: 'Ce que TypeScript n\'est PAS' },
            { t: 'ul', items: [
              '**Pas une validation runtime** : les types disparaissent à l\'exécution. Une donnée externe (API, formulaire) reste à VALIDER — fiches unknown/guards.',
              '**Pas un autre langage** : c\'est JavaScript, plus des annotations. Tout ton savoir JS, tout l\'écosystème npm, reste utilis tel quel.',
              '**Pas une taxe de performance** : la vérification se fait à la compilation ; le JS émis n\'est ni plus lourd ni plus lent.',
              '**Pas magique** : un mauvais modèle (ou un `any` balancé) peut mentir — TS déplace le problème vers l\'honnêteté de tes contrats.'
            ]},
            { t: 'h3', h: 'JS vs TS : le réflexe en deux images' },
            { t: 'table', head: ['Question', 'JavaScript', 'TypeScript'], rows: [
              ['« Ce paramètre accepte quoi ? »', 'lire le code / deviner / tester', 'lire la signature'],
              ['« Cette propriété existe ? »', 'undefined en prod pour le savoir', 'souligné rouge à l\'écriture'],
              ['« Je renomme, ça casse où ? »', 'recherche textuelle + espoir', 'le compilateur énumère tout'],
              ['Au runtime', 'bug possible', 'même JS, mais déjà relu']
            ]},
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« TS est un nouveau langage à ré-apprendre. »** — Ton JavaScript démarre tel quel ; les annotations s\'ajoutent progressivement, et l\'inférence (prochaine fiche) en écrit la moitié pour toi.',
              '**« TS protège à l\'exécution. »** — Non : les types s\'effacent. Annoncer `: Produit` sur une réponse d\'API non vérifiée est un mensonge écrit en toutes lettres que le compilateur croit — validations explicites, toujours.',
              '**« Il FAUT tout typer pour que ça serve. »** — Non : l\'inférence couvre l\'intérieur du code ; seules les frontières méritent des annotations. On en fait toute une fiche.',
              '**« TS, c\'est pour les « gros » projets. »** — Le bénéfice est immédiat dès le premier fichier (aute-complétion, typos attrapées) ; la « taille » ne fait qu\'agrandir le gain.',
              '**« Adopter TS = tout réécrire. »** — C\'est l\'anti-pattern documenté (voir encadré rouge) : migration incrémentale, fichier par fichier, sans arrêter la production.',
              '**« Les erreurs rouges veulent dire que je suis mauvais. »** — Le compilateur est un collègue qui relit ta copie à chaque frappe, pas un examinateur. La rouge se transforme en « attrapé avant le client » — c\'est ça, la victoire.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Dans le module JS, tu as rencontré le typage dynamique, les falsy, la coercion (`"5" + 3`…). TypeScript EST la réponse industrielle à tout cela : il rend explicites les conventions qu\'on vérifiait à la main et dans la tête. Prochaine étape logique — et la seule chose technique avant de coder : installer `tsc` et configurer son `tsconfig.json`, puis découvrir combien d\'annotations tu pourras t\'économiser grâce à l\'inférence.' },
            { t: 'callout', kind: 'tip', h: 'TS se déploie **progressivement** : un fichier à la fois, `strict` réglé à ton rythme, JS et TS cohabitant dans le même projet (`allowJs`). Tu n\'as pas besoin de tout réécrire pour profiter dès demain de la relue automatique — c\'est la même sagesse « migration incrémentale » que partout en logiciel.' }
          ],
          errors: [
            { title: 'Croire que TS protège à l\'exécution', lang: 'ts', bad:
'const reponse: Produit = await fetch("/api/produit").then(r => r.json());\n// TS est content… jusqu\'à ce que l\'API change → crash runtime', good:
'const brut: unknown = await fetch("/api/produit").then(r => r.json());\nconst reponse = estProduit(brut) ? brut : null;   // validation réelle', why: 'Les types sont effacés à la compilation : aucun garde-fou n\'existe à l\'exécution. Annoncer `reponse: Produit` sur une donnée externe non validée, c\'est écrire un mensonge que le compilateur croit. unknown + vérification = honnêteté.' },
            { title: 'Tout convertir d\'un coup', lang: 'ts', bad:
'// Renommer les 60 fichiers .js en .ts lundi 9 h,\n// 800 erreurs à 9 h 05, abandon à 9 h 30.', good:
'// 1. tsc --allowJs sur le code JS existant (zéro réécriture)\n// 2. typer les fichiers NOUVEAUX, puis les plus modifiés\n// 3. durcir strict progressivement (fiche tsconfig)', why: 'TypeScript est conçu pour la migration incrémentale : la valeur vient vite sur les nouveaux fichiers, et le reste se convertit au fil des retouches. La big-bang rewrite échoue presque toujours par découragement devant le compteur d\'erreurs.' }
          ],
          related: ['ts-tsc-config', 'ts-inference', 'js-types-operateurs', 'ts-types-speciaux']
        },

        {
          id: 'ts-tsc-config',
          title: 'tsc & tsconfig.json',
          icon: 'tune',
          level: 'Débutant',
          tagline: 'Le compilateur, ses trois options vitales (strict, target, module) — et les configurations qui font perdre du temps.',
          intro: '`tsc` est le compilateur : il lit tes `.ts`, **vérifie les types** et **émet** du `.js` (ou ne fait que vérifier, avec `--noEmit`). Le fichier `tsconfig.json`, posé à la racine du projet, est son tableau de bord : trois règlages changent fondamentalement ton expérience — `strict`, `target`, `module` — et quelques-uns évitent les pièges classiques de la première semaine. C\'est LA fiche à lire avant de taper ta première ligne de TS.',
          blocks: [
            { t: 'h3', h: 'Le problème : passer de « humain » à « machine », proprement' },
            { t: 'p', h: 'Ton navigateur ne comprend que le JavaScript ; ton éditeur et toi préférez le TypeScript qui relit tout. Il faut donc une étape de traduction entre les deux — la **compilation**. Elle a DEUX rôles distincts que beaucoup de débutants confondent : la **vérification** (est-ce que tous les contrats tiennent ?) et l\'**émission** (produit le .js de livraison). tsconfig.json te laisse régler les deux séparément — et c\'est précisément ce qui sauvera ta première mise en production.' },
            { t: 'h3', h: 'Le compilateur en trois commandes' },
            { t: 'code', lang: 'bash', code:
'npx tsc                 # compile selon tsconfig.json\nnpx tsc --watch         # recompile à chaque sauvegarde (mode veille)\nnpx tsc --noEmit        # VÉRIFIE sans émettre : parfait en CI\nnpx tsc --init          # génère un tsconfig.json commenté' },
            { t: 'p', h: 'Dans un projet réel, la répartition typique est claire : le BUNDLER (Vite, webpack, Next…) émet le JavaScript optimisé pour la prod, tandis que `tsc --noEmit` sert de contrôle qualité — à l\'entrée de chaque pull request, dans la CI. Deux outils, deux métiers : le bundler assemble, `tsc` vérifie. Confondre les rôles mène directement au piège rouge plus bas.' },
            { t: 'h3', h: 'tsconfig.json : le tableau de bord' },
            { t: 'code', lang: 'js', label: 'tsconfig.json — un point de départ honnête', code:
'{\n  "compilerOptions": {\n    "strict": true,                    // LA vraie expérience TS (détail plus bas)\n    "target": "ES2020",                // JS émis compatible navigateurs modernes\n    "module": "ESNext",                // modules ES (import/export)\n    "moduleResolution": "bundler",     // résolution façon Vite/webpack\n    "outDir": "dist",                  // le JS émis ici, jamais à côté des sources\n    "rootDir": "src",\n    "include": ["src"],\n    "noEmitOnError": true,             // pas de JS cassé en cas d\'erreur\n    "skipLibCheck": false,             // vérifie aussi les .d.ts (défaut)\n    "forceConsistentCasingInFileNames": true\n  }\n}' },
            { t: 'p', h: 'Apprends à lire ce fichier comme un contrat avec ton futur toi : chaque option dit quel JS sort (`target`), comment les imports sont compris (`module`/`moduleResolution`), où vivent les sources et la sortie (`rootDir`/`outDir`), et surtout à quel point le gardien est sévère (`strict` + toute la famille d\'options affinées qu\'on rencontrera au fil des fiches).' },
            { t: 'h3', h: 'strict : le sous-marin à 8 interrupteurs' },
            { t: 'p', h: '`strict: true` active d\'un coup une famille de huit vérifications. Les deux qui changent le quotidien : `noImplicitAny` — tout ce qui n\'est pas typé devient visible, fini les `any` furtifs qui neutralisent le contrôle — et `strictNullChecks` — `null` et `undefined` cessent d\'être des pièges gratuits : le compilateur t\'oblige à gérer ces cas AVANT de toucher une propriété. Désactiver `strict`, c\'est payer la syntaxe de TypeScript sans toucher l\'assurance : le pire échange possible.' },
            { t: 'code', lang: 'ts', label: 'strictNullChecks à l\'oeuvre', code:
'function saluer(nom: string | null) {\n  return "Bonjour " + nom.toUpperCase();   // ✗ nom est peut-être null !\n}\n\n// Le compilateur T\'OBLIGE à traiter le cas :\nfunction saluerOK(nom: string | null) {\n  if (nom === null) return "Bonjour, invité";\n  return "Bonjour " + nom.toUpperCase();  // ✓ ici, nom est forcément string\n}\n// C\'est précisément le crash « Cannot read properties of null » du module JS,\n// attrapé à la compilation au lieu de la prod.' },
            { t: 'h3', h: 'target & module : choisir le JavaScript émis' },
            { t: 'p', h: '`target` décide la version du JavaScript produit : viser « ES2015 » force tsc à RÉÉCRIRE les syntaxes récentes (async/await deviennent des machines d\'état, `?.` devient une cascade de `&&`) ; viser « ES2020 » laisse tout tel quel puisque les navigateurs actuels « parlent » 2020. `module` décide la forme des `import` émis : `ESNext` pour les apps web derrière un bundler, `NodeNext` pour une API Node. La règle d\'or : viser ce que l\'environnement-exécution comprend, pas plus bas — le downleveling coûte en taille et en lisibilité.' },
            { t: 'table', head: ['Option', 'Rôle', 'Conseil'], rows: [
              ['`strict`', 'les 8 sévérités d\'un coup', '`true` sur tout nouveau projet'],
              ['`target`', 'niveau de JS émis', '`ES2020`+ (navigateurs modernes)'],
              ['`module`', 'système de modules émis', '`ESNext` (bundler) / `NodeNext` (Node pur)'],
              ['`moduleResolution`', 'comment les imports sont trouvés', 'aligner sur l\'outil : `bundler` ou `NodeNext`'],
              ['`outDir` / `rootDir`', 'séparer sources et sortie', 'src/ -> dist/, proprement'],
              ['`noEmitOnError`', 'bloque l\'émission si erreurs', '`true`, sinon tsc « passe quand même »'],
              ['`exactOptionalPropertyTypes`', '`?` ≠ undefined explicite', 'Avancé — à durcir plus tard']
            ]},
            { t: 'h3', h: 'Le piège d\'anthologie : « compilé avec succès » ≠ « sans erreurs »' },
            { t: 'p', h: 'Retiens ce comportement renversant : par défaut, `tsc` **émet du JavaScript MÊME en présence d\'erreurs de types**. Un build mal pensé peut donc publier un js « compilé » bourré d\'erreurs de types — la cérémonie a eu lieu, mais personne n\'a vérifié les papiers. Le vaccin : `noEmitOnError: true` pour l\'émission ET un `tsc --noEmit` systématique comme étape de vérification avant tout déploiement.' },
            { t: 'h3', h: 'Trois recettes qui couvrent la vraie vie' },
            { t: 'code', lang: 'js', label: 'Réglages types selon le projet', code:
'// 1. App Vite/React : strict + module bundler\n{ "strict": true, "target": "ES2020", "module": "ESNext",\n  "moduleResolution": "bundler", "jsx": "react-jsx", "noEmit": true }\n// (noEmit : c\'est Vite qui émet ; tsc ne fait que vérifier)\n\n// 2. API Node : module natif\n{ "strict": true, "target": "ES2022", "module": "NodeNext",\n  "moduleResolution": "NodeNext", "outDir": "dist" }\n\n// 3. Migration d\'un vieux projet JS : on commence en douceur\n{ "allowJs": true, "checkJs": false, "strict": false }\n// puis on durcit : noImplicitAny, puis strictNullChecks, puis strict: true' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« tsc remplace le bundler. »** — Non : tsc vérifie et traduit ; le bundler assemble, optimise et sert. Dans la plupart des projets modernes, tsc ne voit jamais la prod (`noEmit`), le bundler émet.',
              '**« strict, on verra plus tard. »** — Le coût de NON-strict croît avec chaque fichier : les `any` furtifs et les null non gérés s\'accumulent en dette. Sur du neuf, `strict: true` dès le premier jour est quasi gratuit.',
              '**« Une erreur tsc empêche le site de fonctionner. »** — Sans `noEmitOnError`, le JS est émis quand même et tourne : l\'erreur est un avis, pas une barrière — d\'où l\'étape `--noEmit` en CI pour en faire une vraie frontière.',
              '**« skipLibCheck: true, c\'est « normal ». »** — Par défaut, tsc examine aussi les fichiers .d.ts de tes dépendances (lent, complet). Beaucoup de templates l\'activent pour la vitesse : acceptable, mais sache que ce filet baille.',
              '**« tsconfig se règle une fois au début. »** — Il vit avec le projet : on y ajoute `extends` pour partager une base, des `paths` pour les alias, des `references` pour les monorepos. Reviens-y quand le projet grandit.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Cette fiche fait marcher la promesse de la précédente : sans `strict`, la relue automatique est affaiblie d\'office. Les modules `import/export` du module JS rencontrent ici leur pendant compilateur (`module`/`moduleResolution` — rappelle-toi : serveur obligatoire en natif). Prochaine réjouissance : l\'inférence de type — comment TypeScript écrit la moitié de tes annotations pour toi, et pourquoi `noImplicitAny` est là quand il ne peut pas.' },
            { t: 'callout', kind: 'warn', h: 'En CI : `tsc --noEmit` comme étape de vérification incompressible, PUIS l\'émission par le bundler. Le « vérifié par tsc » doit être un passage obligé, pas un badge décoratif.' }
          ],
          errors: [
            { title: 'Nouveau projet démarré sans strict', lang: 'js', bad:
'{\n  "compilerOptions": { "strict": false }   // projet NEUF, choix par confort\n}', good:
'{\n  "compilerOptions": { "strict": true }\n}\n// Si migration d\'un vieux projet : activer strict progressivement\n// ("noImplicitAny": true d\'abord, puis "strictNullChecks", puis tout)', why: 'strict:false laisse passer l\'essentiel des bugs que TS est censé attraper (null, any implicites, paramètres mal appelés) : tu te retrouves avec le coût de la syntaxe TS et presque aucun bénéfice. Sur du neuf, c\'est se tirer dans le pied.' },
            { title: 'Laisser le JS émis polluer les sources', lang: 'js', bad:
'// pas de outDir : panier.ts ET panier.js côte à côte dans src/\n// → imports du mauvais fichier, diffs illisibles, déploiements confus', good:
'{ "compilerOptions": { "rootDir": "src", "outDir": "dist", "include": ["src"] } }\n// .gitignore : /dist — seules les sources sont versionnées', why: 'Mélanger sources et sortie invite à importer le mauvais fichier (.js périmé), à versionner des artefacts, et à déployer du double. Une frontière nette src/ → dist/ rend toute la chaîne prévisible.' }
          ],
          related: ['ts-pourquoi', 'ts-inference', 'js-es6', 'ts-modules']
        },

        {
          id: 'ts-inference',
          title: 'L\'inférence de type',
          icon: 'insights',
          level: 'Débutant',
          tagline: 'TS devine les types tout seul la plupart du temps — ton travail : savoir où annoter, et où laisser faire.',
          intro: 'Contrairement à la première impression, TypeScript ne t\'oblige PAS à tout annoter : il **infère** les types à partir des valeurs. `const ville = "Cotonou"` est automatiquement `string` — écrire `: string` serait du bruit. La vraie compétence à aiguiser : annoter aux **frontières** (paramètres, retours publics, données externes), laisser inférer à l\'**intérieur**. C\'est ce qui rend le TS fluide à lire au lieu d\'être une paperasse.',
          blocks: [
            { t: 'h3', h: 'Le problème : typer sans noyer le code' },
            { t: 'p', h: 'Atterrissant d\'un langage à annotations obligatoires (Java, C#), on redoute un TypeScript verbeux où CHAQUE variable exigerait son étiquette. Voyons ce que ça donnerait : `const nom: string = "Awa"; const prix: number = 500;` — chaque annotation répète ce que la valeur crie déjà. L\'inférence est la réponse du langage : **TS lit ta valeur d\'initialisation et en DÉDUIT le type**, puis considère ce type comme verrouillé. Tu ne paies l\'annotation que là où elle ajoute de l\'information — et cette économie change le quotidien.' },
            { t: 'h3', h: 'La magie au quotidien' },
            { t: 'code', lang: 'ts', code:
'const ville = "Cotonou";       // TS voit : string  (inférence)\nlet prix = 500;                // number\nconst commandes = [1, 2, 3];   // number[]\n\nville = 42;           // ✗ Type \'number\' is not assignable to type \'string\'\nprix = "500 FCFA";    // ✗ pareil — inféré, puis VERROUILLÉ\n\nfunction total(ht: number) {\n  return ht * 1.18;      // retour inféré : number — inutile de l\'écrire\n}' },
            { t: 'p', h: 'Deux temps mécaniques à graver. 1) **Inférence à l\'initialisation** : le type naît de la valeur. 2) **Verrouillage** : une fois fixé, le type protège la variable — tout ce qui n\'y correspond pas devient une erreur de compilation. Le confort est double : zéro bruit à l\'écriture, sécurité totale à la relecture.' },
            { t: 'h3', h: 'const, let et le widening (l\'élargissement)' },
            { t: 'code', lang: 'ts', code:
'const statutA = "en_attente";   // type : "en_attente" — le LITTÉRAL, figé\nlet statutB = "en_attente";     // type : string — ÉLARGI\n\n// Pourquoi ? const ne changera JAMAIS : sa valeur EST son type.\n// let, elle, est faite pour changer : TS lui donne le type LARGE\n// recevable, sauf si tu verrouilles toi-même le contrat :\nlet statutC: "en_attente" | "payee" | "livree" = "en_attente";' },
            { t: 'p', h: 'Cette nuance — le « widening » — n\'est pas un caprice : elle reflète exactement le contrat que tu as annoncé avec `let` (cette variable évoluera) vs `const` (celle-ci est définitive). Elle deviendra cruciale avec les **literal types** (fiche Unions), où des ensembles de valeurs précises remplacent les `string` fourre-tout — le statut d\'une cotisation de tontine en est le parfait exemple ci-dessus.' },
            { t: 'h3', h: 'Les trois endroits où ANNOTER' },
            { t: 'code', lang: 'ts', code:
'// 1. Les PARAMÈTRES de fonctions (sinon any implicite -> noImplicitAny)\nfunction livrer(zone: string, express: boolean = false) { /* … */ }\n\n// 2. Les RETOURS de fonctions publiques (le contrat visible de l\'extérieur)\nfunction lirePanier(): { articles: string[]; total: number } {\n  return { articles: ["gari"], total: 500 };\n}\n\n// 3. Quand l\'inférence tombe à côté : contenant vide, donnée externe\nconst panier: string[] = [];   // [] tout seul n\'annonce rien de fiable\nconst brut: unknown = JSON.parse(localStorage.getItem("cfg") ?? "null");' },
            { t: 'p', h: 'La logique derrière cette liste : une **signature explicite** est un contrat qui survit aux refactorings (le compilateur prévient tous les appelants si tu changes d\'avis), tandis qu\'un **corps inféré** reste souple à modifier. Chaque annotation doit rapporter sa place : si elle ne dit rien que la valeur ne dise déjà, c\'est du bruit à supprimer — sinon, c\'est une documentation à garder précieusement.' },
            { t: 'h3', h: 'Quand l\'inférence se trompe : deux assistants à connaître' },
            { t: 'code', lang: 'ts', code:
'// as const : fige les valeurs en LITTÉRAUX (pas d\'élargissement)\nconst COULEURS = ["bleu", "vert", "rouge"] as const;   // readonly ["bleu","vert","rouge"]\nconst theme = { mode: "dark", taille: 16 } as const;    // toutes les props en readonly litéral\n\n// satisfies : vérifie le contrat SANS élargir le type inféré\nconst palette = {\n  accent: "#0a84ff",\n  fond: "#ffffff"\n} satisfies Record<string, string>;   // ✓ conforme, ET palette.accent reste "#0a84ff"' },
            { t: 'p', h: '`as const` transforme la valeur en type ultra-précis (littéraux + readonly), ce qui alimente les unions de littéraux et les configurations constantes — tu le rencontreras partout. `satisfies` est le juste milieu moderne : il EXIGE la forme demandée (erreur sinon) tout en laissant l\'inférence fine travailler. L\'un et l\'autre évitent le recours à l\'assertion `as`, qui ment sans prévenir quand elle se trompe (fiche Types spéciaux pour le blâme complet).' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« Un « vrai » projet TS annote chaque variable. »** — Non : c\'est le contraire. Frontières typées, intérieur inféré — l\'annotation n\'a de valeur qu\'aux endroits où elle DOCUMENTE ou CONTRAINT vraiment.',
              '**« L\'inférence peut changer d\'avis en cours de route. »** — Non : le type se fixe à l\'initialisation et se verrouille. Le réassigner à un autre type est l\'erreur rouge qui suit.',
              '**« const s = "x" est de type string. »** — Il est de type "x" (littéral). Ce n\'est qu\'avec let (ou la contrainte) que l\'élargissement à string intervient — d\'où sortent les literal types.',
              '**« any est une inférence qui a réussi. »** — C\'est un aveu d\'ignorance déguisé en succès. Avec noImplicitAny, le compilateur exige que tu l\'écrives de ta main — c\'est volontairement désagréable.',
              '**« [] vide n\'a pas besoin d\'annotation. »** — Sans contexte, TS ne peut pas deviner le contenu futur : `const panier: Produit[] = []` lève l\'ambiguïté dès la déclaration.',
              '**« as Type est une manière d\'annoter. »** — Non : c\'est une ASSERTION qui court-circuite la vérification. Préfère l\'annotation (vérifiée) ou satisfies (vérifié) quand ils suffisent.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Le widening prolonge la différence const/let de la fiche Variables du module JS : le mot-clé choisi AGIT sur le type. `noImplicitAny` configuré dans tsconfig est le gardien des cas où l\'inférence ne peut rien faire (paramètres non annotés). Et le trio « frontières/annotation », « intérieur/inférence », « externes/unknown » annonce proprement la répartition de tout le reste du module : types de base verrouillés, unions de littéraux, validation en entrée. La fiche suivante fixe justement le premier vocabulaire : les types scalaires.' },
            { t: 'callout', kind: 'tip', h: 'Heuristique de pro : **les frontières sont typées, l\'intérieur est inféré**. Quand une annotation n\'apprend rien de nouveau au lecteur, supprime-la ; quand elle ferme une porte dangereuse, elle rentabilise chaque caractère.' }
          ],
          errors: [
            { title: 'Sur-typer l\'évidence', lang: 'ts', bad:
'const nom: string = "Awa";\nconst prix: number = 500;\nconst dispo: boolean = true;\nfunction double(x: number): number { return x * 2; }', good:
'const nom = "Awa";        // string, inféré\nconst prix = 500;          // number, inféré\nconst dispo = true;        // boolean, inféré\nfunction double(x: number) { return x * 2; }   // retour inféré', why: '\': string\' après une chaîne littérale n\'apprend RIEN au compilateur ni au lecteur — c\'est du bruit visuel qui suggère que l\'auteur n\'a pas compris l\'inférence. L\'annotation se réserve aux endroits où elle DOCUMENTE ou CONTRAINT vraiment.' },
            { title: 'let sans valeur initiale, livré à any', lang: 'ts', bad:
'let statut;               // any « évolutif » discret\nstatut = "en_attente";\nstatut = 42;               // le compilateur hausse les épaules', good:
'let statut: string | undefined;\n// ou mieux : let statut: "en_attente" | "payee" | "livree"', why: 'Une variable déclarée sans valeur ni annotation entre dans le mode « evolving any » : son type se déduit de chaque affectation, ce qui neutralise localement la sécurité de strict. Nommer le type dès la déclaration verrouille le contrat.' }
          ],
          related: ['ts-types-scalaires', 'ts-tsc-config', 'ts-pourquoi', 'js-variables']
        }
      ]
    },

    {
      id: 'types-base',
      name: 'Types de base',
      icon: 'text_fields',
      fiches: [
        {
          id: 'ts-types-scalaires',
          title: 'string, number, boolean & tableaux',
          icon: 'title',
          level: 'Débutant',
          tagline: 'Le vocabulaire de base : primitives, tableaux typés, et le tableau de valeurs hétérogènes bien dompté.',
          intro: 'Tous les types TypeScript se construisent sur ce socle : les primitives de JavaScript (`string`, `number`, `boolean`, `null`, `undefined`) et leurs collections (`string[]`, `Array<number>`). C\'est le moment d\'apprendre à lire une annotation à voix haute — parce que lire `livrer(zone: string, articles: string[]): boolean` sans y penser, c\'est déjà de la documentation.',
          blocks: [
            { t: 'h3', h: 'Le problème : lire et écrire le contrat élémentaire' },
            { t: 'p', h: 'Avant les unions sophistiquées et les types génériques, il faut un alphabet. Dans le module JS, tu as rencontré les types à l\'exécution (`typeof`): ici, ce sont les mêmes valeurs, mais du côté du CONTRAT. La bonne nouvelle : il n\'y a presque rien à mémoriser — l\'alphabet reprend les mots de JavaScript. La mauvaise habitude à perdre en revanche : écrire `String` avec une majuscule (le type objet Java, jamais utile) au lieu de `string`.' },
            { t: 'h3', h: 'Les primitives taillées comme JavaScript' },
            { t: 'code', lang: 'ts', code:
'let nom: string = "Awa";\nlet prix: number = 3500;          // tous les nombres, entiers compris\nlet actif: boolean = true;\nlet adresse: null = null;         // le type singleton null\nlet photo: undefined;             // le type singleton undefined\n\nnom = 42;         // ✗ Type \'number\' is not assignable to type \'string\'\nprix = "3500 F";  // ✗ pareil\n\n// Symboles utiles en lisant les erreurs :\n//   \'string\' (guillemets simples) = le TYPE littéral "string", pas une chaîne quelconque' },
            { t: 'p', h: 'Trois points à graver d\'emblée. 1) `null` et `undefined` forment chacun leur propre type singleton — avec `strictNullChecks`, ils n\'entrent dans une variable QUE si tu l\'annonces : `let adresse: string | null`. 2) Les MAJUSCULES (`String`, `Number`, `Boolean`) désignent les objets wrapper de JavaScript : à bannir des annotations. 3) Dans les messages d\'erreur, TS cite les types entre guillemets simples — apprends à les lire à voix haute.' },
            { t: 'h3', h: 'Tableaux : deux notations, un seul choix par équipe' },
            { t: 'code', lang: 'ts', code:
'const prix: number[] = [500, 1200, 800];     // notation suffixe — la plus lue\nconst noms: Array<string> = ["Awa", "Kofi"];  // notation générique — équivalente\n\nprix.push("cher");        // ✗\nprix.push(2500);          // ✓ un tableau homogène reste homogène\n\n// Tableau MIXTE annoncé explicitement :\nconst ligne: (string | number)[] = ["gari", 500, "huile", 1200];' },
            { t: 'p', h: '`number[]` et `Array<number>` sont STRICTEMENT la même chose — la première se prononce « tableau de number », la deuxième est une forme générique dont tu verras l\'utilité aux Utility types (`ReadonlyArray<T>`, `Promise<T>`…). Choix d\'équipe : suffixe partout, sauf cas spécial. Le tableau homogène est la norme ; le mixte n\'est bienvenu que si tu l\'explicites, sinon TS te rendra la lecture pénible à chaque sortie de boucle.' },
            { t: 'h3', h: 'Sous le capot : ce que TS sait sur tes boucles' },
            { t: 'code', lang: 'ts', code:
'const produits = ["gari", "huile", "tissu"];   // string[] inféré\n\nfor (const p of produits) {\n  p.toUpperCase();     // ✓ ICI, p est string — autocomplétion complète\n  p.toFixed(2);        // ✗ toFixed n\'existe pas sur string !\n}\n\nproduits.map((p) => p.length);   // number[] inféré en sortie : éclatant' },
            { t: 'p', h: 'L\'inférence travaille dans les collections : `map` rend un tableau du type RETOURNÉ par ton callback (le `.map` ci-dessus produit `number[]` sans aucune annotation), `filter` garde le type d\'entrée, et chaque élément extrait hérite du type du tableau. Conséquence pratique : typer LE TABLEAU une fois (à la déclaration ou à la réception) profite à toutes les lignes qui le manipulent — c\'est la magie « typer aux frontières » appliquée aux collections.' },
            { t: 'h3', h: 'Méthodes raccourcies : +, //, /** */' },
            { t: 'code', lang: 'ts', code:
'// Un repère rapidement — tableaux en lecture seule :\nfunction verifier(tags: readonly string[]) {\n  tags.push("x");    // ✗ refusé : la fonction annonce qu\'elle NE MODIFIE PAS\n}\nverifier(prix.slice().map(String));  // pas d\'inquiétude pour l\'entrée' },
            { t: 'p', h: '`readonly string[]` est le contrat « je lis, je ne touche pas » : parfait pour les fonctions utilitaires. À l\'inverse, une liste passée à une fonction qui trie est généralement COPIÉE d\'abord (`[...tags].sort()`) — la discipline immuable qu\'on a déjà rencontrée dans les tableaux JS, maintenant exprimable dans le contrat même.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« string et String sont identiques. »** — Non : `string` est la primitive (ce qu\'on veut), `String` est l\'objet wrapper (jamais voulu en annotation). La même loi vaut pour Number/Boolean.',
              '**« number a besoin de distinctions int/float. »** — Non : tous les nombres JS vivent dans le même type `number`. Pour l\'entier, c\'est une convention de nommage, pas un type natif.',
              '**« null/undefined s\'invitent partout. »** — Seulement si tu les autorises dans le type (`string | null`). C\'est le cœur de strictNullChecks : les « vides » deviennent explicites.',
              '**« (string | number)[] est un tableau pratique. »** — Possible, mais rarement souhaitable : chaque sortie exige une vérification. Préfère un objet `{ nom, prix }` ou une ligne structurée.',
              '**« modifier un tableau reçu en paramètre, c\'est OK. »** — Techniquement possible, dangereux en pratique : l\'appelant garde sa référence. readonly + copie évitent le crash à distance.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Ces types prennent vie via l\'inférence du chapitre précédent — `const prix = [500, 800]` est `number[]` sans rien écrire — et ils répondent à strictNullChecks sur `null`/`undefined`. La prochaine notion les spécialise en *ensembles de valeurs précises* (tuples et enums), puis les 4 types spéciaux du bateau (`any`, `unknown`…) te donneront les portes d\'entrée/sortie sécurisées des données externes.' },
            { t: 'callout', kind: 'tip', h: 'Réflexe de lecture : lis les annotations à voix haute, naturellement — `livrer(zone: string, jours: number[]): boolean` se prononce « zone est une chaîne, jours est un tableau de nombres, et ça te répond vrai ou faux ». Si la phrase coule, le type est probablement bon.' }
          ],
          errors: [
            { title: 'String au lieu de string', lang: 'ts', bad:
'let nom: String = "Awa";     // l\'objet wrapper Java !\nnom.toUpperCase();            // passe… mais le type ment', good:
'let nom: string = "Awa";     // la primitive, la vraie\nnom.toUpperCase();           // ✓ et l\'autocomplétion est exacte', why: 'String (majuscule) désigne l\'objet wrapper de JS, pas la primitive : les deux ne sont pas interchangeables (typeof différent, égalité boiteuse, aucune utilité). En annotation TS, on écrit toujours les primitives en minuscules.' },
            { title: 'Un tableau mixte par facilité', lang: 'ts', bad:
'const ligne = ["gari", 500];          // (string | number)[] inféré\nconst libelle = ligne[0].toUpperCase();  // ✗ : il est peut-être number !', good:
'const ligne: [string, number] = ["gari", 500];   // tuple précis\nconst libelle = ligne[0].toUpperCase();           // ✓ string garanti\n// ou mieux le plus souvent : { nom: "gari", prix: 500 }', why: 'Un tableau mixte annoncé par l\'inférence rend chaque lecture incertaine : le compilateur ne sait plus quelle position abrite quel type. La fiche Tuples (suivante) montre l\'outil fait pour — à une position, un type.' }
          ],
          related: ['ts-inference', 'ts-tuples-enums', 'ts-types-speciaux', 'js-types-operateurs']
        },

        {
          id: 'ts-tuples-enums',
          title: 'Tuples & enums',
          icon: 'format_list_numbered',
          level: 'Intermédiaire',
          tagline: 'Tableaux à chaises numérotées et ensembles de constantes nommées — avec les alternatives modernes aux enums.',
          intro: 'Deux outils ciblés, souvent mal choisis : le **tuple** (un tableau dont chaque position a un type fixe) pour les petites lignes structurées, et l\'**enum** (des constantes nommées) pour les ensembles de valeurs finis — avec, pour chacun, l\'alternative moderne que beaucoup d\'équipes préfèrent aujourd\'hui. L\'objectif : sortir de cette fiche en sachant EXACTEMENT quand choisir quoi.',
          blocks: [
            { t: 'h3', h: 'Le tuple : un tableau à chaises numérotées' },
            { t: 'p', h: 'Le tableau mixte de la fiche précédente faisait voter au hasard : chaque sortie de `ligne[0]` exigeait une vérification. Le tuple remet de l\'ordre en déclarant chaque chaise : `[string, number]` = une chaîne d\'abord, un nombre ensuite, point final. La position porte le sens, les erreurs d\'ordre deviennent des erreurs de compilation, et l\'autocomplétion sait enfin où vous êtes.' },
            { t: 'code', lang: 'ts', code:
'// Une ligne de facture : nom, prix, quantité\nconst ligne: [string, number, number] = ["Gari premium", 500, 2];\n\nligne[0].toUpperCase();   // ✓ string garanti\nligne[1] * ligne[2];      // ✓ numeros garantis\nligne[1] = "cher";        // ✗ Type \'string\' is not assignable to type \'number\'\n\n// Le grand classique : un couple [latitude, longitude]\ntype Coordonnees = [number, number];\nconst dantokpa: Coordonnees = [6.3703, 2.3912];' },
            { t: 'p', h: 'Les usages qui durent : les paires qui reviennent souvent via des API (`useState` de React rend `[valeur, setter]` — un tuple !), les CSV parsés, les coordonnées. Sous le capot : un tuple EST un tableau, et sans précaution il accepte `push`/`pop` — atténue avec `readonly [string, number]` quand la taille est sacrée.' },
            { t: 'h3', h: 'Tuple ou objet ? La règle de décision' },
            { t: 'table', head: ['Situation', 'Choix', 'Pourquoi'], rows: [
              ['Paire homogène très conventionnelle (x, y) / (valeur, setter)', 'tuple', 'la convention parle ; l\'ordre est le sens'],
              ['Ligne avec 3+ champs métiers', 'objet', 'les noms de clés remplacent la mémorisation des positions'],
              ['Donnée renvoyée par un hook/API existante', 'tuple', 'tu épouses le format imposé'],
              ['Ligne de facture, d\'adresse, de commande', 'objet', '`{ nom, prix, qte }` reste lisible dans 6 mois']
            ]},
            { t: 'h3', h: 'Les enums : des constantes nommées' },
            { t: 'code', lang: 'ts', code:
'enum StatutCommande {\n  EnAttente,\n  Payee,\n  Expediee,\n  Livree\n}\n\nfunction avancer(statut: StatutCommande) {\n  if (statut === StatutCommande.Payee) {\n    preparerColis();\n  }\n}\navancer(StatutCommande.Payee);\navancer("payee");            // ✗ : ce n\'est PAS une chaîne !' },
            { t: 'p', h: 'L\'enum numérique (ci-dessus) assigne 0, 1, 2, 3 dans l\'ordre ; l\'enum à chaînes (`EnAttente = "en_attente"`) explicite les valeurs. Ils ont deux vertus : le NOM qui dit l\'intention, et la complétion dans l\'éditeur. MAIS — et c\'est le débat moderne — les enums génèrent un objet JavaScript au runtime (du code émis, une particularité dans l\'univers « les types s\'effacent »), et ils ne se mélangent pas avec les chaînes.' },
            { t: 'h3', h: 'L\'alternative montante : union de littéraux + as const' },
            { t: 'code', lang: 'ts', code:
'// Le même service, sans enum :\ntype StatutCommande = "en_attente" | "payee" | "expediee" | "livree";\n\nfunction avancer(statut: StatutCommande) {\n  if (statut === "payee") { preparerColis(); }\n}\navancer("payee");        // ✓ la chaîne brute EST valide — parfait avec des API JSON\n\n// Variante objet si tu veux des noms séparés des valeurs (i18n, labels) :\nconst STATUT_LABEL = {\n  en_attente: "En attente de paiement",\n  payee: "Payée (MoMo confirmé)",\n  expediee: "Expédiée par zémidjan",\n  livree: "Livrée au client"\n} as const;' },
            { t: 'p', h: 'L\'union de littéraux est le choix dominant aujourd\'hui : aucune émission de code, compatibilité directe avec le JSON des APIs (`"payee"` circule tel quel), et l\'exhaustivité du switch la couvre mieux (fiche Unions discriminées). L\'enum reste raisonnable pour un vocabulaire interne compact où l\'unification format/valeur est un vrai bonus ; pour le reste, préfère littéraux — sachant que les deux ne se convertissent PAS entre eux sans gymnastique.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« Un tuple est juste un tableau mixte. »** — Non : la POSITION fait le contrat. [string, number] refuse [number, string] ; un tableau mixte accepte n\'importe quel ordre.',
              '**« Les tuples peuvent s\'étirer avec push. »** — Par défaut oui : `readonly [..]` si la forme est un contrat.',
              '**« enum = un vrai type TypeScript sans coût. »** — Il émet du JS (objet runtime), exception notable de la règle « les types s\'effacent » ; l\'union de littéraux n\'émet rien.',
              '**« On compare enum et chaînes librement. »** — Non : `StatutCommande.Payee !== "payee"`. D\'où les unions de littéraux quand la donnée vient d\'une API.',
              '**« Les enums numériques sont lisibles dans les logs. »** — `StatutCommande.Payee` s\'affiche 1, pas "payee" : préfère l\'enum à chaînes ou les littéraux si le log compte.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Les tuples règlent le casse-tête du tableau mixte soulevé à la fiche précédente ; les unions de littéraux ouvrent la piste de toute la section Types avancés (tu vas les retrouver partout) ; et `as const` croisé à l\'inférence sert à nouveau ici. Reste à voir les quatre « types de passage » qui gouvernent les entrées/sorties incertaines — la fiche qui ferme ce chapitre : `any`, `unknown`, `never` et `void`.' },
            { t: 'callout', kind: 'tip', h: 'Raccourci de décision : donnée technique courte dont l\'ordre dit le sens → tuple ; vocabulaire de statuts couvrant l\'API → union de littéraux ; convention interne stable où le NOM compte → enum. À doute entre les deux derniers : littéraux.' }
          ],
          errors: [
            { title: 'Un enum numérique dans les logs/API', lang: 'ts', bad:
'enum Statut { Actif, Inactif }\nconsole.log(statut);          // 1 — « 1 » dans les logs/API : illisible\nenvoyer({ statut });          // le serveur reçoit 1, pas "inactif"', good:
'type Statut = "actif" | "inactif";\nconsole.log(statut);          // "inactif" — lisible en log ET en JSON\n// ou enum Statut { Actif = "actif", Inactif = "inactif" }', why: 'Les enums numériques transforment chaque valeur en entier opaque dans les logs et les payloads. L\'union de littéraux (ou l\'enum à chaînes) garde la donnée auto-descriptive partout.' },
            { title: 'Utiliser un tuple pour une ligne métier riche', lang: 'ts', bad:
'const adresse: [string, string, string, number, string] =\n  ["Rue 12.345", "Akpakpa", "Cotonou", 29, "Bénin"];\n// deux semaines après : les champs 3 et 0, c\'était quoi déjà ?', good:
'interface Adresse { rue: string; quartier: string; ville: string;\n                    numero: number; pays: string }\nconst adresse: Adresse = { rue: "Rue 12.345", quartier: "Akpakpa",\n  ville: "Cotonou", numero: 29, pays: "Bénin" };', why: 'Au-delà de deux positions relatives à une convention (x,y), mémoriser l\'ordre des champs devient un pari. Les NOMS de propriétés remplacent la position par du sens — et les fiches suivantes verrouillent le tout.' }
          ],
          related: ['ts-types-scalaires', 'ts-unions', 'ts-interfaces', 'js-tableaux']
        },

        {
          id: 'ts-types-speciaux',
          title: 'any, unknown, never & void',
          icon: 'help',
          level: 'Intermédiaire',
          tagline: 'Les quatre types de bord : la sortie de secours, le sas sécurisé, l\'impossible et le vide.',
          intro: 'Quatre types que tout le monde croise dans les messages d\'erreur avant de les comprendre : `any` (l\'amnésie volontaire), `unknown` (l\'amnésie honnête), `void` (l\'absence de retour) et `never` (l\'impossible lui-même). Leur mission commune : gérer proprement les **bords** de ton programme — entrées externes, retours sans valeur, branches impossibles — là où les types « normaux » ne savent pas quoi dire.',
          blocks: [
            { t: 'h3', h: 'any : la sortie de secours qui désarme tout' },
            { t: 'code', lang: 'ts', code:
'let truc: any = JSON.parse("{}");\n\ntruc.quelquechose.inexistant();   // ✓ compile… puis CRASH au runtime\ntruc = 42;                        // ✓ aucune résistance\ntotal(truc, "x");                 // ✓ et il CONTAMINE tout ce qu\'il touche' },
            { t: 'p', h: '`any` dit au compilateur : « arrête de vérifier cette valeur ». C\'est parfois nécessaire (le point d\'entrée d\'une donnée stratosphériquement dynamique), mais son comportement est nucléaire : non seulement il passe tous les tests, mais il CONTAMINE ce qu\'il touche — assigner `any` à une variable `string` passe sans objection, et l\'erreur ne resurgira qu\'au runtime. `noImplicitAny` existe précisément pour forcer l\'`any` à être un choix explicite plutôt qu\'un accident.' },
            { t: 'h3', h: 'unknown : le sas sécurisé, fait pour les données externes' },
            { t: 'code', lang: 'ts', code:
'let brut: unknown = await reponse.json();\n\nbrut.nom;              // ✗ Refusé : on ne sait RIEN de brut\nif (typeof brut === "string") {\n  brut.toUpperCase();  // ✓ ICI, TS sait que c\'est une string\n}\n// unknown exige une VÉRIFICATION avant usage : c\'est la différence radicale.' },
            { t: 'p', h: '`unknown` est le « any vertueux » : il accepte n\'importe quelle valeur (comme any) mais refuse toute utilisation sans preuve préalable. C\'est exactement le contrat d\'une frontière honnête : tu reçois du contenu incertain (fetch, JSON.parse, localStorage, formulaire), tu le ranges en `unknown`, puis tu PROUVES ce qu\'il est au moment de servir (typeof, guards maison — fiches Narrowing & Type guards). Tout ce que any ferait pour ton compte dans l\'ombre, unknown te le fait écrire.' },
            { t: 'h3', h: 'void vs never : le retour absent, deux façons' },
            { t: 'code', lang: 'ts', code:
'function journaliser(message: string): void {\n  console.log(message);        // rien n\'est retourné\n}\n\nfunction planter(raison: string): never {\n  throw new Error(raison);      // ne REND JAMAIS la main : l\'exception part\n}\n\nfunction boucle(): never {\n  while (true) { /* serveur */ } // idem : pas de « sortie normale »\n}' },
            { t: 'p', h: '`void` annonce « cette fonction ne RETOURNE rien d\'utile » — c\'est un marqueur d\'intention (tu peux techniquement `return;` dedans, jamais une valeur). `never`, lui, prouve « cette fonction ne TERMINE JAMAIS normalement » : elle lève ou boucle toujours. Où never redevient brillant, c\'est dans l\'**exhaustivité** : la variable `statut` ne peut plus avoir AUCUNE valeur, et si tu ajoutes un statut demain, c\'est le compilateur qui te montre où tu as oublié de le gérer (la recette complète arrive avec les unions discriminées).' },
            { t: 'h3', h: 'Le tableau de bord des quatre' },
            { t: 'table', head: ['Type', 'Se prononce', 'Résout', 'À utiliser quand'], rows: [
              ['`any`', '« peu importe »', 'fermer une erreur vite', 'EXCEPTIONNELLEMENT, en connaissance de cause'],
              ['`unknown`', '« inconnu à prouver »', 'ranger la donnée externe', 'fetch, JSON.parse, localStorage, saisies'],
              ['`void`', '« ne retourne rien »', 'typer les fonctions à effet', 'callbacks, loggers, effets de bord'],
              ['`never`', '« impossible »', 'branches impossibles, exhaustivité', 'switch exhaustifs, erreurs by design']
            ]},
            { t: 'h3', h: 'Sous le capot : never habite en bas de la hiérarchie' },
            { t: 'p', h: 'Dans la hiérarchie des types, `never` est le type **vide** : aucune valeur ne l\'habite, et il est assignable à tout (une fonction qui ne finit jamais peut « promettre » n\'importe quel retour — elle ne livrera jamais rien). À l\'opposé, `unknown` est le type sommet (tout y est assignable) mais inutilisable sans preuve, tandis qu\'`any` triche en jouant les deux rôles. Retenir cette géométrie, c\'est comprendre pourquoi `never` filtre si bien dans les unions (il s\'y évapore) — propriété exploitée plus tard par les conditional types.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« any fait juste « du JS sans TS » pour cette variable. »** — C\'est pire : any se propager (assignation, retours) et annule les garanties autour de lui. Un any local peut empoisonner toute une chaîne.',
              '**« unknown bloque le travail. »** — Il le PROLONGE honnêtement : chaque vérification écrite devient un garde réel au runtime, exactement là où JS se crashait.',
              '**« void et undefined, c\'est pareil. »** — Non : void est le type du RETOUR d\'une fonction à effet ; undefined est une valeur (et un type). Une fonction `: undefined` doit écrire `return undefined;`, pas négliger le return.',
              '**« never ne sert à rien. »** — Sans lui, l\'exhaustivité des unions discriminées ne prouve rien : c\'est lui qui rend « tous les cas sont gérés » vérifiable par la machine.',
              '**« as Type résout unknown élégamment. »** — as est une assertion qui court-circuite : elle efface l\'erreur sans vérifier. À réserver aux cas où tu sais quelque chose que TS ne PEUT pas savoir — jamais pour faire taire le compilateur.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Ces quatre types répondent aux appels lancés dès la première fiche : `unknown` donne un toit à la donnée API (fiche « Pourquoi TS »), `noImplicitAny` du tsconfig pousse vers `any` explicite, et `void` complète les signatures de fonctions auxquelles l\'inférence ne peut pas répondre seule. La pièce manquante est la « preuve » devant `unknown` : les prochaines fiches (Interfaces pour la forme, puis Narrowing pour les vérifications) la fournissent.' },
            { t: 'callout', kind: 'warn', h: '`unknown` est l\'ami des frontières, `any` le mensonge commode. Quand tu hésites entre les deux, pose-toi cette question : « est-ce que je suis prêt à ce que CETTE ligne se crash en prod ? » — unknown rend la réponse écrite.' }
          ],
          errors: [
            { title: 'Faire taire le compilateur avec as', lang: 'ts', bad:
'const cfg = JSON.parse(text) as Config;\n// OK « parce que je sais » — jusqu\'à ce qu\'un champ disparaisse du fichier', good:
'const brut: unknown = JSON.parse(text);\nconst cfg = estConfig(brut) ? brut : CONFIG_DEFAUT;', why: '`as` est une assertion sans vérification : elle écrit ce que TS doit croire. Dès que la réalité change (fichier tronqué, API modifiée), le crash arrive SANS aucun indice préalable. unknown + une preuve, c\'est la même confiance — avec une ceinture de sécurité.' },
            { title: 'Recopier les données sans vérification via any', lang: 'ts', bad:
'const lignes: any[] = await rep.json();\nconst totaux = lignes.map((l) => l.montant * l.qte);   // l = any, silence total', good:
'const lignes: unknown = await rep.json();\nif (Array.isArray(lignes)) {\n  const produits = lignes.filter(estLigneValide);  // gardes réelles\n  const totaux = produits.map((l) => l.montant * l.qte);\n}', why: 'Un `any[]` « pour aller vite » neutralise le compilateur sur TOUTE la chaîne de traitement : le premier bug se révèle en prod, sur une vraie transaction. Le schéma unknown → vérifications → données propres garde chaque étape couverte.' }
          ],
          related: ['ts-narrowing', 'ts-type-guards', 'ts-pourquoi', 'js-erreurs']
        }
    ]
  },
  {
      id: 'interfaces',
      name: 'Interfaces',
      icon: 'handshake',
      fiches: [
        {
          id: 'ts-interfaces',
          title: 'Interfaces : contrats d\'objets',
          icon: 'assignment',
          level: 'Débutant',
          tagline: 'Décris une fois la forme d\'un objet ; le compilateur l\'exige ensuite partout, gratuitement.',
          intro: 'Une interface est un **contrat de forme** : elle énumère les propriétés qu\'un objet doit posséder — noms ET types — pour être accepté par une fonction, un composant, une API. Elle ne fabrique rien, ne s\'instancie pas, et disparaît sans laisser de trace à la compilation : c\'est de la pure spécification. Pourquoi s\'en soucier ? Parce que les trois quarts d\'une application réelle consistent à faire circuler des objets qui se ressemblent — un produit de la Boutique Awa, une ligne de commande, un membre de tontine. Nommer ces formes une fois, puis les exiger partout, transforme « j\'espère que cet objet a bien un prix » en « le compilateur le garantit ». Cette fiche pose les fondations : syntaxe, optionnel, readonly, extension — et le typage structurel, la mécanique qui rend le tout si naturel.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : une forme écrite une fois, exigée partout' },
            { t: 'p', h: 'Repense au module JavaScript : un objet, c\'est un sac de clés-valeurs où rien n\'est promis. Tant que tout tient dans un fichier de vingt lignes, ta mémoire suffit. Mais le catalogue de la Boutique Awa, lui, voyage : création du produit, mise en rayon, panier, facture, bilan du soir — cinq fonctions, cinq occasions d\'oublier une clé ou d\'y ranger une string à la place d\'un nombre. Sans contrat, chaque fonction FAIT CONFIANCE au maillon précédent. L\'interface inverse la charge : tu décris la forme UNE fois, tu lui donnes un nom — `Produit` — et chaque fonction EXIGE ce nom. Désormais, la faute de frappe `preix` ou l\'oubli de `stock` sont refusés à l\'entrée, à l\'écriture, avant même la sauvegarde.' },
            { t: 'h3', h: 'Une interface minimale' },
            { t: 'code', lang: 'ts', label: 'premiere-interface.ts', code:
'interface Produit {\n  nom: string;\n  prix: number;   // en FCFA\n  stock: number;\n}\n\nfunction fiche(p: Produit): string {\n  return p.nom + \" — \" + p.prix + \" F (stock : \" + p.stock + \")\";\n}\n\nfiche({ nom: \"Gari premium 50 kg\", prix: 21000, stock: 12 });    // ✓\nfiche({ nom: \"Gari premium 50 kg\", prix: 21000 });               // ✗ il manque stock\nfiche({ nom: \"Gari premium 50 kg\", prix: \"21000\", stock: 12 }); // ✗ prix n\'est pas un number' },
            { t: 'p', h: 'Lecture en trois temps. 1) Le mot-clé `interface`, un nom en PascalCase par convention, puis l\'inventaire des propriétés entre accolades, chacune annotée — les séparateurs `;` et `,` sont interchangeables, choisis-en un et tiens-t\'y. 2) L\'annotation `p: Produit` sur le paramètre transforme la signature en portier : seuls les objets conformes entrent. 3) Remarque ce qui N\'EXISTE PAS : pas de `new`, pas de fonction générée, pas de valeur créée. `Produit` n\'est qu\'un nom donné à une forme ; à l\'exécution, il n\'en reste rien.' },
            { t: 'h3', h: 'Optionnelles avec `?`, figées avec `readonly`' },
            { t: 'code', lang: 'ts', label: 'reglages-fins.ts', code:
'interface Produit {\n  readonly id: string;   // attribué à la création, inchangeable ensuite\n  nom: string;\n  prix: number;\n  stock: number;\n  promo?: number;        // réduction éventuelle, en FCFA\n}\n\nconst p: Produit = { id: \"GARI-50\", nom: \"Gari premium\", prix: 21000, stock: 12 };\n// promo est absente : parfaitement légal\n\np.id = \"AUTRE\";   // ✗ readonly : le socle ne bouge plus\np.stock = 11;      // ✓ les autres champs restent vivants' },
            { t: 'p', h: 'Deux réglages fins, deux intentions différentes. `?` déclare une propriété QUI PEUT MANQUER : son type effectif devient `number | undefined`, et le code qui la consomme devra gérer l\'absence (le drapeau `exactOptionalPropertyTypes` distingue même « absente » et « explicitement undefined » — nuance d\'expert, retiens le principe). `readonly` interdit la RÉASSIGNATION du champ après création : parfait pour les identifiants, les dates de création, tout ce qui fait socle. Attention, c\'est un verrou superficiel : il protège la case, pas le contenu pointé — un tableau déclaré `readonly lignes` accepte encore `push`, comme le montre la deuxième carte d\'erreur en bas de fiche.' },
            { t: 'h3', h: 'Décrire aussi les fonctions et les dictionnaires' },
            { t: 'code', lang: 'ts', label: 'signatures.ts', code:
'// Une signature d\'appel : la forme d\'une fonction\ninterface FormaterPrix {\n  (montant: number): string;\n}\nconst enFcfa: FormaterPrix = (m) => m.toLocaleString(\"fr-FR\") + \" FCFA\";\n\n// Une signature d\'index : un dictionnaire à clés dynamiques\ninterface TarifsZem {\n  [quartier: string]: number;   // toute clé texte renvoie un prix\n}\nconst tarifs: TarifsZem = {\n  \"Dantokpa -> Akpakpa\": 300,\n  \"Dantokpa -> Calavi\": 700,\n  \"Calavi -> Godomey\": 400\n};\ntarifs[\"Dantokpa -> Ganhi\"] = 350;   // ✓ clé nouvelle, forme respectée' },
            { t: 'p', h: 'Une interface ne décrit pas que des sacs de données. La signature d\'appel `(montant: number): string` décrit une FONCTION conforme — utile pour typer un callback réutilisé dans tout le projet. La signature d\'index `[quartier: string]: number` décrit un dictionnaire dont on ignore les clés à l\'avance, mais dont toutes les valeurs partagent un type — typique des tarifs, caches et tables de correspondance. La fiche `Record` te donnera plus tard un raccourci d\'écriture pour ce second cas ; la signature d\'index reste la mécanique sous-jacente, celle qu\'il faut savoir lire.' },
            { t: 'h3', h: 'Composer avec `extends` : hériter d\'un contrat' },
            { t: 'code', lang: 'ts', label: 'extends.ts', code:
'interface Perissable extends Produit {\n  joursConservation: number;\n}\n\nconst gari: Perissable = {\n  id: \"GARI-50\", nom: \"Gari premium 50 kg\",\n  prix: 21000, stock: 12,\n  joursConservation: 90        // champs hérités + nouveau : tout est exigé\n};\n\n// Plusieurs parents, séparés par des virgules :\ninterface Etiquetable { reference: string; }\ninterface Denree extends Produit, Etiquetable {\n  origine: string;\n}' },
            { t: 'p', h: '`extends` signifie « tout ce que le parent exige, plus ceci » : l\'enfant hérite de chaque champ, et la liste complète devient obligatoire à la création de l\'objet. C\'est de la COMPOSITION DE CONTRATS, pas de l\'héritage d\'implémentation — aucun code ne voyage, seulement des exigences. Bonus de sécurité : si deux parents exigent le même champ avec des types incompatibles, le compilateur refuse la fusion à la déclaration. Mieux vaut une alarme au moment d\'écrire qu\'un objet contradictoire en production.' },
            { t: 'h3', h: 'Sous le capot : le typage STRUCTUREL' },
            { t: 'p', h: 'Voici la mécanique qui distingue TypeScript de Java ou de C#. TS ne compare jamais les NOMS de types : il compare les FORMES. Un objet qui possède les bonnes propriétés avec les bons types EST un `Produit`, sans avoir à déclarer « j\'implémente Produit » — c\'est le fameux « duck typing » de JavaScript (« si ça cancane comme un canard… »), mais vérifié statiquement. Conséquence directe : l\'interface n\'est pas un moule d\'usine, c\'est un GABARIT DE CONTRÔLE qu\'on plaque sur n\'importe quel objet — un littéral, le résultat d\'un fetch validé, une donnée relue depuis localStorage. Si le gabarit passe, l\'objet entre.' },
            { t: 'code', lang: 'ts', label: 'structurel.ts', code:
'interface Point { x: number; y: number; }\n\nfunction distance(a: Point, b: Point): number {\n  return Math.hypot(a.x - b.x, a.y - b.y);\n}\n\nconst repere = { x: 6.37, y: 2.35, altitude: 12 };  // jamais annoté « Point »\ndistance(repere, { x: 6.5, y: 2.4 });               // ✓ la forme suffit\n\n// Deux interfaces de forme identique sont interchangeables :\ninterface Coords { x: number; y: number; }\nconst c: Coords = repere;   // ✓ Point et Coords : même forme, même laissez-passer' },
            { t: 'h3', h: 'La vérification d\'excès : les littéraux « frais » sous la loupe' },
            { t: 'p', h: 'Un détail déroute tout le monde au début : un objet écrit DIRECTEMENT comme littéral est inspecté plus sévèrement qu\'un objet d\'abord rangé dans une variable. La logique est fine mais juste : dans un littéral neuf, une clé en trop est presque toujours une faute de frappe ou un reste de copier-coller — TS la signale. En revanche, transporter via une variable un objet plus riche que nécessaire est légitime : le receveur ignore simplement le surplus. Règle pratique : quand une clé fantôme te semble filer, colle le littéral directement dans l\'appel — la « vérification d\'excès de propriétés » (excess property check) l\'attrapera.' },
            { t: 'code', lang: 'ts', label: 'exces.ts', code:
'const saisie = { nom: \"Pagne bogolan\", prix: 8500, stock: 30, magasin: \"Dantokpa\" };\n\n// ✗ refusé : le littéral frais est inspecté à la loupe\nfiche({ nom: \"Pagne bogolan\", prix: 8500, stock: 30, magasin: \"Dantokpa\" });\n//                                                ~~~~~~~ inconnu de Produit\n\n// ✓ accepté : via une variable, l\'excédent est simplement ignoré\nfiche(saisie);' },
            { t: 'h3', h: 'Quand créer une interface (et quand s\'en passer)' },
            { t: 'table', head: ['Situation', 'Interface nommée ?', 'Pourquoi'], rows: [
              ['Forme réutilisée par plusieurs fonctions', 'oui', 'le nom voyage mieux que la redite'],
              ['Frontière d\'API ou de module', 'oui', 'le contrat EST la documentation'],
              ['Donnée métier centrale (Produit, Commande, Membre)', 'oui', 'elle mérite un nom dans le domaine'],
              ['Objet de config lu une fois, à un seul endroit', 'annotation inline possible', 'un nom porté par rien alourdit'],
              ['Brouillon de script jetable', 'inutile', 'l\'inférence suffit largement']
            ]},
            { t: 'h3', h: 'Exemple fil rouge : la commande de la Boutique Awa' },
            { t: 'code', lang: 'ts', label: 'commande.ts', code:
'interface Produit {\n  readonly id: string;\n  nom: string;\n  prix: number;\n}\n\ninterface LigneCommande {\n  produit: Produit;\n  quantite: number;\n}\n\ninterface Commande {\n  readonly numero: string;\n  lignes: LigneCommande[];\n  payee: boolean;\n  moyenPaiement?: string;   // raffiné en littéraux dans la fiche Unions\n}\n\nfunction total(c: Commande): number {\n  let somme = 0;\n  for (const l of c.lignes) {\n    somme += l.produit.prix * l.quantite;   // chaque étage est contrôlé\n  }\n  return somme;\n}\n\nconst cmd: Commande = {\n  numero: \"CMD-1042\",\n  lignes: [\n    { produit: { id: \"GARI-50\", nom: \"Gari premium 50 kg\", prix: 21000 }, quantite: 2 },\n    { produit: { id: \"HUILE-5L\", nom: \"Huile rouge 5 L\", prix: 6500 }, quantite: 1 }\n  ],\n  payee: true,\n  moyenPaiement: \"momo\"\n};\ntotal(cmd);   // 48500 — chaque objet, à chaque étage, a montré patte blanche' },
            { t: 'p', h: 'Observe l\'emboîtement : `Commande` contient des `LigneCommande`, qui contiennent des `Produit` — exactement comme les données réelles s\'emboîtent. Modéliser un domaine, c\'est précisément ça : nommer les formes à chaque étage et laisser le compilateur vérifier la chaîne entière. Une faute de frappe à trois niveaux de profondeur est attrapée aussi sûrement qu\'en surface, et la lecture du fichier `types.ts` suffit à comprendre le métier — le contrat fait office de documentation qui ne peut pas mentir.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« interface = classe »** — non : une classe FABRIQUE des objets (constructeur, méthodes, code réel) ; une interface DÉCRIT une forme puis disparaît à la compilation. `new Produit()` n\'existera jamais.',
              '**« readonly fige tout »** — non : il verrouille la réassignation du champ, pas le contenu pointé. Un tableau déclaré `readonly lignes` accepte encore `push`. Le gel profond exige `readonly` + `readonly T[]` — ou `Object.freeze` au runtime.',
              '**« `?` veut dire que je recevrai undefined »** — pas exactement : cela veut dire « peut être absente ». `Object.keys` ne la verra pas, l\'opérateur `in` répondra `false`. Avec `exactOptionalPropertyTypes`, « absente » et « explicitement undefined » deviennent même deux cas distincts.',
              '**« il faut déclarer qu\'un objet implémente l\'interface »** — jamais : le typage est structurel. Si la forme colle, l\'objet est accepté, qu\'il vienne d\'un littéral, d\'un `JSON.parse` validé ou d\'une interface jumelle au même gabarit.',
              '**« une interface coûte au runtime »** — zéro octet, zéro cycle : elle est effacée à l\'émission. Son seul coût est un peu de réflexion au moment d\'écrire — le meilleur investissement du projet.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Tu réutilises ici les objets du module JS (`js-objets` : propriétés, accès, parcours) en y ajoutant le contrat qui manquait. Les annotations de base viennent de `ts-types-scalaires`, et l\'esprit « typer les frontières, laisser inférer l\'intérieur » de `ts-inference` — l\'interface EST la frontière par excellence. La suite logique : `ts-interface-vs-type` pour trancher le débat d\'écriture, puis `ts-classes`, où le mot-clé `implements` branche une véritable fabrique d\'objets sur un contrat d\'interface.' },
            { t: 'callout', kind: 'tip', h: 'Réflexe d\'équipe : les formes principales portent les noms du MÉTIER (Produit, Commande, MembreTontine) et vivent dans un fichier `types.ts` partagé. Quand le vocabulaire du code épouse celui de la Boutique, le compilateur devient le gardien officiel du langage commun — et les nouveaux arrivants lisent le domaine avant même le code.' }
          ],
          errors: [
            { title: 'Instancier une interface comme une classe', lang: 'ts', bad:
'const p = new Produit();   // ✗ \"Produit\" ne désigne qu\'un type,\n                           // rien à exécuter : il n\'existe plus à l\'émission', good:
'const p: Produit = { id: \"GARI-50\", nom: \"Gari\", prix: 21000, stock: 12 };\n// ou, pour fabriquer en série avec comportement : une CLASSE\n// (l\'interface en décrira alors la forme, via implements)', why: 'Une interface est de la PURE SPÉCIFICATION : effacée à la compilation, elle ne produit ni valeur ni constructeur. Confondre les deux, c\'est confondre le plan de la maison avec la maison. Pour « fabriquer » des objets : littéral direct, fonction de création (factory), ou classe.' },
            { title: 'Croire que `readonly` fige le contenu', lang: 'ts', bad:
'interface Panier { readonly lignes: string[] }\nconst panier: Panier = { lignes: [\"gari\"] };\npanier.lignes = [];          // ✗ bloqué, ouf\npanier.lignes.push(\"riz\");   // ✓ accepté : le tableau, lui, reste muable', good:
'interface Panier { readonly lignes: readonly string[] }\n// là, push est refusé : un tableau readonly n\'expose que la lecture\n// (et Object.freeze([...]) verrouille aussi AU RUNTIME)', why: '`readonly` verrouille la CASE (la réassignation du champ), pas l\'objet pointé. Pour un tableau vraiment figé, combine le readonly de la propriété et le tableau readonly (`ReadonlyArray<string>`) — et si le gel doit tenir à l\'exécution, seul `Object.freeze` agit au runtime.' }
          ],
          related: ['ts-interface-vs-type', 'ts-classes', 'ts-types-scalaires', 'js-objets']
        },
        {
          id: 'ts-interface-vs-type',
          title: 'interface vs type : la vraie différence',
          icon: 'compare_arrows',
          level: 'Intermédiaire',
          tagline: 'Deux syntaxes, 90 % de chevauchement — et des marges qui changent tout au quotidien.',
          intro: '`interface` et `type` décrivent tous deux des formes d\'objets — et se ressemblent tellement que la question « lequel choisir ? » a bloqué des équipes entières en réunion. La vérité tient en trois phrases : pour un objet classique, les deux marchent pareil ; les différences réelles vivent aux MARGES (unions et tuples réservés à `type`, fusion de déclarations et erreurs plus lisibles pour `interface`) ; et la cohérence d\'équipe compte plus que le choix lui-même. Cette fiche ouvre les deux boîtes, compare point par point, et te donne la règle de décision que tu appliqueras les yeux fermés.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : un débat qui mérite d\'être tranché une fois pour toutes' },
            { t: 'p', h: 'Un peu d\'histoire, parce qu\'elle explique la confusion : au tout début de TypeScript, `type` est né comme simple ALIAS — « ce long type pénible, appelons-le `Callback` » — tandis qu\'`interface` était LE moyen de décrire les objets. Puis chaque version du langage a élargi `type` : les formes d\'objets d\'abord, les mapped types et les conditionnels ensuite. Résultat aujourd\'hui : pour décrire `{ nom: string; prix: number }`, les deux syntaxes sont équivalentes au caractère près, et le compilateur applique la même logique structurelle aux deux. Le vrai sujet n\'est donc pas « lequel est correct » mais « lequel exprime le mieux mon intention — et que se passe-t-il quand mon besoin sort du simple objet ? »' },
            { t: 'h3', h: 'Le même contrat, deux écritures' },
            { t: 'code', lang: 'ts', label: 'equivalence.ts', code:
'interface ProduitI {\n  nom: string;\n  prix: number;\n}\n\ntype ProduitT = {\n  nom: string;\n  prix: number;\n};\n\n// Interchangeables, parce que le typage est structurel :\nconst a: ProduitI = { nom: \"Gari premium\", prix: 21000 };\nconst b: ProduitT = a;   // ✓ même forme : aucune conversion, aucun effort' },
            { t: 'p', h: 'Rien dans cet exemple ne distingue les deux déclarations : mêmes objets acceptés, mêmes erreurs signalées, même effacement à la compilation. Si le débat s\'arrêtait là, cette fiche n\'existerait pas. Il reprend aux marges — et ces marges sont précisément ce que tu paies chaque semaine sur un vrai projet : lisibilité des erreurs, évolutions, refactors.' },
            { t: 'h3', h: 'Ce que seul `type` sait faire' },
            { t: 'code', lang: 'ts', label: 'seul-type.ts', code:
'// 1) Les unions — le cœur du réacteur\ntype MoyenPaiement = \"momo\" | \"moov\" | \"especes\";\n\n// 2) Les alias de primitifs et leurs compositions\ntype Fcfa = number;\ntype Id = string | number;\n\n// 3) Les tuples\ntype LigneRetour = [boolean, string];   // [succès, message]\n\n// 4) Les fonctions flèches, en une ligne\ntype FormaterPrix = (montant: number) => string;\n\n// 5) Les mapped / conditional types (fiches dédiées plus loin)\ntype Gel<T> = { readonly [K in keyof T]: T[K] };' },
            { t: 'p', h: 'La frontière est nette : dès que ton type n\'est PAS « simplement un objet », c\'est `type` qu\'il faut. Une union n\'a pas de forme unique à « implémenter » : elle EST une alternative — impossible à exprimer avec `interface`. Même verdict pour un primitif renommé, un tuple ordonné, une fonction flèche, ou les types calculés que tu rencontreras dans les fiches Utility types et Manipulation de types. Quand le besoin dépasse le sac de propriétés, la question est tranchée d\'avance — `interface` déclare forfait.' },
            { t: 'h3', h: 'Ce que seule `interface` sait faire' },
            { t: 'code', lang: 'ts', label: 'seule-interface.ts', code:
'// La fusion de déclarations : deux blocs, un seul contrat\ninterface MembreTontine { nom: string; }\ninterface MembreTontine { quartier: string; }\n// MembreTontine vaut désormais { nom, quartier } : les blocs FUSIONNENT.\n// (avec « type », la seconde déclaration serait une erreur de doublon)\n\n// Cas d\'usage légitime n° 1 : augmenter les types d\'une bibliothèque\ndeclare global {\n  interface Window {\n    boutiqueVersion: string;   // ta config devient disponible et typée\n  }\n}' },
            { t: 'p', h: 'Deux autres avantages, moins spectaculaires mais quotidiens. D\'abord l\'extension : `interface B extends A` s\'écrit et se lit comme un héritage de contrat, quand `type B = A & { ... }` tisse une intersection — même résultat la plupart du temps, mais `extends` signale immédiatement les champs en conflit, là où l\'intersection peut avaler une incompatibilité en silence (jusqu\'au `never` surprise). Ensuite, les DIAGNOSTICS : `tsc` conserve le NOM d\'une interface dans ses messages (« Type X is not assignable to type \'Produit\' »), alors qu\'un alias d\'objet tend à s\'afficher déplié champ par champ — illisible sur les grosses formes.' },
            { t: 'h3', h: 'Le comparatif complet' },
            { t: 'table', head: ['Capacité', 'interface', 'type'], rows: [
              ['Forme d\'objet classique', '✓', '✓'],
              ['Unions (`"a" | "b"`)', '✗', '✓'],
              ['Alias de primitif / tuple / fonction', '✗', '✓'],
              ['Mapped & conditional types', '✗', '✓'],
              ['Fusion de déclarations', '✓', '✗ (doublon interdit)'],
              ['Extension d\'un contrat', '`extends` — conflits détectés', '`&` — peut avaler un conflit'],
              ['Nom affiché dans les erreurs', 'conservé', 'souvent déplié'],
              ['Coût au runtime', 'zéro', 'zéro']
            ]},
            { t: 'h3', h: 'La règle de la maison' },
            { t: 'p', h: 'Après des années de débats communautaires, un consensus simple s\'est dégagé — le même que recommande l\'équipe TypeScript : **interface par défaut pour les objets, type pour tout le reste**. Interface pour les formes de données métier et les contrats publics (extensibles, erreurs nommées, fusion disponible pour les consommateurs d\'une librairie) ; type dès qu\'apparaît une union, un alias de primitif, un tuple, une fonction ou un calcul de types. Et la règle au-dessus des règles : quelle que soit la convention choisie, applique-la PARTOUT. Un projet qui tranche une fois dans son README lit infiniment mieux qu\'un projet qui « optimise » fichier par fichier.' },
            { t: 'h3', h: 'Sous le capot : effacement identique, évaluation différente' },
            { t: 'p', h: 'Au runtime, rien à comparer : les deux disparaissent intégralement — le JavaScript émis est octet pour octet identique. Sous le capot DU VÉRIFICATEUR, en revanche, il y a une vraie nuance : les interfaces sont identifiées par leur nom et leurs membres résolus à la demande, quand les alias (surtout les intersections profondes) étaient historiquement dépliés plus tôt et plus souvent. Sur les très gros codebases, cette différence de stratégie a longtemps donné aux interfaces un léger avantage de vitesse de vérification et de stabilité des « instantanés de types » affichés par l\'éditeur. L\'écart s\'est sérieusement réduit dans les versions récentes, mais il explique pourquoi les recommandations officielles penchent encore côté interface pour les formes d\'objets.' },
            { t: 'h3', h: 'Exemple : le module paiements de la Boutique Awa' },
            { t: 'code', lang: 'ts', label: 'paiements.ts', code:
'// Formes d\'objets -> interface\ninterface Paiement {\n  readonly id: string;\n  montant: number;\n  date: Date;\n}\n\n// Alternatives et compositions -> type\ntype MoyenPaiement = \"momo\" | \"moov\" | \"especes\";\ntype StatutPaiement = \"initie\" | \"confirme\" | \"echoue\";\n\n// Croisement de deux formes -> type avec &\ntype PaiementTrace = Paiement & {\n  moyen: MoyenPaiement;\n  reference: string;   // ex : référence de transaction MoMo\n};\n\nfunction enregistrer(p: PaiementTrace): StatutPaiement {\n  return p.montant > 0 ? \"confirme\" : \"echoue\";\n}\n\nenregistrer({\n  id: \"PAY-2207\", montant: 21000, date: new Date(),\n  moyen: \"momo\", reference: \"MOMO-8F3K2\"\n});   // ✓ chaque choix d\'écriture épouse la nature du type' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« type est moderne, interface est dépassée »** — faux : les deux sont activement maintenus et grandissent encore à chaque version. Ce qui les distingue, ce sont des capacités marginales (fusion contre unions), pas la modernité.',
              '**« l\'un est plus performant »** — à l\'exécution, rien n\'existe : les deux sont effacés. Les écarts de vitesse de VÉRIFICATION sur très gros projets existent mais restent secondaires face à la lisibilité.',
              '**« extends et & sont strictement pareils »** — presque, pas tout à fait : `extends` détecte les conflits de champs incompatibles à la déclaration ; `&` peut produire silencieusement une propriété de type `never` qui n\'explosera qu\'à l\'usage.',
              '**« il faut convertir entre interface et type »** — aucune conversion n\'existe ni n\'est nécessaire : le typage structurel rend les deux interchangeables dès que la forme colle.',
              '**« on peut fusionner deux `type` du même nom »** — non : le doublon est une erreur immédiate. La fusion de déclarations est l\'apanage des interfaces, et c\'est voulu : elle sert à AUGMENTER des contrats existants.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Cette fiche prolonge directement `ts-interfaces` — même mécanique structurelle, autres outils d\'écriture — et prépare `ts-unions`, où le mot-clé `type` devient l\'unique possibilité. L\'histoire des messages d\'erreur nommés fait écho à `ts-tsc-config` : ton confort quotidien dépend autant des diagnostics que des contrats. Enfin, les mapped et conditional types évoqués en passant trouveront leur mode d\'emploi complet dans `ts-utility-types`, `ts-keyof-typeof` et `ts-conditional-types`.' },
            { t: 'callout', kind: 'tip', h: 'Test d\'une seconde en revue de code : « ce type est-il un objet, ou autre chose ? ». Objet → `interface`. Autre chose (alternative, alias, tuple, fonction, calcul) → `type`. La réponse devient un réflexe, et la convention ne demande qu\'à être écrite une fois pour toutes dans le README.' }
          ],
          errors: [
            { title: 'Étendre une union avec une interface', lang: 'ts', bad:
'type Statut = \"payee\" | \"en_retard\";\ninterface Detaillee extends Statut { note: string }\n// ✗ une interface ne peut étendre qu\'une forme d\'objet, pas une alternative', good:
'type Statut2 = Statut | \"annulee\";                       // ✓ composer l\'union\ntype Detaillee = Paiement & { note: string };            // ou croiser des objets' , why: '`extends` exige une forme d\'objet nommable et statique ; une union est un CHOIX entre alternatives — on l\'enrichit avec `|`, on la croise avec `&`, mais on n\'en « hérite » pas. Cette erreur est en fait une excellente nouvelle : elle t\'oblige à choisir le bon connecteur pour la bonne intention.' },
            { title: 'Croire que le choix change le code émis', lang: 'ts', bad:
'// « Je prends interface partout, paraît-il que c\'est plus rapide à l\'exécution. »', good:
'// Les deux sont effacés intégralement : le JS émis est identique,\n// octet pour octet. Choisis pour la lisibilité et les capacités\n// (unions, fusion, messages d\'erreur) — jamais pour la performance.', why: 'Interfaces et alias vivent et meurent dans la phase de vérification : aucune trace n\'atteint le runtime. Le débat `interface` vs `type` est un débat d\'EXPRESSIVITÉ et d\'équipe — la seule « performance » en jeu est celle de ton cerveau dans six mois, quand il relira le code.' }
          ],
          related: ['ts-interfaces', 'ts-unions', 'ts-utility-types', 'ts-inference']
        }
      ]
    },
  {
      id: 'types-avances',
      name: 'Types avancés',
      icon: 'merge_type',
      fiches: [
        {
          id: 'ts-unions',
          title: 'Unions, intersections & literal types',
          icon: 'call_split',
          level: 'Intermédiaire',
          tagline: '« l\'un OU l\'autre » avec |, « tout à la fois » avec & — la grammaire des alternatives.',
          intro: 'Jusqu\'ici, chaque annotation disait « c\'est exactement CECI ». Le monde réel préfère « c\'est ceci OU cela » : une course de zémidjan se paie par MTN MoMo, Moov Money ou en espèces ; un identifiant est une string ou un nombre ; un statut vaut en cours, payé ou annulé. L\'**union** `|` modélise ces alternatives fermées, les **literal types** donnent à des valeurs précises le statut de types, et l\'**intersection** `&` fait l\'opération inverse — exiger plusieurs formes simultanément. C\'est le cœur expressif de TypeScript, celui qui transforme des « conventions orales » en frontières vérifiées par la machine.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : le réel est fait d\'alternatives fermées' },
            { t: 'p', h: 'À la caisse de la Boutique Awa, personne ne paie « avec du moyen de paiement » : on paie par MoMo, par Moov Money, ou en espèces — et la VÉRIFICATION n\'est pas la même selon le cas (référence de transaction d\'un côté, comptage des billets de l\'autre). Le JavaScript naïf écrit `function encaisser(montant, moyen)` et confie tout à la vigilance : la variable `moyen` peut recevoir n\'importe quoi, y compris la faute de frappe `"mono"` qui fera échouer la vente en silence. L\'union déclarative inscrit la règle dans le code : « voici EXACTEMENT les trois cas possibles — refuse tout le reste, et aide-moi à traiter chacun proprement ». Tu transformes un panneau affiché au mur de la boutique en loi du programme.' },
            { t: 'h3', h: 'Les literal types : des valeurs promues au rang de types' },
            { t: 'code', lang: 'ts', label: 'litteraux.ts', code:
'let ville: \"Cotonou\" = \"Cotonou\";     // seule la valeur \"Cotonou\" est acceptée\nville = \"Calavi\";                      // ✗\n\n// La hiérarchie à retenir :\n//   \"momo\"            — UNE valeur exacte (literal type)\n//   \"momo\" | \"moov\"   — une union de littéraux\n//   string            — tous les textes possibles\n// Chaque niveau ÉLARGIT le précédent : \"momo\" est un string,\n// mais un string quelconque n\'est pas forcément \"momo\".\n\nconst moyen = \"momo\";   // const -> type inféré : \"momo\" (le littéral, figé)\nlet m = \"momo\";        // let   -> type inféré : string (élargi, voir Inférence)' },
            { t: 'p', h: 'Un literal type, c\'est une VALEUR (`\"momo\"`, `200`, `true`) utilisée comme type : il n\'accepte strictement que cette valeur. Isolé, ça semble inutile — qui veut une variable incapable de valoir autre chose que \"Cotonou\" ? — mais ces littéraux sont les BRIQUES de tout ce qui suit : unions de valeurs, discriminants, exhaustivité. Deux rappels vitaux depuis la fiche Inférence : `const` conserve le littéral (la valeur ne changera jamais, elle EST le type), `let` l\'élargit à `string` (la variable est faite pour changer). Et la relation entre les niveaux est de l\'inclusion d\'ensembles : le littéral est contenu dans l\'union, elle-même contenue dans `string`.' },
            { t: 'h3', h: 'L\'union `|` : « l\'un OU l\'autre, et rien d\'autre »' },
            { t: 'code', lang: 'ts', label: 'union.ts', code:
'type MoyenPaiement = \"momo\" | \"moov\" | \"especes\";\n\nfunction encaisser(montant: number, moyen: MoyenPaiement): string {\n  return \"Encaissement de \" + montant + \" F via \" + moyen;\n}\n\nencaisser(1500, \"momo\");    // ✓\nencaisser(1500, \"moov\");    // ✓\nencaisser(1500, \"mono\");    // ✗ faute de frappe refusée À L\'ÉCRITURE\nencaisser(1500, \"cheque\");  // ✗ moyen non prévu : oubli impossible' },
            { t: 'p', h: 'La déclaration `type MoyenPaiement = ...` crée un alias réutilisable : la liste FERMÉE des cas, écrite une seule fois dans le projet. Dès lors, chaque fonction qui encaisse exige cette union, et toute valeur extérieure à la liste est refusée — y compris demain, quand un collègue écrira `"cheque"` dans un coin du code. Note le rapport de force habituel renversé : ce n\'est plus toi qui relis ton code, c\'est le compilateur qui relit TOUT ce qui touche à `MoyenPaiement`. La fiche suivante (`ts-discriminated-unions`) exploitera cette liste fermée pour vérifier aussi les OUBLIS de traitement.' },
            { t: 'h3', h: 'La règle d\'or : sans réduction, seulement le commun' },
            { t: 'code', lang: 'ts', label: 'commun.ts', code:
'function afficherId(id: string | number) {\n  id.toUpperCase();          // ✗ refusé : number n\'a pas toUpperCase\n  id.valueOf();              // ✓ existe sur les deux : autorisé\n\n  if (typeof id === \"string\") {\n    id.toUpperCase();        // ✓ ICI, id EST une string : l\'union est réduite\n  }\n}' },
            { t: 'p', h: 'Voici le point que la plupart des débutants ratent : une union n\'ADDITIONNE pas les possibilités, elle les CROISE. Tant que tu n\'as pas prouvé de quel membre il s\'agit, TS ne t\'autorise que les opérations présentes chez TOUS les membres — toute méthode qui manque à l\'un d\'eux est interdite d\'office. Ce n\'est pas de la bureaucratie : c\'est exactement la garantie « aucun appel de méthode absente au runtime » que tu attends du langage. Pour débloquer les capacités spécifiques, tu RÉDUIS l\'union avec un test (`typeof`, comparaison, `switch`…) : la fiche `ts-narrowing` détaillera tout l\'outillage. Ici, retiens le principe : vérifier d\'abord, utiliser ensuite — comme compter les billets avant de ranger le tiroir-caisse.' },
            { t: 'h3', h: 'L\'intersection `&` : toutes les formes à la fois' },
            { t: 'code', lang: 'ts', label: 'intersection.ts', code:
'interface Paiement { montant: number; date: Date; }\ninterface Tracable { reference: string; }\n\n// « à la fois » Paiement ET Tracable :\ntype PaiementTrace = Paiement & Tracable;\n\nconst p: PaiementTrace = {\n  montant: 21000,\n  date: new Date(),\n  reference: \"MOMO-8F3K2\"    // tout est exigé, des deux côtés\n};\n\n// Croiser des primitifs, en revanche…\ntype Jamais = string & number;   // = never : aucune valeur n\'est les deux' },
            { t: 'p', h: '`&` fusionne des EXIGENCES : pour être un `PaiementTrace`, un objet doit satisfaire `Paiement` ET `Tracable` simultanément. C\'est le cousin de `interface PaiementTrace extends Paiement, Tracable {}` — le choix de style est débattu dans la fiche interface vs type. Le piège classique tient en une ligne : croiser des primitifs. `string & number` exige une valeur qui serait en même temps texte et nombre — impossible : le type s\'effondre en `never`, le type vide. Si un jour une variable refuse TOUT sans raison apparente, inspecte-la : une intersection trop ambitieuse s\'est probablement évaporée en `never`.' },
            { t: 'h3', h: '`|` ou `&` : le tableau qui tranche' },
            { t: 'table', head: ['Question', '`|` (union)', '`&` (intersection)'], rows: [
              ['Résout', '« l\'un des deux »', '« les deux ensemble »'],
              ['Valeur acceptée', 'un membre quelconque', 'ce qui satisfait TOUT'],
              ['Accessible sans test', 'le COMMUN des membres', 'l\'ENSEMBLE des membres'],
              ['Exemple utile', '"momo" | "moov"', 'Paiement & Tracable'],
              ['Piège typique', 'utiliser sans réduire', 'croiser des primitifs → never']
            ]},
            { t: 'h3', h: 'Sous le capot : des ensembles, pas de la magie' },
            { t: 'p', h: 'Le vérificateur NORMALISE les unions : les doublons disparaissent (`"a" | "a"` vaut `"a"`), l\'ordre ne compte pas, et un membre impossible s\'évapore — `string | never` vaut simplement `string`, parce que `never` est le type VIDE : ajouter rien à un ensemble le laisse identique. Cette règle anodine alimente des mécaniques avancées que tu croiseras plus tard : les mapped types filtrent des unions en injectant du `never` pour rayer des cas, et l\'exhaustivité des unions discriminées repose sur le même calcul. Pas besoin de maîtriser la machinerie aujourd\'hui ; sache seulement que les types TS forment des ENSEMBLES de valeurs — union, intersection, inclusion, vide — et que toute la « magie » du compilateur est de la théorie des ensembles qui travaille pour toi.' },
            { t: 'h3', h: 'Exemple : la grille tarifaire du zémidjan' },
            { t: 'code', lang: 'ts', label: 'tarifs-zem.ts', code:
'type Zone = \"centre\" | \"calavi\" | \"godomey\";\ntype Periode = \"jour\" | \"nuit\";\n\nfunction tarif(zone: Zone, periode: Periode): number {\n  let base: number;\n  switch (zone) {\n    case \"centre\":  base = 300; break;\n    case \"calavi\":  base = 700; break;\n    case \"godomey\": base = 400; break;\n  }\n  return periode === \"nuit\" ? base + 100 : base;\n}\n\ntarif(\"calavi\", \"nuit\");    // 800\ntarif(\"akpapka\", \"jour\");   // ✗ faute de frappe : refusée AVANT toute course' },
            { t: 'p', h: 'Compte ce que la modélisation t\'a acheté, gratuitement : la liste des zones est FERMÉE et partagée — un quartier nouveau se déclare dans `Zone`, et chaque `switch` incomplet deviendra un endroit à compléter. La faute de frappe est bloquée avant la course, pas pendant. Les périodes ne peuvent plus valoir que jour ou nuit, fini les `"JOUR"` / `"jour "` selon l\'humeur du saisisseur. Et au runtime ? De simples strings ordinaires — toute la rigueur s\'est exercée AVANT l\'exécution, sans coûter un octet.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« `|` ressemble à `||`, c\'est pareil »** — non : `||` choisit entre deux VALEURS pendant l\'exécution ; `|` déclare une alternative de TYPES avant l\'exécution. Les symboles se ressemblent, les mondes sont étrangers.',
              '**« une union me donne toutes les méthodes de tous ses membres »** — c\'est l\'inverse : elle n\'accorde que le COMMUN. Le reste se débloque en réduisant (fiche Narrowing).',
              '**« `&` est un et logique »** — pas au sens booléen : c\'est une fusion d\'exigences de FORMES. Sur des primitifs incompatibles, il produit `never`, le vide — pas une valeur magique à double casquette.',
              '**« les unions, c\'est pour les cas savants »** — pas du tout : trois strings en union (`"petit" | "moyen" | "grand"`) remplacent avantageusement un booléen ambigu ou un commentaire « valeurs possibles : … » non vérifiable.',
              '**« on peut faire `new` d\'une union »** — jamais : unions et intersections ne désignent aucune classe ni fabrique. Elles décrivent des ensembles de valeurs ; la construction reste ton affaire (littéral, factory, classe).'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Tu as croisé ces idées sans les nommer : les unions de littéraux ont été évoquées dans `ts-types-scalaires` et la fiche `ts-inference` a expliqué pourquoi `const` garde le littéral quand `let` élargit. Le type `never`, rencontré ici au détour des intersections impossibles, a sa carte d\'identité complète dans `ts-types-speciaux`. Et tu avais déjà pratiqué le raisonnement par cas avec les conditions de `js-conditions` — sauf qu\'ici, le compilateur t\'assure que les cas sont les bons. Les deux fiches suivantes s\'appuient directement dessus : `ts-discriminated-unions` structure les alternatives en objets étiquetés, `ts-narrowing` fournit tout l\'outillage de réduction.' },
            { t: 'callout', kind: 'tip', h: 'Réflexe de modélisation : dès que tu t\'apprêtes à écrire `string` pour un champ, demande-toi si la liste des valeurs est en réalité FERMÉE (statuts, moyens de paiement, zones, tailles). Si oui, une union de littéraux la verrouille — et le compilateur devient le gardien officiel de ta liste, sans test à écrire.' }
          ],
          errors: [
            { title: 'Confondre `|` (type) et `||` (exécution)', lang: 'ts', bad:
'function encaisser(m: string || number) {\n  // ✗ syntaxe invalide : \"||\" n\'a aucun sens dans une annotation', good:
'function encaisser(m: string | number) {\n  if (typeof m === \"string\") return m.length;\n  return m.toFixed(0);\n}', why: '`||` est un opérateur d\'EXÉCUTION qui choisit entre deux valeurs selon leur vérité ; `|` est un connecteur de TYPES qui déclare une alternative avant l\'exécution. Écrire l\'un à la place de l\'autre n\'est pas une erreur de logique : c\'est un hors-sujet de couche — ils vivent dans deux étages différents du programme.' },
            { title: 'Croiser des primitifs avec `&`', lang: 'ts', bad:
'type QuantiteTexte = string & number;   // = never : ensemble vide\nconst q: QuantiteTexte = 3;              // ✗ never n\'accepte RIEN, jamais', good:
'type Reserve = { enStock: boolean } & { quantite: number };\nconst r: Reserve = { enStock: true, quantite: 12 };   // ✓ formes croisées', why: 'L\'intersection exige une valeur qui satisfasse TOUTES les formes à la fois. Aucune valeur n\'est simultanément string et number : le type s\'effondre en `never`. `&` se réserve aux formes d\'objets compatibles — pour dire « texte OU nombre », le connecteur est `|`.' }
          ],
          related: ['ts-discriminated-unions', 'ts-narrowing', 'ts-types-speciaux', 'ts-interface-vs-type']
        },
        {
          id: 'ts-discriminated-unions',
          title: 'Unions discriminées',
          icon: 'alt_route',
          level: 'Avancé',
          tagline: 'Un champ étiquette, un switch qui réduit, un compilateur qui vérifie que rien n\'est oublié.',
          intro: 'Certaines données CHANGENT DE FORME selon leur état : une cotisation de tontine en attente n\'a ni date ni référence ; une fois payée par MoMo, elle en a forcément une ; en retard, ce sont les jours et les relances qui comptent. Le réflexe naïf — un seul objet avec tous les champs optionnels — autorise des états impossibles : payée sans date, en attente mais déjà référencée. L\'**union discriminée** résout ça avec une élégance rare : plusieurs formes unies par un champ commun à valeur littérale distincte — le **discriminant** — sur lequel un `switch` réduit au cas par cas, avec exhaustivité VÉRIFIÉE par le compilateur. C\'est l\'un des patterns les plus puissants de tout le langage.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : interdire les états impossibles' },
            { t: 'p', h: 'Ouvre le cahier de la tontine d\'Awa : une cotisation traverse trois états — en attente, payée, en retard. Et chaque état a SON bagage : rien de plus pour l\'attente ; la date et la référence MoMo pour le paiement ; les jours de retard et le nombre de relances sinon. Maintenant, modélise ça avec un objet unique à champs optionnels (`datePaiement?`, `reference?`, `joursRetard?`) : tu viens de créer une machine à états impossibles. « Payée » sans date, « en retard » avec une référence, « en attente » déjà datée — tout ça COMPILE. La devise à graver sur ton bureau, c\'est : **rendre les états impossibles irreprésentables** (« make impossible states unrepresentable »). Si un état ne peut pas exister dans la réalité, ton type ne doit pas savoir l\'écrire. L\'union discriminée est l\'outil qui fait ça, sans une ligne de code supplémentaire au runtime.' },
            { t: 'code', lang: 'ts', label: 'le-piege.ts', code:
'// Le modèle « objet à trous » : tout est optionnel, donc tout est permis\ninterface CotisationPermissive {\n  statut: string;         // n\'importe quel texte…\n  datePaiement?: Date;\n  reference?: string;\n  joursRetard?: number;\n}\n\n// Cet objet ABSURDE compile : payée sans date, avec des jours de retard.\nconst bug: CotisationPermissive = { statut: \"payee\", joursRetard: 14 };' },
            { t: 'h3', h: 'Le pattern en trois ingrédients' },
            { t: 'ol', items: [
              'Un champ COMMUN à toutes les variantes — souvent nommé `type`, `kind` ou `statut`.',
              'Chaque variante le fixe à un LITERAL TYPE distinct : c\'est le **discriminant**.',
              'Les autres champs varient librement : chaque variante porte son propre bagage, exigible à la création.'
            ]},
            { t: 'code', lang: 'ts', label: 'cotisation.ts', code:
'interface CotisationEnAttente {\n  statut: \"en_attente\";                    // le discriminant, en littéral\n}\ninterface CotisationPayee {\n  statut: \"payee\";\n  datePaiement: Date;                       // obligatoire : payée SANS date = impossible\n  reference: string;                        // ex : référence de transaction MoMo\n}\ninterface CotisationEnRetard {\n  statut: \"en_retard\";\n  joursRetard: number;\n  relances: number;\n}\n\ntype Cotisation = CotisationEnAttente | CotisationPayee | CotisationEnRetard;\n\nconst c1: Cotisation = { statut: \"en_attente\" };                              // ✓\nconst c2: Cotisation = { statut: \"payee\", datePaiement: new Date(),\n                         reference: \"MOMO-8F3K2\" };                          // ✓\nconst c3: Cotisation = { statut: \"payee\" };                                   // ✗ date ET référence exigées' },
            { t: 'h3', h: 'Le switch qui réduit, champ par champ' },
            { t: 'code', lang: 'ts', label: 'libelle.ts', code:
'function libelle(c: Cotisation): string {\n  switch (c.statut) {\n    case \"en_attente\":\n      return \"En attente de paiement\";\n    case \"payee\":\n      // ICI, c EST une CotisationPayee : date et référence disponibles\n      return \"Payée le \" + c.datePaiement.toLocaleDateString(\"fr-FR\") +\n             \" (réf. \" + c.reference + \")\";\n    case \"en_retard\":\n      // ICI, c EST une CotisationEnRetard\n      return \"Retard de \" + c.joursRetard + \" j (\" + c.relances + \" relances)\";\n  }\n}' },
            { t: 'p', h: 'Observe la précision mécanique : dans chaque `case`, le compilateur sait EXACTEMENT de quelle variante il s\'agit, parce que le discriminant a été comparé à un littéral distinct. `c.reference` est accessible dans la branche \"payee\" et REFUSÉ partout ailleurs — tu ne peux plus lire une référence sur une cotisation qui n\'en a pas. C\'est la règle « seul le commun sans test » de la fiche Unions, transformée en outil de précision : le discriminant EST le test qui réduit. Et tout fonctionne pareil avec `if (c.statut === \"payee\")` ou un ternaire imbriqué : c\'est la comparaison au littéral qui fait le travail, pas le mot-clé `switch`.' },
            { t: 'h3', h: 'L\'exhaustivité : `assertNever`, le filet de sécurité' },
            { t: 'code', lang: 'ts', label: 'assert-never.ts', code:
'function assertNever(x: never): never {\n  throw new Error(\"Cas non traité : \" + JSON.stringify(x));\n}\n\nfunction smsRelance(c: Cotisation): string {\n  switch (c.statut) {\n    case \"en_attente\": return \"Rappel : cotisation du mois en attente.\";\n    case \"payee\":      return \"Merci, cotisation bien reçue !\";\n    case \"en_retard\":  return \"Retard de \" + c.joursRetard + \" j : régularise vite.\";\n    default:           return assertNever(c);   // ← le filet\n  }\n}\n\n// Demain, on ajoute CotisationAnnulee à l\'union :\n// assertNever(c) devient une ERREUR DE COMPILATION, ici et dans CHAQUE\n// switch du projet. La liste des endroits à compléter s\'écrit toute seule.' },
            { t: 'p', h: 'Voilà le dividende qui justifie toute la construction : l\'EXHAUSTIVITÉ VÉRIFIÉE. Tant que tous les cas sont traités, le `default` ne peut recevoir que `never` — le cas « il ne reste plus rien » — et l\'appel compile. Ajoute une quatrième variante demain : le `default` reçoit alors un vrai type, `assertNever` refuse de compiler, et chaque `switch` du projet se signale lui-même comme « à compléter ». Tu remplaces « espérer avoir pensé à tout » par « suivre les erreurs du compilateur » — un refactor assisté, gratuit, sans écrire le moindre test. En pratique, les équipes qui découvrent ce filet ne s\'en séparent plus jamais.' },
            { t: 'h3', h: 'Objet permissif contre union discriminée' },
            { t: 'table', head: ['Critère', 'Objet à champs optionnels', 'Union discriminée'], rows: [
              ['États impossibles représentables', 'oui (payée sans date…)', 'non, par construction'],
              ['Champs requis selon l\'état', 'jamais garantis', 'exigés à la création'],
              ['Ajout d\'un état demain', 'silencieux, bugs latents', 'erreurs de compile ciblées'],
              ['Lecture hors contexte', '« ce champ sert à quoi ? »', 'le statut porte le sens'],
              ['Coût au runtime', 'zéro', 'zéro (de simples objets)']
            ]},
            { t: 'h3', h: 'Sous le capot : l\'analyse de flux sur les littéraux' },
            { t: 'p', h: 'Comment TS « sait-il » quelle branche est laquelle ? Le vérificateur effectue une ANALYSE DE FLUX DE CONTRÔLE (control flow analysis) : il suit les comparaisons dans ton code et affine le type des variables au fil des branches. Comparer un champ à un littéral (`c.statut === \"payee\"`) EXCLUT toutes les variantes dont le discriminant vaut autre chose — et comme chaque variante a une étiquette distincte, il n\'en reste qu\'une. Deux détails à connaître : le mécanisme exige des LITTÉRAUX — déclare le champ `string` et la magie s\'éteint, puisque `string` n\'exclut rien (première carte d\'erreur plus bas) ; et le discriminant peut combiner PLUSIEURS champs, réduits l\'un après l\'autre dans des branches imbriquées. C\'est de la logique d\'ensembles appliquée à l\'exécution de TON code, ligne par ligne.' },
            { t: 'h3', h: 'Exemple : le suivi de livraison Dantokpa → Abomey-Calavi' },
            { t: 'code', lang: 'ts', label: 'livraison.ts', code:
'type Livraison =\n  | { etape: \"preparee\"; colis: string }\n  | { etape: \"en_route\"; colis: string; zemId: string; depart: Date }\n  | { etape: \"livree\"; colis: string; receptionnePar: string; arrivee: Date };\n\nfunction notifie(l: Livraison): string {\n  switch (l.etape) {\n    case \"preparee\":\n      return \"Colis \" + l.colis + \" en préparation à la boutique.\";\n    case \"en_route\":\n      return \"Le zémidjan \" + l.zemId + \" vient de partir.\";\n    case \"livree\":\n      return \"Livré à \" + l.receptionnePar + \" — merci !\";\n  }\n}\n\n// « en_route » SANS identifiant de zem est impossible à écrire :\nconst l: Livraison = { etape: \"en_route\", colis: \"CMD-1042\" };   // ✗ zemId exigé' },
            { t: 'p', h: 'Deux remarques de style. D\'abord, l\'écriture directe en union de littéraux d\'objets, sans interfaces nommées : parfaitement légitime quand les variantes ne sont réutilisées nulle part ailleurs — nomme-les dès qu\'un deuxième fichier en a besoin. Ensuite, le champ `colis`, présent dans les trois variantes : il fait partie du « commun », accessible sans aucun test — ton modèle reflète la réalité à trois vitesses, exactement comme le suivi réel d\'un colis. Et chaque notification ne peut puiser que dans les données réellement disponibles à cet instant du parcours : impossible d\'annoncer le nom du réceptionnaire alors que le zem n\'a même pas démarré.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« c\'est juste une union comme une autre »** — la DISCIPLINE fait tout : sans champ littéral commun et distinct, le `switch` ne peut rien exclure, et le pattern retombe en union ordinaire où chaque branche reste floue.',
              '**« un `default: break;` suffit »** — il avale silencieusement les FUTURS cas : tout l\'intérêt est dans `default: return assertNever(c)`, qui convertit l\'oubli en erreur de compilation.',
              '**« enum ou union de littéraux ? »** — pour un simple feu tricolore (trois états sans données), l\'enum convient ; dès que chaque état PORTE des données différentes, l\'union discriminée gagne : elle colle à la forme réelle sans champs fantômes, et se sérialise en JSON naturellement.',
              '**« le discriminant doit être une string »** — non : des littéraux numériques (`etape: 1 | 2 | 3`) ou un booléen fonctionnent aussi ; la string reste la plus lisible dans les erreurs et le JSON, d\'où la convention.',
              '**« il faudrait plutôt une classe par état »** — les hiérarchies de classes jouent ce rôle (fiche Classes avancées), mais l\'union discriminée est plus légère, sérialisable telle quelle, et son exhaustivité ne demande aucun pattern visiteur.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Tout s\'assemble : les literal types et les unions viennent de `ts-unions`, avec la règle « seul le commun est accessible sans test » ; l\'analyse de flux sera approfondie dans `ts-narrowing`, puis étendue à tes propres tests maison dans `ts-type-guards` ; et le débat enum contre littéraux renvoie à `ts-tuples-enums`. Côté JavaScript, tu reconnais le `switch` de `js-conditions` — sauf qu\'ici, une machine vérifie que ton switch est COMPLET, aujourd\'hui et après chaque évolution du modèle.' },
            { t: 'callout', kind: 'tip', h: 'Réflexe de revue de code : dès qu\'un objet possède un champ `type` / `statut` ET plusieurs champs optionnels « selon les cas », une union discriminée se cache en dessous. Extrais les variantes une à une, exige leurs champs propres, ajoute `assertNever` — le compilateur te remerciera à la toute première évolution du produit.' }
          ],
          errors: [
            { title: 'Un discriminant déclaré trop large', lang: 'ts', bad:
'interface CotisationPayee {\n  statut: string;        // ✗ trop large : \"payee\" ? \"payé\" ? n\'importe quoi\n  reference: string;\n}\n// le switch ne réduit plus rien : reference reste inaccessible en sécurité', good:
'interface CotisationPayee {\n  statut: \"payee\";      // ✓ littéral distinct : la comparaison EXCLUT les autres\n  reference: string;\n}', why: 'Toute la mécanique repose sur des littéraux DISTINCTS : comparer à \"payee\" exclut les variantes dont le statut n\'est pas ce littéral. Déclaré `string`, le champ n\'exclut plus rien, le `switch` redevient décoratif — et tu perds à la fois la réduction et l\'exhaustivité. Le littéral EST le verrou ; ne le remplace jamais par sa catégorie.' },
            { title: 'Un `default` silencieux', lang: 'ts', bad:
'switch (c.statut) {\n  case \"en_attente\": return \"Rappel…\";\n  default: return \"\";      // ✗ le futur statut \"annulee\" tombera ici en silence\n}', good:
'function assertNever(x: never): never {\n  throw new Error(\"Cas non traité : \" + JSON.stringify(x));\n}\n// default: return assertNever(c);   // ✓ tout oubli devient erreur de compile', why: 'Un default qui retourne une valeur bidon transforme chaque ajout de variante en bombe à retardement : rien ne signale les switch à compléter, et le nouveau cas récolte une réponse générique en production. assertNever inverse la physique : l\'oubli devient une erreur localisée, à la compilation, liste à l\'appui.' }
          ],
          related: ['ts-unions', 'ts-narrowing', 'ts-type-guards', 'ts-tuples-enums']
        }
      ]
    },
  {
      id: 'generics',
      name: 'Generics',
      icon: 'dynamic_form',
      fiches: [
        {
          id: 'ts-generics',
          title: 'Fonctions & types génériques',
          icon: 'inventory',
          level: 'Intermédiaire',
          tagline: 'Écrire la logique une fois, laisser l\'appelant choisir le type — sans jamais recourir à any.',
          intro: 'Une fonction comme « renvoyer le premier élément d\'un tableau » devrait marcher sur un tableau de prix, de noms de clients ou de produits — la LOGIQUE est identique, seul le type change. Trois options : dupliquer la fonction pour chaque type (maintenance en triple), écrire `any` (le compilateur démissionne), ou déclarer un **paramètre de type** : `<T>`. Le générique, c\'est une variable qui reçoit un TYPE au lieu d\'une valeur — l\'appelant le choisit, souvent sans même s\'en rendre compte grâce à l\'inférence. C\'est l\'outil qui fait passer TypeScript de « décorateur de signatures » à « langage de bibliothèques » : les tableaux, les Promises, `Map`, tout l\'écosystème en est bâti.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : la même logique, des types différents' },
            { t: 'p', h: 'À la Boutique Awa, tu récupères souvent « le premier » de quelque chose : le premier prix d\'une liste, la première commande du jour, le premier produit du rayon. En JavaScript, une seule fonction suffit — mais en la typant tu te cognes au mur : `premier(tab)` sur `number[]` doit renvoyer `number`, sur `string[]` doit renvoyer `string`… Écris `premier(tab: any): any` et hop, tout compile — y compris `premier([1,2,3]).toUpperCase()`, promesse d\'un crash. Duplique pour chaque type et chaque évolution se fera en triple exemplaire, avec les divergences qui vont avec. Il manque un cran d\'abstraction : une façon de dire « le type d\'ENTRÉE et le type de SORTIE sont LIÉS, peu importe leur valeur concrète ». Ce cran-là s\'appelle le générique.' },
            { t: 'h3', h: 'La syntaxe : un paramètre de type entre chevrons' },
            { t: 'code', lang: 'ts', label: 'premier.ts', code:
'function premier<T>(tab: T[]): T | undefined {\n  return tab[0];   // retourne undefined sur tableau vide : honnête\n}\n\n// À chaque appel, l\'appelant (in)consciemment CHOISIT T :\npremier([500, 1200, 800]);      // T = number  -> number | undefined\npremier([\"gari\", \"huile\"]);     // T = string  -> string | undefined\n\ninterface Produit { nom: string; prix: number; }\nconst catalogue: Produit[] = [{ nom: \"Gari premium\", prix: 21000 }];\npremier(catalogue);             // T = Produit -> Produit | undefined' },
            { t: 'p', h: 'Lecture mécanique, en quatre temps. 1) `<T>` placé avant les parenthèses DÉCLARE un paramètre de type — une variable muette qui recevra un type au moment de l\'appel. 2) `T` est ensuite utilisé comme n\'importe quel type : ici « tableau de T en entrée, T ou rien en sortie ». 3) À l\'appel, le compilateur INFÈRE T depuis les arguments : tableau de nombres → `T = number`. 4) Le lien est établi : la sortie suit l\'entrée, typée au plus juste, et `premier([1,2,3]).toUpperCase()` est refusé — le compilateur sait que tu tiens un `number`. Le nom `T` est une convention (Type) ; sur deux paramètres tu verras `T, U` ou des noms parlants comme `TItem`, `TKey`.' },
            { t: 'h3', h: 'L\'inférence d\'abord, l\'explicite quand il faut' },
            { t: 'code', lang: 'ts', label: 'inference-generique.ts', code:
'// La plupart du temps, ne RIEN écrire : T se déduit des arguments.\npremier([\"a\", \"b\"]);\n\n// Mais parfois l\'inférence manque de matière…\npremier([]);            // T = ? inferé never — inutilisable\npremier<string>([]);    // ✓ explicite : T = string\n\n// … ou tu veux resserrer davantage que l\'inférence :\nconst tete = premier<Produit>(catalogue);' },
            { t: 'p', h: 'La règle tient en une phrase : **écris le générique explicitement seulement quand l\'inférence échoue ou ment**. Conteneur vide, chaîne d\'appels trop complexe, volonté de verrouiller plus finement que ce que le compilateur proposerait — dans ces cas-là, les chevrons à l\'appel (`premier<string>(…)`) tranchent proprement. Partout ailleurs, laisse faire : ta signature déclare la relation, l\'appelant n\'a rien à savoir, et le code reste aussi fluide qu\'en JavaScript — avec le filet en plus. C\'est exactement la philosophie « frontières typées, intérieur inféré » de la fiche Inférence, un étage plus haut.' },
            { t: 'h3', h: 'Les types aussi deviennent génériques' },
            { t: 'code', lang: 'ts', label: 'paginated.ts', code:
'interface Paginated<T> {\n  items: T[];\n  page: number;\n  total: number;\n}\n\ninterface ApiResponse<T> {\n  ok: boolean;\n  data: T;\n  erreur?: string;\n}\n\n// Le même contenant, rempli de marchandises différentes :\ntype PageCatalogue = Paginated<Produit>;\ntype PageCommandes = Paginated<Commande>;\ntype ReponseCatalogue = ApiResponse<Produit[]>;\n\nfunction rendrePage(p: Paginated<Produit>): string {\n  return p.items.map(i => i.nom).join(\", \") +\n         \" (page \" + p.page + \"/\" + p.total + \")\";\n}' },
            { t: 'p', h: 'Le générique ne se limite pas aux fonctions : interfaces et alias acceptent les mêmes chevrons. `Paginated<T>` décrit « une page de QUELQUE CHOSE » — la structure est fixe, le contenu paramétrable. Quand tu instancies (`Paginated<Produit>`), chaque occurrence de `T` est remplacée par `Produit`, et le compilateur vérifie le tout comme si tu avais écrit l\'interface à la main. C\'est exactement ce que tu lis déjà, sans peut-être t\'en être rendu compte, dans `Promise<Produit>`, `Array<string>` ou `Map<string, number>` : la bibliothèque standard EST générique, et maintenant tu sais écrire les tiennes.' },
            { t: 'h3', h: 'Exemple : la fonction de recherche de la Boutique Awa' },
            { t: 'code', lang: 'ts', label: 'recherche.ts', code:
'async function rechercher<T>(endpoint: string): Promise<ApiResponse<T>> {\n  const res = await fetch(endpoint);\n  const brut: unknown = await res.json();\n  // + les gardes de la fiche Type guards pour valider brut…\n  return brut as ApiResponse<T>;\n}\n\nconst page = await rechercher<Paginated<Produit>>(\"/api/produits?page=1\");\n// page.data.items : Produit[] — autocomplété, vérifié, zéro duplication' },
            { t: 'p', h: 'Voici la forme canonique de « l\'API wrapper » : UNE fonction de transport, et le type de la réponse décidé À L\'APPEL, là où on sait ce qu\'on demande. `rechercher<Paginated<Produit>>` annonce « cet endpoint renvoie une page de produits » ; une autre ligne pourra dire `rechercher<MembreTontine[]>(\"/api/membres\")` sans toucher à la fonction. Honnêteté oblige : l\'annotation décrit ce que la boutique EST CENSÉE renvoyer — une promesse, pas une preuve. La preuve, c\'est la combinaison `unknown` + gardes (fiches `ts-types-speciaux` puis `ts-type-guards`) ; ici, retiens la mécanique du générique.' },
            { t: 'h3', h: 'Sans générique, quelles options ?' },
            { t: 'table', head: ['Stratégie', 'Ce qui arrive', 'Verdict'], rows: [
              ['Dupliquer par type', '3 fonctions, 3 maintenances, divergences garanties', '✗'],
              ['any partout', 'tout compile, plus aucune vérification en aval', '✗ le pire'],
              ['Union de types précis', 'marche, mais se referme : tout nouveau type exige une retouche', 'à réserver aux unions nommées et fermées'],
              ['Générique `<T>`', 'une implémentation, un lien entrée/sortie, inférence à l\'appel', '✓ l\'outil fait pour']
            ]},
            { t: 'h3', h: 'Sous le capot : effacement, pas spécialisation' },
            { t: 'p', h: 'Contrairement au C++ qui « monomorphise » (génère une copie machine de la fonction par type), TypeScript ÉFFACE tout le générique à l\'émission : il n\'existe au runtime qu\'UNE fonction JavaScript ordinaire, sans la moindre trace de `T`. Deux conséquences pratiques à retenir. D\'abord, zéro surcoût : dix appels avec dix types différents tournent sur la même fonction. Ensuite — et c\'est le point qui surprend — `T` N\'EXISTE PAS à l\'exécution : impossible d\'écrire `new T()`, impossible de tester `if (x instanceof T)`. Si ta logique a besoin de CONNAÎTRE le type au runtime (créer des instances, brancher selon le type), le générique seul ne suffit pas : il faudra passer une fabrique ou un discriminant en argument. Le générique est un outil de vérification STATIQUE — puissant, gratuit, mais confiné à la compilation.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« le générique choisit le type tout seul »** — non : c\'est l\'APPELANT qui le choisit, via ses arguments (inférence) ou les chevrons explicites. La fonction, elle, doit fonctionner pour TOUT choix de T — c\'est la vraie signification de « générique ».',
              '**« T en entrée, T en sortie, c\'est décoratif »** — c\'est le LIEN qui compte : T apparaît aux deux bouts pour nouer la relation. Un T isolé de chaque côté ne prouverait rien.',
              '**« générique = any amélioré »** — tout le contraire : `any` efface l\'information, le générique la TRANSPORTE. Avec `any`, la sortie est imprévisible ; avec `<T>`, le compilateur sait exactement ce qui sort.',
              '**« je peux utiliser des méthodes sur T »** — `T` libre ne garantit RIEN : ni length, ni nom, ni toFixed. Pour exiger une capacité, il faut CONTRAINDRE T — sujet de la fiche suivante.',
              '**« les génériques, c\'est pour les auteurs de librairies »** — eux d\'abord, et toi ensuite : la moindre utilité « contenant » ou « transport » de ton projet (page, cache, réponse API, paire clé-valeur) en gagne à être générique dès le deuxième usage.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Tu réutilises ici les tableaux typés de `ts-types-scalaires` (`T[]`, `Array<T>` — tu vois maintenant d\'où vient cette syntaxe chevronnée !), les promesses de `js-asynchrone` (`Promise<T>`, enfin expliquée), et l\'honnêteté de `ts-types-speciaux` (le `unknown` avant validation). La fiche cousine est `ts-generics-contraintes` : `<T extends …>` pour exiger des capacités, `keyof` pour naviguer dans les clés, les défauts `T = X` pour la douceur d\'usage — et plus tard, `ts-utility-types` te montrera tout un arsenal PRÉFABRIQUÉ sur ces fondations.' },
            { t: 'callout', kind: 'tip', h: 'Test de nécessité : si ta fonction marche mot pour mot avec trois types différents et qu\'aucune méthode spécifique n\'est appelée à l\'intérieur, elle est générique de nature. Écris `<T>`, relie entrée et sortie — et laisse l\'inférence cacher les chevrons à tous tes appelants.' }
          ],
          errors: [
            { title: 'Contourner avec any ce que le générique résout', lang: 'ts', bad:
'function premier(tab: any): any {\n  return tab[0];\n}\npremier([1, 2, 3]).toUpperCase();   // ✓ compile… CRASH au runtime', good:
'function premier<T>(tab: T[]): T | undefined {\n  return tab[0];\n}\npremier([1, 2, 3]).toUpperCase();   // ✗ number n\'a pas toUpperCase' , why: '`any` ne résout rien : il DÉMISSIONNE — la vérification s\'arrête à la porte de la fonction et le bug voyage jusqu\'au runtime. Le générique transporte l\'information de type à travers la fonction : l\'entrée et la sortie restent liées, contrôlées, autocomplétées.' },
            { title: 'Un T qui sort de nulle part', lang: 'ts', bad:
'function creer<T>(): T {\n  return {} as T;   // quoi que l\'appelant demande… on sert un objet vide\n}\nconst p = creer<Produit>();\np.prix.toFixed(0);   // ✗ crash : prix n\'existe pas dans {}', good:
'function envelopper<T>(valeur: T): { contenu: T } {\n  return { contenu: valeur };   // T vient d\'un PARAMÈTRE : il est réel\n}', why: 'Un paramètre de type qui n\'apparaît NI dans les arguments NI dans un contexte qui le fixe peut être choisi LIBREMENT par l\'appelant — ta fonction ne peut honorer cette promesse qu\'en mentant (as, cast). Règle d\'hygiène : chaque T doit être « ancré » à une donnée réelle qui entre.' }
          ],
          related: ['ts-generics-contraintes', 'ts-utility-types', 'ts-types-speciaux', 'js-asynchrone']
        },
        {
          id: 'ts-generics-contraintes',
          title: 'Contraintes extends, interfaces génériques & défauts',
          icon: 'rule',
          level: 'Avancé',
          tagline: 'Un T libre ne garantit rien ; un T contraint exige le minimum utile — et garde toute sa précision.',
          intro: 'Le générique libre `<T>` transporte les types mais ne sait rien d\'eux : impossible d\'appeler `.length`, de lire une clé, de comparer — `T` pourrait être n\'importe quoi. C\'est parfait pour un contenant, insuffisant dès que la fonction a besoin d\'une CAPACITÉ minimale de son entrée. La **contrainte** `T extends Forme` résout l\'équation : « n\'importe quel type, POURVU QU\'il ait au moins cette forme ». Ajoute les interfaces génériques (`Repository<T>`), la navigation dans les clés (`K extends keyof T`) et les paramètres par défaut (`T = unknown`), et tu tiens la boîte à outils complète des API typées de qualité librairie.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : « n\'importe quoi » ne suffit plus' },
            { t: 'p', h: 'Écris la fonction « afficher la taille d\'un article » : elle marche aussi bien sur un nom de quartier (nombre de caractères) que sur une liste de prix (nombre d\'éléments) — les deux ont `.length`. La logique est générique, mais elle EXIGE quelque chose de son entrée. Avec `<T>` tout seul, `x.length` est refusé : le compilateur réplique « et si T était un number ? » — et il a raison, c\'est son travail. Il te faut exprimer un marché plus fin : « j\'accepte n\'importe quel type À CONDITION qu\'il possède au moins une propriété `length` numérique ». Ni aucune exigence, ni un type unique : une exigence MINIMALE. C\'est la définition exacte de `extends` appliqué à un paramètre de type.' },
            { t: 'h3', h: '`T extends …` : exiger une capacité minimale' },
            { t: 'code', lang: 'ts', label: 'contrainte.ts', code:
'function afficherTaille<T extends { length: number }>(x: T): number {\n  return x.length;   // ✓ garanti par la contrainte\n}\n\nafficherTaille(\"Akpakpa\");          // ✓ string a length\nafficherTaille([500, 1200, 800]);   // ✓ les tableaux aussi\nafficherTaille(42);                 // ✗ number n\'a pas length : refusé' },
            { t: 'p', h: 'Décortique la formule `T extends { length: number }`. D\'abord, ce n\'est PAS de l\'héritage au sens classes : ici, `extends` se lit « **a AU MOINS cette forme** » — n\'importe quel type dont la structure contient un `length` numérique est éligible. Ensuite, la magie à double détente : à l\'INTÉRIEUR de la fonction, `x` offre `length` (la contrainte est respectée) ; à l\'EXTÉRIEUR, T garde toute sa précision — `afficherTaille(\"Akpakpa\")` sait que T était `string`, pas « un truc avec length ». Tu n\'échanges pas de la précision contre de la permissivité : tu as les deux. C\'est toute la différence avec accepter `{ length: number }` directement en paramètre, qui, elle, JETTE l\'information.' },
            { t: 'h3', h: 'Le duo légendaire : `K extends keyof T`' },
            { t: 'code', lang: 'ts', label: 'get-prop.ts', code:
'function getProp<T, K extends keyof T>(objet: T, cle: K): T[K] {\n  return objet[cle];\n}\n\nconst produit = { id: \"GARI-50\", nom: \"Gari premium\", prix: 21000 };\n\ngetProp(produit, \"prix\");    // ✓ K = \"prix\", retour : number\ngetProp(produit, \"nom\");     // ✓ retour : string\ngetProp(produit, \"preix\");   // ✗ \"preix\" n\'est PAS une clé de produit' },
            { t: 'p', h: 'Retiens cette signature, tu la rencontreras partout : DEUX paramètres de type liés — `T` capture la forme de l\'objet, `keyof T` produit l\'union de ses clés (`\"id\" | \"nom\" | \"prix\"` ici), et `K extends keyof T` contraint le second argument à être une de ces clés. Le but : les fautes de frappe dans les noms de propriétés deviennent des erreurs de compilation (`\"preix\"` est refusé), et le TYPE DE RETOUR s\'ajuste tout seul selon la clé demandée : `T[K]` vaut `number` pour \"prix\", `string` pour "nom". La fiche `ts-keyof-typeof` ouvrira la mécanique en grand ; ici, garde l\'idée : les génériques peuvent se contraindre les uns par rapport aux autres, et les clés d\'un objet forment un type à part entière.' },
            { t: 'h3', h: 'Interfaces génériques : le Repository de la Boutique' },
            { t: 'code', lang: 'ts', label: 'repository.ts', code:
'// Un contrat de stockage, indépendant de CE qui est stocké\ninterface Repository<T> {\n  tous(): T[];\n  parId(id: string): T | undefined;\n  ajouter(item: T): T;\n}\n\n// Trois magasins, un seul contrat :\ntype CatalogueProduits = Repository<Produit>;\ntype RegistreMembres = Repository<MembreTontine>;\ntype HistoriquePaiements = Repository<Paiement>;\n\n// Le code CONSOMMATEUR ignore tout du type concret :\nfunction compter<T>(depot: Repository<T>): number {\n  return depot.tous().length;   // marche pour CHAQUE magasin\n}' },
            { t: 'p', h: 'Voici l\'architecture que tout le monde finit par écrire : le même service (stocker, lister, retrouver) pour des données différentes — produits, membres, paiements. L\'interface générique capture le CONTRAT une fois ; chaque magasin concret n\'est plus qu\'une instanciation. Deux bénéfices d\'équipe, énormes : les développeurs n\'apprennent qu\'UN vocabulaire de stockage (`tous`, `parId`, `ajouter`) quel que soit le domaine, et les fonctions « transverses » (compter, exporter, sauvegarder) s\'écrivent une seule fois, en générique. On frôle le design pattern — mais sans boilerplate : tout tient dans la déclaration du contrat.' },
            { t: 'h3', h: 'Les paramètres par défaut : `T = …`' },
            { t: 'code', lang: 'ts', label: 'defauts.ts', code:
'interface ApiResponse<T = unknown> {\n  ok: boolean;\n  data: T;\n}\n\n// Usage simple : pas besoin de préciser, data vaut unknown\nconst ping: ApiResponse = { ok: true, data: null };\n\n// Usage avancé : on précise quand on sait\ntype ReponseCatalogue = ApiResponse<Produit[]>;\n\n// Idem sur les fonctions :\nfunction creerCache<T = string>(): Map<string, T> {\n  return new Map();\n}\ncreerCache();          // Map<string, string>\ncreerCache<number>();  // Map<string, number>' },
            { t: 'p', h: 'Comme les paramètres de fonction (`= false`), les paramètres de type acceptent une valeur PAR DÉFAUT : si l\'utilisateur ne fournit pas T, c\'est le défaut qui s\'applique. L\'effet recherché est ergonomique : l\'API reste simple pour l\'usage courant (« une réponse, je ne sais pas encore de quoi » → `unknown`, qui FORCERA une validation avant usage — la bonne hygiène par défaut) et précise pour l\'usage avancé. Quand tu conçois un type public, demande-toi ce que signifie « l\'appelant n\'a rien précisé », et choisis le défaut qui rend l\'oubli SÛR plutôt que permissif : `unknown` bat `any` à ce jeu, à tous les coups.' },
            { t: 'h3', h: 'Le catalogue des formes de contrainte' },
            { t: 'table', head: ['Besoin', 'Écriture', 'Effet'], rows: [
              ['Capacité minimale (length, une méthode…)', 'T extends { length: number }', 'T garde sa précision, la capacité est garantie'],
              ['Clé d\'un objet', 'K extends keyof T', 'le second paramètre navigue dans les clés du premier'],
              ['Une liste fermée de valeurs', 'T extends \"petit\" | \"grand\"', 'T ne peut être qu\'un de ces littéraux'],
              ['Un constructeur', 'T extends new () => object', 'permet new T() à l\'intérieur (avancé)'],
              ['Adoucir l\'usage', 'T = unknown', 'l\'appelant peut omettre T en sécurité']
            ]},
            { t: 'h3', h: 'Sous le capot : comment T est choisi, puis effacé' },
            { t: 'p', h: 'Deux mécaniques pour finir de bien comprendre. Côté INFÉRENCE : quand plusieurs arguments peuvent fixer `T`, le compilateur cherche le meilleur candidat commun — et signale quand ils se contredisent (`fusionne(1, \"a\")` avec un seul T, c\'est une erreur, parce que number et string ne peuvent pas être un même T). Côté EXÉCUTION : contraintes comprises, tout s\'efface — ni `T`, ni `keyof`, ni les défauts ne laissent de trace dans le JavaScript émis. Le runtime ne voit que des objets et des fonctions ordinaires ; toute cette machinerie n\'existe que pour que TON code soit relu avant de démarrer. C\'est le paradoxe fondateur du langage, une fois de plus : plus la vérification est riche, plus la sortie est banale.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« extends = héritage »** — ici, non : c\'est « a AU MOINS cette forme ». `{ length: number }` n\'est pas une classe mère ; c\'est un seuil structurel que les strings et les tableaux franchissent naturellement.',
              '**« la contrainte transforme T »** — non : `T extends Produit` ne convertit rien en `Produit` ; T reste le type EXACT fourni par l\'appelant, simplement prouvé compatible. Retourner `Produit` au lieu de `T` jetterait cette précision (première carte d\'erreur).',
              '**« il faut contraindre chaque générique »** — au contraire : un T sans besoin de capacité particulière reste libre (contenants, enveloppes). La contrainte se justifie par un USAGE intérieur précis, sinon elle ne fait que repousser des types légitimes.',
              '**« keyof T liste les valeurs »** — non : les CLÉS, sous forme d\'union de littéraux. Les valeurs s\'obtiennent ensuite via `T[K]` — clés et valeurs sont deux projections différentes du même objet.',
              '**« le défaut T = X force X »** — non : il s\'applique SEULEMENT quand l\'appelant omet T et que rien ne l\'infère. Dès que T est fourni ou déduit, le défaut s\'efface sans discuter.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Cette fiche s\'appuie sur `ts-generics` (mécanique de base, inférence à l\'appel), sur les interfaces de `ts-interfaces` (le contrat `Repository<T>` n\'est qu\'une interface avec chevrons), et annonce deux approfondissements majeurs : `keyof` déplie ses secrets dans `ts-keyof-typeof`, et l\'idée « exiger au moins une forme » trouve son aboutissement dans les unions discriminées (`ts-discriminated-unions`) quand le T est un choix d\'états. Côté JavaScript, les paramètres par défaut de `js-fonctions` sont le même réflexe, un étage plus bas : ergonomie d\'appel, oubli sans danger.' },
            { t: 'callout', kind: 'tip', h: 'Heuristique de design : commence TOUJOURS par `<T>` libre ; ajoute `extends` seulement quand le corps de la fonction te demande une capacité précise ; ajoute `= X` seulement quand l\'appelant peut légitimement ne pas savoir. Trois étages, trois besoins — dans cet ordre, jamais l\'inverse.' }
          ],
          errors: [
            { title: 'Jeter la précision au retour', lang: 'ts', bad:
'function etiqueter<T extends Produit>(p: T): Produit {\n  return p;   // le type EXACT de l\'appelant est perdu en route\n}\nconst gari = etiqueter({ id: \"G1\", nom: \"Gari\", prix: 21000, promo: 500 });\ngari.promo;   // ✗ promo n\'existe plus : Produit ne le connaît pas', good:
'function etiqueter<T extends Produit>(p: T): T {\n  return p;   // T reste T : promo survit, toute la forme voyage\n}', why: 'La contrainte prouve la COMPATIBILITÉ, elle ne CONVERTIT pas. Annoter le retour avec la contrainte (`: Produit`) rabote tout ce que T avait de plus riche — l\'appelant perd ses propres propriétés. Toujours faire voyager T de bout en bout : c\'est lui qui porte la précision.' },
            { title: 'Un paramètre de type décoratif', lang: 'ts', bad:
'function saluer<T extends string>(nom: T): string {\n  return \"Bonjour \" + nom;\n}\n// T n\'apporte RIEN : ni lien, ni contrainte utile, ni précision conservée', good:
'function saluer(nom: string): string {\n  return \"Bonjour \" + nom;\n}', why: 'Un T qui n\'apparaît qu\'UNE fois ne lie rien à rien : l\'inférence n\'a aucune relation à établir, le générique est du bruit cérémoniel qui suggère une subtilité absente. Le générique se justifie dès qu\'il LIE deux positions (entrée/sortie, objet/clé) — pas comme décoration de signature.' }
          ],
          related: ['ts-generics', 'ts-keyof-typeof', 'ts-interfaces', 'ts-utility-types']
        }
      ]
    },
  {
      id: 'narrowing',
      name: 'Narrowing & gardes',
      icon: 'filter_alt',
      fiches: [
        {
          id: 'ts-narrowing',
          title: 'Narrowing intégré : typeof, instanceof, in',
          icon: 'search',
          level: 'Intermédiaire',
          tagline: 'Transformer « peut-être » en « certainement » — les tests que le compilateur comprend.',
          intro: 'La fiche Unions t\'a appris la règle d\'or : sans vérification, TS n\'autorise que le COMMUN aux membres d\'une union. Le **narrowing** (réduction) est le mécanisme miroir : certains tests en JavaScript — `typeof`, `instanceof`, `in`, comparaisons, vérité — sont RECONNUS par le compilateur, qui réduit le type de la variable dans la branche protégée. Tu ne changes rien au code qui tourne ; tu écris les mêmes tests qu\'en JS défensif — mais désormais, chaque test ACHÈTE une garantie de type. C\'est l\'expérience TypeScript dans ce qu\'elle a de plus agréable : écrire du code prudent et voir le compilateur encaisser chaque précaution comme un crédit.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : du doute à la certitude, branche par branche' },
            { t: 'p', h: 'À la caisse de la Boutique Awa, l\'identifiant d\'un produit peut arriver comme code numérique (`1042`) ou comme référence texte (`\"GARI-50\"`). Afficher une fiche exige de traiter les deux — mais `toFixed` n\'existe que sur l\'un, `toUpperCase` que sur l\'autre. En JavaScript, tu écris des `if (typeof …)` par PRUDENCE, en espérant ne rien oublier. En TypeScript, ces mêmes tests deviennent des PREUVES : à l\'intérieur de la branche, le compilateur a SUIVI ton raisonnement — il sait que la variable est un number ici, une string là, et il accorde à chacune ses méthodes légitimes en refusant les autres. Le nom du mécanisme est parlant : narrowing, « rétrécissement » — l\'ensemble des types possibles se rétrécit au fil de tes tests, jusqu\'à la certitude.' },
            { t: 'h3', h: 'typeof : le garde des primitifs' },
            { t: 'code', lang: 'ts', label: 'typeof.ts', code:
'function afficherId(id: string | number) {\n  if (typeof id === \"string\") {\n    return id.toUpperCase();        // ✓ ici : string, toutes ses méthodes\n  }\n  // Ici, TS sait que ce N\'EST PAS une string -> number\n  return id.toFixed(0);             // ✓ réduit automatiquement\n}\n\n// Les valeurs de retour de typeof — la liste est pauvre :\n// \"string\" \"number\" \"boolean\" \"object\" \"function\" \"undefined\" \"symbol\" \"bigint\"' },
            { t: 'p', h: 'Deux réflexes à prendre avec `typeof`. D\'abord, sa liste de verdicts est COURTE et textuelle — retourne des strings, compare avec `===`, et désespère du jour où tu chercheras `\"array\"` : les tableaux, les dates, `null` rapportent tous `\"object\"` (première carte d\'erreur). Ensuite, note le mouvement automatique : tu n\'as vérifié QUE le cas string… et TS en déduit que l\'autre branche contient « tout ce qui reste », ici `number`. La réduction marche dans les DEUX sens du test : ce qui est prouvé, et ce qui est exclus. C\'est de la soustraction d\'ensembles, appliquée à TON flot de code.' },
            { t: 'h3', h: 'instanceof : le garde des classes' },
            { t: 'code', lang: 'ts', label: 'instanceof.ts', code:
'function horodater(x: Date | string): string {\n  if (x instanceof Date) {\n    return x.toLocaleDateString(\"fr-FR\");   // ✓ une vraie Date\n  }\n  return x;                                 // ✓ ici : string\n}\n\n// Le réflexe salutaire avec les erreurs reçues : catch (e: unknown)\nfunction cause(e: unknown): string {\n  if (e instanceof Error) return e.message;   // ✓ message garanti\n  return String(e);\n}' },
            { t: 'p', h: '`instanceof` vérifie la CHAÎNE DE PROTOTYPES — « cet objet descend-il de cette classe ? » — et TypeScript comprend le verdict, y compris avec TES propres classes (les fiches Classes arrivent). Deux cas d\'usage dominent : distinguer des classes natives (`Date`, `Error`, `RegExp`…) d\'autres formes, et — quotidien — la clause `catch`, dont l\'objet reçu est `unknown` en mode strict : `e instanceof Error` est LA porte d\'entrée vers `e.message` sans cast douteux. Limite à connaître : `instanceof` interroge le RUNTIME, donc il est aveugle aux interfaces (effacées, sans existence) — pour distinguer des formes pures, c\'est le garde suivant qui prend le relais.' },
            { t: 'h3', h: 'in : le garde des formes d\'objets' },
            { t: 'code', lang: 'ts', label: 'in.ts', code:
'interface Virement { montant: number; reference: string; }\ninterface Especes { montant: number; billets: number[]; }\n\nfunction verifier(p: Virement | Especes): string {\n  if (\"reference\" in p) {\n    return \"Réf. MoMo : \" + p.reference;     // ✓ ici : Virement\n  }\n  // Ici, TS sait que reference est ABSENT -> Especes\n  return p.billets.length + \" billets à compter\";\n}' },
            { t: 'p', h: 'Quand deux formes se distinguent par la PRÉSENCE d\'une propriété, l\'opérateur `in` de JavaScript devient un garde de types : `\"reference\" in p` sépare Virement d\'Especes, proprement, dans les deux sens. C\'est l\'outil naturel pour les unions d\'objets non étiquetés — mais avouons-le, il teste une HABITUDE plutôt qu\'un contrat : si demain un troisième type possède AUSSI `reference`, le raisonnement se fissure. C\'est la fiche Unions discriminées qui apporte la réponse définitive : un champ `type` littéral, commun et distinct — le discriminant EST un garde `in` institutionnalisé, sans ambiguïté possible. Garde `in` pour les unions simples et les librairies externes.' },
            { t: 'h3', h: 'Les gardes implicites : vérité, égalité stricte, Array.isArray' },
            { t: 'code', lang: 'ts', label: 'implicites.ts', code:
'// La vérité (truthiness) : retire null / undefined… et aussi 0, \"\", NaN !\nfunction remise(optionnel: number | undefined) {\n  if (optionnel !== undefined) {          // ✓ précis : le 0 d\'une promo gratuite survit\n    return optionnel * 0.9;\n  }\n  return \"pas de promo\";\n}\n\n// L\'égalité stricte : réduit aux singletons\nfunction message(s: string | null) {\n  if (s === null) return \"rien\";\n  return s.toUpperCase();                  // ✓ string\n}\n\n// Array.isArray : LE garde officiel des tableaux\nfunction total(x: number | number[]): number {\n  if (Array.isArray(x)) return x.reduce((a, b) => a + b, 0);   // ✓ number[]\n  return x;                                                     // ✓ number\n}' },
            { t: 'p', h: 'Trois réflexes, trois degrés d\'attention. La VÉRITÉ (`if (x)`) est commode mais dangereuse avec les nombres et les textes : `0`, `\"\"` et `NaN` sont « faux » — un total de zéro FCFA disparaîtrait du calcul ; avec les montants, compare EXPLICITEMENT (`!== undefined`). L\'ÉGALITÉ STRICTE est le chirurgien : `=== null` retire exactement `null`, sans toucher au reste. Et `Array.isArray` est LE garde agréé par le compilateur pour les tableaux — `typeof` ne les distingue pas, `instanceof Array` a des limites historiques entre contextes (iframes) : `isArray` est la voie garantie, recommandée, et parfaitement comprise du narrowing.' },
            { t: 'h3', h: 'Quel garde pour quelle cible ?' },
            { t: 'table', head: ['Cible à distinguer', 'Garde', 'Piège à connaître'], rows: [
              ['Primitifs (string, number, boolean…)', 'typeof', 'pas de \"array\", null rend \"object\"'],
              ['Instances de classes (Date, Error, les tiennes)', 'instanceof', 'aveugle aux interfaces (effacées)'],
              ['Formes d\'objets (propriété présente ?)', 'opérateur in', 'fragile si les formes se rapprochent'],
              ['Tableaux', 'Array.isArray', 'typeof est muet sur les tableaux'],
              ['null / undefined', '=== ou ??', 'la vérité avale aussi 0 et \"\"'],
              ['Unions étiquetées (champ littéral)', 'switch / === sur le discriminant', 'le champ doit être un literal type']
            ]},
            { t: 'h3', h: 'Sous le capot : l\'analyse de flux (control flow analysis)' },
            { t: 'p', h: 'Le vérificateur ne se contente pas de regarder les `if` : il SUIT ton flot d\'exécution, ligne par ligne, comme un lecteur très appliqué. Les retours anticipés (« guard clauses ») resserrent le type dès la sortie de branche : après `if (s === null) return;`, tout le reste de la fonction travaille sur `string` — pas besoin de `else`, la suite EST l\'autre branche. Il suit aussi les réaffectations : une fois que tu assignes une valeur plus précise à une variable `let`, son type affiné suit la nouvelle valeur. Mais — et c\'est important — l\'analyse est LOCALE ET PRUDENTE : une variable modifiable capturée par une fonction (callback, `async`) peut avoir changé entre le test et l\'usage ; TS revient alors au type déclaré dans ces zones-là. D\'où le réflexe du pro : tester, puis TRAVAILLER IMMÉDIATEMENT, ou ranger la version réduite dans une `const` qui ne bougera plus.' },
            { t: 'h3', h: 'Exemple : l\'encaissement de la Boutique Awa, réduit au propre' },
            { t: 'code', lang: 'ts', label: 'encaissement.ts', code:
'type Paiement =\n  | { moyen: \"momo\"; montant: number; reference: string }\n  | { moyen: \"moov\"; montant: number; reference: string }\n  | { moyen: \"especes\"; montant: number };\n\nfunction encaisser(p: Paiement): string {\n  switch (p.moyen) {\n    case \"momo\":  return \"MoMo \" + p.reference + \" : \" + p.montant + \" F\";\n    case \"moov\":  return \"Moov \" + p.reference + \" : \" + p.montant + \" F\";\n    case \"especes\": return \"Espèces : \" + p.montant + \" F (compter les billets)\";\n  }\n}\n\n// Et avec un identifiant souple en entrée :\nfunction ficheProduit(id: string | number, stock: number | null): string {\n  const ref = typeof id === \"number\" ? \"PROD-\" + id : id;\n  if (stock === null) return ref + \" — stock inconnu\";\n  if (stock === 0)    return ref + \" — RUPTURE\";\n  return ref + \" — \" + stock + \" en rayon\";\n}' },
            { t: 'p', h: 'Le premier exemple combine deux fiches : l\'union discriminée (`moyen` comme étiquette) et le narrowing par `switch` — chaque branche voit SON paiement, référence ou non, sans aucun « peut-être » dans les accès. Le second montre le style « réduit puis enchaîne » : normaliser `id` en une seule const, traiter `null` séparément de `0` — la rupture de stock (zéro) EST une information, pas une absence. Ce petit choix de modélisation, permis par un narrowing précis, évite l\'un des bugs les plus banals du e-commerce : confondre « inconnu » et « à zéro ».' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« typeof reconnaît les tableaux »** — non : `typeof [] === \"object\"`. Seul `Array.isArray` distingue proprement un tableau, et c\'est le seul garde que le narrowing sait lire à cet effet.',
              '**« if (x) suffit toujours »** — la vérité avale `0`, `\"\"`, `NaN` en plus de `null`/`undefined`. Avec des montants, des stocks, des quantités : compare explicitement, ou accepte de perdre les zéros.',
              '**« le narrowing survive aux await »** — pour une variable `let` partagée, pas garanti : entre le test et la suite d\'un `await`, le monde a pu tourner. Le réflexe : réduire dans une `const`, qui ne peut plus bouger.',
              '**« instanceof fonctionne sur les interfaces »** — impossible : les interfaces sont effacées, elles n\'existent pas au runtime. `instanceof` ne distingue que des CLASSES (et objets natifs) ; pour les formes : `in`, discriminant, ou garde maison.',
              '**« un seul garde pour tout »** — chaque garde a son terrain : primitifs → typeof, classes → instanceof, présence → in, tableaux → isArray, étiquettes → discriminant. Choisir le bon, c\'est déjà la moitié du narrowing.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Tu remets ici en musique les conditions de `js-conditions` — les mêmes tests, mais crédités de garanties — et la règle des unions de `ts-unions` (« seul le commun sans test ») trouve enfin ses outils. Le `switch` sur discriminant vient de `ts-discriminated-unions` ; les types `unknown` et `null`/`undefined` de `ts-types-speciaux` sont précisément ceux que ces gardes débloquent. La suite logique et immédiate : `ts-type-guards`, où tu apprendras à écrire TES propres gardes — celles que le compilateur ne peut pas deviner seul, comme « cet objet venu du réseau est-il bien un Produit ? ».' },
            { t: 'callout', kind: 'tip', h: 'Style recommandé en équipe : traiter les cas anormaux EN PREMIER avec un retour anticipé (`if (stock === null) return …`), puis laisser le « chemin heureux » s\'écouler à plat, tout en bas, pleinement réduit. C\'est le pattern « guard clause » : moins d\'imbrication, un narrowing plus simple à suivre pour le compilateur — et pour toi dans six mois.' }
          ],
          errors: [
            { title: 'Chercher typeof x === "array"', lang: 'ts', bad:
'function taille(x: number | number[]): number {\n  if (typeof x === \"array\") {   // ✗ \"array\" n\'existe PAS dans les verdicts\n    return x.length;            // branche morte : jamais exécutée\n  }\n  return 1;\n}', good:
'if (Array.isArray(x)) {         // ✓ vrai test ET narrowing officiel\n  return x.length;              //    ici : number[], garanti\n}', why: '`typeof` rend toujours `\"object\"` pour les tableaux (héritage historique du JS) : ta branche ne s\'exécute jamais et le type n\'est pas réduit. `Array.isArray` est à la fois le bon test au runtime ET le seul que le compilateur interprète comme un garde de tableaux.' },
            { title: 'Réduire un montant avec la simple vérité', lang: 'ts', bad:
'function total(ht: number, remise?: number) {\n  if (remise) {                  // ✗ remise = 0 (promo 100 %) est avalée !\n    return ht * (1 - remise);\n  }\n  return ht;\n}', good:
'function total(ht: number, remise?: number) {\n  if (remise !== undefined) {    // ✓ seul l\'absent est écarté\n    return ht * (1 - remise);\n  }\n  return ht;\n}', why: 'La vérité confond « absent » et « zéro » : une remise de 0 — mathématiquement légitime — prendrait le chemin « pas de remise ». Dès qu\'un type contient `undefined` ou `null` autour de nombres ou de textes, compare EXPLICITEMENT : `!== undefined` retire exactement ce qu\'il faut, ni plus, ni moins.' }
          ],
          related: ['ts-type-guards', 'ts-discriminated-unions', 'ts-unions', 'js-conditions']
        },
        {
          id: 'ts-type-guards',
          title: 'Type guards maison : is & asserts',
          icon: 'shield',
          level: 'Avancé',
          tagline: 'Enseigner au compilateur TES règles métier : « si je dis vrai, alors c\'est un Produit ».',
          intro: '`typeof`, `instanceof`, `in`… le compilateur reconnaît les tests GÉNÉRIQUES du langage. Mais « cet objet reçu du serveur est-il un Produit conforme ? » — ça, seule TA mécanique métier peut le décider : `nom` string non vide, `prix` number positif, `stock` entier. Les **type guards maison** branchent cette vérification sur le système de types : le prédicat `x is Produit` promet « si je retourne true, traite x comme un Produit », la fonction `asserts x is Produit` promet « si je ne lance pas d\'erreur, la voie est sûre ». C\'est le pont définitif entre le monde extérieur non fiable (API, JSON, formulaires) et ton intérieur rigoureusement typé — l\'endroit précis où `unknown` devient `Produit`, honnêtement.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : le compilateur ne connaît pas ton métier' },
            { t: 'p', h: 'Souviens-toi de la fiche `ts-types-speciaux` : une donnée externe (fetch, JSON.parse, localStorage) doit arriver en `unknown` — le compilateur refuse alors tout usage, et il a raison. Les gardes intégrés de la fiche précédente réduisent les cas SIMPLES (un string contre un number, une Date contre du texte). Mais distinguer « ce JSON est un Produit conforme de la Boutique Awa » de « ce JSON est du bruit » n\'entre dans AUCUNE case standard : c\'est une règle métier — la forme exacte de TON catalogue. Si tu la vérifies dans une fonction ordinaire qui retourne `true`/`false`, le compilateur reste aveugle : il voit un booléen, pas une preuve. Los type guards maison existent pour RELIER les deux : ta fonction décide, et le système de types en tient compte, comme si le test était natif.' },
            { t: 'h3', h: 'Le prédicat de type : `x is Produit`' },
            { t: 'code', lang: 'ts', label: 'est-produit.ts', code:
'interface Produit {\n  id: string;\n  nom: string;\n  prix: number;\n  stock: number;\n}\n\n// L\'annotation est ICI : « si je renvoie true, x EST un Produit »\nfunction estProduit(x: unknown): x is Produit {\n  if (typeof x !== \"object\" || x === null) return false;\n  const o = x as Record<string, unknown>;\n  return typeof o.id === \"string\"\n      && typeof o.nom === \"string\" && o.nom.length > 0\n      && typeof o.prix === \"number\" && o.prix > 0\n      && typeof o.stock === \"number\" && o.stock >= 0;\n}\n\nconst brut: unknown = JSON.parse(texte);\nif (estProduit(brut)) {\n  console.log(brut.prix);   // ✓ ici, brut EST un Produit : accès libre\n}' },
            { t: 'p', h: 'Trois pièces à assembler. 1) Une fonction ORDINAIRE qui retourne un booléen — ton code, ta logique, exécutée au runtime pour de vrai. 2) Une annotation de retour spéciale, `x is Produit`, au lieu du simple `boolean` : elle déclare au compilateur « mon true est une PREUVE ». 3) Au point d\'appel, le narrowing habituel prend le relais : dans la branche protégée, `brut` devient `Produit`, autocomplété, sans cast. Note l\'écriture interne : on teste d\'abord « objet non null » (car `typeof null === \"object\"`, le piège), puis on ouvre la boîte via un type dictionnaire temporaire pour inspecter chaque champ avec `typeof`. C\'est verbeux — et c\'est le prix juste : chaque seconde de doute est convertie en CERTITUDE réutilisable.' },
            { t: 'h3', h: 'Exemple : valider le catalogue reçu du serveur' },
            { t: 'code', lang: 'ts', label: 'catalogue.ts', code:
'async function chargerCatalogue(): Promise<Produit[]> {\n  const res = await fetch(\"/api/produits\");\n  const brut: unknown = await res.json();\n\n  if (!Array.isArray(brut)) {\n    throw new Error(\"Catalogue invalide : ce n\'est pas un tableau\");\n  }\n  // filter + garde : le tableau RÉSULTANT est typé Produit[]\n  const propres = brut.filter(estProduit);\n  if (propres.length !== brut.length) {\n    console.warn(brut.length - propres.length + \" ligne(s) ignorée(s)\");\n  }\n  return propres;\n}\n\n// Ou le garde-chiourme pour tableaux entiers :\nfunction sontTousProduits(xs: unknown[]): xs is Produit[] {\n  return xs.every(estProduit);   // le prédicat se compose aux tableaux\n}' },
            { t: 'p', h: 'Deux usages, deux philosophies. `filter(estProduit)` est TOLÉRANT : les lignes corrompues sont écartées, le reste passe — et la cerise sur le gâteau, TS comprend le prédicat DANS filter et type le résultat `Produit[]` (les méthodes d\'array connaissent les gardes !). `every` + prédicat de tableau est STRICT : tout conforme ou rien. Et le journal d\'avertissement n\'est pas décoratif : quand l\'API change (et elle changera), c\'est ce `console.warn` qui te préviendra avant que le rayon ne s\'affiche vide. Règle de la maison : une donnée externe entre par la PORTE DES GARDES, jamais par la fenêtre du `as`.' },
            { t: 'h3', h: 'Les assertions : `asserts x is T`' },
            { t: 'code', lang: 'ts', label: 'asserts.ts', code:
'function affirmeProduit(x: unknown): asserts x is Produit {\n  if (!estProduit(x)) {\n    throw new Error(\"Refusé à l\'entrée : ce n\'est pas un Produit conforme\");\n  }\n}\n\nfunction traiterLigne(texte: string) {\n  const brut: unknown = JSON.parse(texte);\n  affirmeProduit(brut);\n  // Si on lit CETTE ligne, la preuve est faite :\n  brut.prix.toFixed(0);   // ✓ brut est un Produit pour le reste du bloc\n}' },
            { t: 'p', h: 'Le prédicat `is` RÉPOND à une question ; l\'assertion `asserts` TRANCHE : « soit c\'est un Produit, soit je lève une erreur ». Si la fonction se termine normalement, le compilateur considère la variable comme prouvée POUR LE RESTE DU BLOC — pas besoin d\'enrouler dans un `if`. C\'est l\'outil des frontières strictes : début de fonction, point d\'entrée d\'un module, validation de configuration au démarrage — les endroits où une donnée invalide doit ARRÊTER le programme plutôt que le contaminer. Choisis selon la gravité : `is` pour filtrer et dégrader en douceur, `asserts` pour refuser net. La Boutique Awa utilise `is` sur les lignes du catalogue (une ligne pourrie ne vide pas le rayon) et `asserts` sur son propre fichier de configuration (une config pourrie, si : tout s\'arrête au démarrage).' },
            { t: 'h3', h: 'is, asserts, ou cast ? Le tableau de décision' },
            { t: 'table', head: ['Outil', 'Vérifies-tu vraiment ?', 'Quand l\'utiliser'], rows: [
              ['as Produit (cast)', 'NON — pure affirmation', 'quasi jamais sur de l\'externe ; ok en interne prouvé'],
              ['x is Produit (prédicat)', 'oui, code booléen exécuté', 'filtrer, tester, dégrader en douceur'],
              ['asserts x is Produit', 'oui, code qui peut lancer', 'frontière stricte : invalide = exception'],
              ['Bibliothèque (zod…)', 'oui, schéma déclaratif', 'gros contrats d\'API (même mécanique, générée)']
            ]},
            { t: 'h3', h: 'Sous le capot : une promesse que RIEN ne vérifie' },
            { t: 'p', h: 'Le point le plus important de la fiche tient en une phrase : le compilateur NE VÉRIFIE PAS que ta fonction teste bien ce que l\'annotation promet. Écris `function estProduit(x: unknown): x is Produit { return true; }` — il compile, et il te croit. Le prédicat est une DECLARATION SUR L\'HONNEUR : à toi d\'écrire les tests qui rendent la promesse vraie, et de les maintenir quand l\'interface évolue (ajoute un champ requis à `Produit`, pense à l\'ajouter à `estProduit` — un test unitaire sur des données volontairement pourries achète cette vigilance pour pas cher). Au runtime, d\'ailleurs, rien de spécial ne se passe : ta fonction s\'exécute comme n\'importe quel booléen ; le `is` n\'existe que pour la phase de vérification. Et si un jour ces gardes écrites à la main deviennent un fardeau, sache que des bibliothèques de schémas (zod et consœurs) GÉNÈRENT exactement ces prédicats à partir d\'une déclaration — le mécanisme que tu viens d\'apprendre est leur fondation.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« le compilateur contrôle ma garde »** — non : `x is T` est une promesse non vérifiée. Le compilateur relit l\'USAGE de la garde ; sa VÉRITÉ, c\'est ton code de test qui la fabrique. Une garde paresseuse est pire qu\'aucune : elle ment avec aplomb.',
              '**« as et is, même combat »** — tout le contraire : le cast `as` ne VÉRIFIE rien (affirmation gratuite, le runtime te contredira), le prédicat `is` exécute de vrais tests puis fait circuler la garantie. L\'un devine, l\'autre prouve.',
              '**« il faut un guard par champ »** — non : la garde teste la FORME entière, composée de petits `typeof` enchaînés. Un guard par type de frontière (Produit, Commande, Membre…) — réutilisé partout.',
              '**« asserts est dangereux, à éviter »** — il est EXPLICITE : mieux vaut une erreur franche à la frontière (« données corrompues, arrêt net ») qu\'un `NaN` qui voyage trois fonctions avant d\'exploser au moment de la facture.',
              '**« typeof suffit dans le guard »** — pas tout seul : `typeof x === \"object\"` est D\'ACCORD avec `null`, avec les tableaux, avec les Dates. La séquence correcte commence toujours par `typeof === \"object\" && !== null`, puis inspecte CHAMP PAR CHAMP.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'C\'est le sommet d\'une chaîne commencée trois fiches plus tôt : `ts-types-speciaux` a posé `unknown` (« la donnée externe n\'a pas de type tant qu\'on n\'a pas vérifié »), `ts-narrowing` a donné les tests reconnus, et voici les gardes MAISON pour tes formes à toi. `ts-discriminated-unions` complète le paysage de l\'autre côté : une fois la donnée validée, c\'est souvent une union étiquetée qui la modélise. Et côté JavaScript, `js-fetch` et `js-erreurs` retrouvent leur plein sens : rattraper le JSON, c\'est TA responsabilité — mais désormais, elle est typée, testée et réutilisable.' },
            { t: 'callout', kind: 'tip', h: 'Livre d\'or de la Boutique : chaque frontière (API, localStorage, import CSV, saisie formulaire) possède SA garde, rangée à côté de l\'interface qu\'elle vérifie (`estProduit` vit à côté de `Produit`), avec deux ou trois échantillons pourris en test. La garde qui voyage avec son contrat ne vieillit jamais seule.' }
          ],
          errors: [
            { title: 'Court-circuiter la vérification avec as', lang: 'ts', bad:
'const p = JSON.parse(texte) as Produit;\np.prix.toFixed(0);   // compile… puis puis CRASH le jour où l\'API change', good:
'const brut: unknown = JSON.parse(texte);\nif (!estProduit(brut)) throw new Error(\"Produit invalide\");\nbrut.prix.toFixed(0);   // ✓ prouvé par du code réellement exécuté', why: '`as` est une AFFIRMATION GRATUITE : rien ne s\'exécute, rien n\'est vérifié — si la réalité contredit, tu l\'apprends au runtime, loin de la frontière. Le guard maison fait le travail SOUVERAIN de la donnée externe : échouer tôt, à l\'entrée, avec un message clair.' },
            { title: 'Une garde qui ment', lang: 'ts', bad:
'function estProduit(x: unknown): x is Produit {\n  return true;   // compilateur satisfait, garantie nulle\n}', good:
'function estProduit(x: unknown): x is Produit {\n  if (typeof x !== \"object\" || x === null) return false;\n  const o = x as Record<string, unknown>;\n  return typeof o.nom === \"string\"\n      && typeof o.prix === \"number\" && o.prix > 0;\n}', why: 'L\'annotation `x is T` n\'est JAMAIS vérifiée par le compilateur : il te CROIT. Une garde-bouchon est donc pire que le cast — elle porte l\'uniforme de la preuve sans en avoir le contenu, et tout le code en aval s\'appuie sur ce mensonge avec confiance.' }
          ],
          related: ['ts-narrowing', 'ts-types-speciaux', 'ts-discriminated-unions', 'js-fetch']
        }
      ]
    },
  {
      id: 'classes',
      name: 'Classes',
      icon: 'category',
      fiches: [
        {
          id: 'ts-classes',
          title: 'Classes : modificateurs & raccourcis',
          icon: 'school',
          level: 'Débutant',
          tagline: 'La classe JS que tu connais, plus des gardiens d\'accès vérifiés à la compilation.',
          intro: 'Tu connais déjà les classes de JavaScript : un moule à objets, un constructeur, des méthodes. TypeScript les garde TELLES QUELLES au runtime — le JavaScript émis est ta classe, sans transformation — et y ajoute trois choses qui n\'existent qu\'à la compilation : des **propriétés typées**, des **modificateurs d\'accès** (`public`, `private`, `protected`, `readonly`) qui font respecter les frontières de l\'objet, et un raccourci de constructeur qui divise le boilerplate par deux. Résultat : l\'encapsulation, ce principe que JS laissait à la discipline de chacun (« ne touche pas à mon `_solde`, s\'il te plaît »), devient une règle que le compilateur fait appliquer. Cette fiche prend la classe que tu écris déjà et la met sous protection.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : l\'encapsulation, enfin appliquée' },
            { t: 'p', h: 'Un compte Mobile Money a UN invariant sacré : le solde ne change que par un dépôt ou un retrait contrôlé — jamais par écriture directe (`compte.solde = -500000`, le cauchemar de tout commerçant). En JavaScript, la seule défense était la CONVENTION : préfixer d\'un underscore et espérer que personne — y compris toi-même à 2 h du matin — n\'écrive dessus « temporairement ». TypeScript transforme la convention en frontière déclarée : `private solde` — et toute écriture directe devient une ERREUR, à l\'écriture, dans l\'éditeur. La classe reste la même machine à objets que tu connais ; elle gagne simplement des murs que le compilateur fait respecter, sans rien coûter au runtime.' },
            { t: 'h3', h: 'La classe minimale, version TypeScript' },
            { t: 'code', lang: 'ts', label: 'classe-minimale.ts', code:
'class CompteMoMo {\n  titulaire: string;           // propriété DÉCLARÉE et typée\n  private solde: number;    // inaccessible depuis l\'extérieur\n\n  constructor(titulaire: string, depotInitial: number = 0) {\n    this.titulaire = titulaire;\n    this.solde = depotInitial;\n  }\n\n  deposer(montant: number): void {\n    if (montant <= 0) throw new Error(\"Montant invalide\");\n    this.solde += montant;\n  }\n\n  lireSolde(): number {      // la lecture passe par une méthode\n    return this.solde;\n  }\n}\n\nconst c = new CompteMoMo(\"Awa Mensah\", 5000);\nc.deposer(21000);\nc.solde;        // ✗ private : refusé À L\'ÉCRITURE\nc.lireSolde();  // ✓ 26000, par la porte officielle' },
            { t: 'p', h: 'Deux différences avec la classe JS que tu connais. D\'abord, chaque propriété se DÉCLARE en tête de classe avec son type — en mode strict, le compilateur exige qu\'elle soit initialisée (au constructeur ou à la déclaration), ce qui éradique les `undefined` surprises. Ensuite, le mot-clé `private` devant `solde` : la dernière ligne du mauvais exemple ne compile plus. Le reste — constructeur, méthodes, `this` — est du JavaScript standard ; si tu ouvrais le fichier émis, tu retrouverais ta classe presque à l\'identique. Le typage n\'a rien changé à la machine : il a posé des verrous sur les portes.' },
            { t: 'h3', h: 'Le raccourci du constructeur : les « parameter properties »' },
            { t: 'code', lang: 'ts', label: 'raccourci.ts', code:
'// AVANT : déclarer, recevoir, recopier — trois fois la même chose\nclass ProduitAvant {\n  id: string;\n  nom: string;\n  prix: number;\n  constructor(id: string, nom: string, prix: number) {\n    this.id = id; this.nom = nom; this.prix = prix;\n  }\n}\n\n// APRÈS : le modificateur dans la signature fait TOUT à la fois\nclass Produit {\n  constructor(\n    public readonly id: string,\n    public nom: string,\n    public prix: number\n  ) {}\n}\nconst gari = new Produit(\"GARI-50\", \"Gari premium 50 kg\", 21000);\ngari.nom;      // ✓ public\ngari.id = \"X\"; // ✗ readonly' },
            { t: 'p', h: 'Ce raccourci est un pur sucre TypeScript : placer un modificateur (`public`, `private`, `protected`, `readonly`) DEVANT un paramètre du constructeur déclare la propriété, la reçoit et l\'assigne — en une seule geste. Le corps du constructeur reste vide (ou libre pour la logique réelle : validations, calculs). C\'est l\'une des syntaxes les plus agréables du langage, tu la liras partout dans le code professionnel — et au passage, note le duo `public readonly id` : lisible de partout, réassignable nulle part. C\'est exactement le réglage d\'un IDENTIFIANT : créé une fois, consulté à volonté, jamais modifié.' },
            { t: 'h3', h: 'Les trois niveaux d\'accès, sans jargon' },
            { t: 'table', head: ['Modificateur', 'Qui peut toucher ?', 'Usage typique'], rows: [
              ['public (défaut)', 'tout le monde, partout', 'méthodes et données publiques, API de l\'objet'],
              ['private', 'uniquement la classe ELLE-MÊME', 'état interne, invariants à protéger (solde…)'],
              ['protected', 'la classe ET ses descendants', 'pièces réutilisables par héritage'],
              ['readonly', 'lecture partout, écriture jamais après création', 'identifiants, dates de création, configuration']
            ]},
            { t: 'p', h: 'Le réflexe de conception à adopter : commence TOUT en `private`, puis n\'ouvre que ce qui est réellement utile à l\'extérieur — c\'est l\'inverse du réflexe JavaScript, et c\'est celui qui vieillit bien. Pourquoi ? Parce que chaque membre `public` est un CÂBLE branché entre ta classe et le reste du monde : impossible de renommer, de recalculer ou de supprimer sans casser quelqu\'un. Chaque membre `private` reste ton jardin : tu le remodelles quand tu veux, le compilateur garantit que personne ne s\'y est accroché. La règle professionnelle tient en une ligne : **l\'extérieur ne voit que des capacités (méthodes), jamais de la mécanique (état)**.' },
            { t: 'h3', h: 'static : ce qui appartient à la classe, pas aux instances' },
            { t: 'code', lang: 'ts', label: 'static.ts', code:
'class MembreTontine {\n  static totalMembres = 0;               // UN compteur pour toute la tontine\n  static readonly COTISATION = 5000;     // une règle partagée, figée\n\n  constructor(public nom: string) {\n    MembreTontine.totalMembres += 1;\n  }\n\n  cotiser(): number {\n    return MembreTontine.COTISATION;    // on passe par le NOM de la classe\n  }\n}\n\nnew MembreTontine(\"Awa Mensah\");\nnew MembreTontine(\"Kofi Agbo\");\nMembreTontine.totalMembres;   // 2 — pas besoin d\'instance pour consulter' },
            { t: 'p', h: '`static` rattache une propriété ou méthode à la CLASSE elle-même : un seul exemplaire, partagé par toutes les instances — le compteur de membres de la tontine, le montant de cotisation qui vaut pour tout le monde, une fabrique (`MembreTontine.depuisSaisie(texte)`). Au runtime, c\'est du JavaScript ordinaire : la propriété vit sur le constructeur. Piège classique de lecture : à l\'intérieur des méthodes, on y accède via le NOM DE CLASSE (`MembreTontine.COTISATION`), jamais via `this` — `this` désigne UNE instance, le static ne lui appartient pas. TypeScript ajoute ici sa seule touche : les modificateurs fonctionnent aussi sur le static (`private static cache`), et `readonly` fige les règles communes.' },
            { t: 'h3', h: 'Sous le capot : private est un verrou de COMPILE, pas de runtime' },
            { t: 'p', h: 'Le secret que peu de débutants connaissent : à l\'exécution, `private` n\'existe plus DU TOUT. Le JavaScript émis contient une propriété `solde` ordinaire, accessible comme n\'importe quelle autre — `console.log(JSON.stringify(compte))` l\'affichera, un `compte[\"solde\"]` dynamique la lira. Le privé de TypeScript est un CONTRAT RELU À LA COMPILATION : puissant contre les erreurs (99 % des cas), transparent face à la malice ou à l\'introspection. Si tu as besoin d\'un privé RÉEL, qui résiste au runtime lui-même, c\'est la syntaxe `#` native de JavaScript qu\'il te faut — c\'est précisément le sujet de la fiche suivante. Retiens la formule : `private` empêche les ACCIDENTS, `#` empêche les INTRUSIONS.' },
            { t: 'h3', h: 'Exemple complet : le compte de la Boutique Awa' },
            { t: 'code', lang: 'ts', label: 'compte-boutique.ts', code:
'class Caisse {\n  private solde = 0;\n  private readonly historique: string[] = [];\n\n  constructor(public readonly nom: string) {}\n\n  encaisser(montant: number, motif: string): void {\n    if (montant <= 0) throw new Error(\"Montant invalide\");\n    this.solde += montant;\n    this.historique.push(\"+\" + montant + \" F — \" + motif);\n  }\n\n  decaisser(montant: number, motif: string): void {\n    if (montant > this.solde) throw new Error(\"Solde insuffisant\");\n    this.solde -= montant;\n    this.historique.push(\"-\" + montant + \" F — \" + motif);\n  }\n\n  bilan(): string {\n    return this.nom + \" : \" + this.solde + \" F (\" +\n           this.historique.length + \" opérations)\";\n  }\n}\n\nconst caisse = new Caisse(\"Boutique Awa — Dantokpa\");\ncaisse.encaisser(21000, \"Vente gari premium\");\ncaisse.decaisser(350, \"Course zem fournisseur\");\ncaisse.bilan();          // \"Boutique Awa — Dantokpa : 20650 F (2 opérations)\"\ncaisse.solde = 999999;   // ✗ private : le fantasme reste refusé à l\'écriture' },
            { t: 'p', h: 'Tout l\'art de la fiche tient dans cet exemple : l\'état (`solde`, `historique`) est MURÉ ; le monde extérieur ne connaît que des CAPACITÉS (`encaisser`, `decaisser`, `bilan`). Chaque capacité fait respecter un invariant — montant positif, solde suffisant — avant de toucher l\'état. Résultat : impossible, littéralement impossible d\'écrire le code qui contourne les règles de la caisse, puisque le compilateur le refuse à la frappe. Quand tu reliras cette classe dans un an, tu n\'auras pas à te demander « qui a pu modifier ce solde ? » : personne, sauf les deux portes officielles.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« private existe à l\'exécution »** — non : c\'est un verrou de COMPILATION, effacé à l\'émission. Contre les accidents, c\'est parfait ; contre la malice ou la sérialisation, il faut le `#` natif (fiche suivante).',
              '**« readonly empêche de modifier un tableau »** — comme pour les interfaces : `readonly historique` interdit de RÉASSIGNER le tableau, pas de le remplir. Le gel du contenu demande `readonly string[]` en plus — ou `Object.freeze` au runtime.',
              '**« il faut déclarer ET assigner chaque propriété »** — la déclaration suffit, PLUS une initialisation au constructeur ou à la volée : en mode strict (`strictPropertyInitialization`), une propriété annoncée mais jamais remplie est une erreur — et c\'est une excellente nouvelle.',
              '**« protected = private mais pour deux classes »** — non : `protected` concerne l\'HÉRITAGE (accessible aux descendants), pas la collaboration entre classes voisines. Deux classes amies n\'ont pas de canal privé entre elles.',
              '**« static, c\'est pour les constantes seulement »** — les constantes partagées, OUI, mais aussi les fabriques (`Classe.depuisSaisie(...)`), les compteurs globaux au type, les registres : tout ce qui relève du TYPE plus que de l\'instance.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Tu retrouves ici les classes du JavaScript moderne (module JS, objets et prototypes de `js-objets`) — TypeScript n\'y ajoute que des verrous et des déclarations, jamais de nouvelle mécanique d\'exécution. Le `readonly` fait écho à celui des interfaces (`ts-interfaces`), dont l\'esprit « contrat » se prolongera dans le `implements` de la fiche suivante. L\'initialisation stricte, elle, est une conséquence directe du mode strict réglé dans `ts-tsc-config`. Et le privé réel `#` que tu verras dans `ts-classes-avancees` vient du JavaScript natif — preuve que les deux langages avancent main dans la main.' },
            { t: 'callout', kind: 'tip', h: 'Réflexe d\'atelier : dans chaque classe, relis les membres `public` à voix haute — chacun doit JUSTIFIER son ouverture (« l\'extérieur en a besoin parce que… »). Ceux qui bafouillent retournent en `private`. Ton futur refactor, lui, ne dira merci à personne d\'autre.' }
          ],
          errors: [
            { title: 'Croire que private protège au runtime', lang: 'ts', bad:
'const c = new Caisse(\"Boutique\");\nconsole.log(JSON.stringify(c));   // { \"solde\": 0, ... } — solde EXPOSE au log !\n(c as any).solde = -500;          // le cast contourne le verrou sans effort', good:
'class CaisseSolide {\n  #solde = 0;                     // privé NATIF JavaScript : mur au runtime\n  bilan(): number { return this.#solde; }\n}\n// JSON.stringify : #solde est invisible ; (c as any).#solde : impossible', why: '`private` est effacé à la compilation : la propriété reste une propriété ordinaire à l\'exécution — visible dans une sérialisation, ouvrable par un cast. Le `#` du JavaScript natif crée un VRAI champ privé physiquement inaccessible depuis l\'extérieur. Distingue toujours : verrous de compile (TS) contre erreurs, murs de runtime (#) contre intrusions.' },
            { title: 'Propriété déclarée, jamais remplie', lang: 'ts', bad:
'class Commande {\n  numero: string;\n  constructor() {}\n}\nnew Commande().numero.toUpperCase();   // ✗ compile même pas :\n// strictPropertyInitialization refuse la classe AVANT le crash undefined', good:
'class Commande {\n  numero: string;\n  constructor(numero: string) { this.numero = numero; }\n}\n// ou avec le raccourci : constructor(public numero: string) {}', why: 'En JavaScript, une propriété annoncée mais non initialisée vaut undefined et le crash attend son heure. Le mode strict de TS refuse la classe dès l\'ÉCRITURE : chaque propriété déclarée doit être initialisée au constructeur (ou marquée `numero?: string` / `numero!: string` si tu sais vraiment ce que tu fais).' }
          ],
          related: ['ts-classes-avancees', 'ts-interfaces', 'ts-types-scalaires', 'js-objets']
        },
        {
          id: 'ts-classes-avancees',
          title: 'Abstract, implements & #privé natif',
          icon: 'military_tech',
          level: 'Intermédiaire',
          tagline: 'Le contrat branché sur la fabrique : implements, bases abstraites, et un privé qui tient au runtime.',
          intro: 'Trois outils montent la classe TypeScript d\'un cran. `implements` **branche une classe sur un contrat d\'interface** : « cette fabrique promet de produire des objets conformes » — le compilateur vérifie la promesse. Les **classes abstraites** factorisent le commun sans pouvoir être instanciées : le squelette est offert, les variantes complètent. Et `#privé`, la syntaxe native de JavaScript, offre enfin un privé qui tient à l\'EXÉCUTION, là où le `private` de TypeScript n\'est qu\'un verrou de compilation. Avec ces trois pièces, les structures objet de la Boutique Awa passent de « bien écrites » à « architecturées ».',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : promettre, factoriser, verrouiller' },
            { t: 'p', h: 'Les classes de la fiche précédente se suffisent… jusqu\'au premier vrai projet. Trois besoins arrivent alors, prévisibles comme la saison des pluies. PROMETTRE : ta fonction `exporter(depot)` accepte un `Repository` — comment GARANTIR que `CatalogueProduits` honore ce contrat, aujourd\'hui et après chaque retouche ? FACTORISER : trois moyens de paiement (MoMo, Moov, espèces) partagent référence, montant, journal — comment écrire le commun UNE fois sans autoriser l\'instanciation d\'un « paiement générique » qui ne veut rien dire ? VERROUILLER : le `private` de TS disparaît au runtime — comment protéger un solde contre la sérialisation et l\'accès dynamique ? implements, abstract et # répondent à ces trois questions, dans l\'ordre.' },
            { t: 'h3', h: 'implements : la fabrique signe le contrat' },
            { t: 'code', lang: 'ts', label: 'implements.ts', code:
'interface Depot<T> {\n  tous(): T[];\n  parId(id: string): T | undefined;\n  ajouter(item: T): T;\n}\n\nclass CatalogueProduits implements Depot<Produit> {\n  private stock = new Map<string, Produit>();\n\n  tous(): Produit[] { return [...this.stock.values()]; }\n  parId(id: string): Produit | undefined { return this.stock.get(id); }\n  ajouter(p: Produit): Produit { this.stock.set(p.id, p); return p; }\n  // oublie UNE méthode du contrat → ERREUR sur la classe entière\n}\n\n// Le typage structurel aurait suffi au passage de valeur ; implements, lui,\n// verrouille la FABRIQUE : la classe ne compile plus si elle dévie.' },
            { t: 'p', h: 'Attention au raisonnement fin : sans `implements`, la classe pourrait QUAND MÊME servir de `Repository` (le typage structurel, fiche Interfaces, accepte toute forme compatible). Alors pourquoi écrire `implements` ? Parce que le CONTRÔLE change de camp. Sans lui, l\'erreur surgit au point d\'USAGE (« cet objet n\'est pas un Repository… » — et tu remontes comprendre pourquoi). Avec lui, l\'erreur surgit sur la CLASSE ELLE-MÊME, à la déclaration, avec la mention précise du membre manquant ou mal typé. C\'est la différence entre découvrir un défaut de fabrication au contrôle qualité de l\'usine, ou chez le client. Sur un projet sérieux, toute classe destinée à remplir un contrat écrit `implements` : la promesse est explicite, vérifiée, et documentée en tête de fichier.' },
            { t: 'h3', h: 'Les classes abstraites : le commun gratuit, l\'instanciation interdite' },
            { t: 'code', lang: 'ts', label: 'abstract.ts', code:
'abstract class Paiement {\n  constructor(public readonly montant: number, public readonly date: Date) {}\n\n  // LE COMMUN : écrit une fois, hérité par tous\n  recu(): string {\n    return this.reference() + \" — \" + this.montant + \" F le \" +\n           this.date.toLocaleDateString(\"fr-FR\");\n  }\n\n  // LA VARIABLE : chaque variante le fournira\n  abstract reference(): string;\n}\n\nclass PaiementMoMo extends Paiement {\n  constructor(m: number, private transactionMoMo: string) {\n    super(m, new Date());\n  }\n  reference(): string { return \"MoMo/\" + this.transactionMoMo; }\n}\n\nconst p = new PaiementMoMo(21000, \"8F3K2\");\np.recu();                     // ✓ le commun + la pièce fournie\nnew Paiement(500, new Date()); // ✗ la base ASTRAITE n\'est pas instanciable' },
            { t: 'p', h: 'Une classe abstraite est un DEMI-CONTRAT : elle peut contenir du code réel (propriétés, méthodes complètes, constructeur) ET des méthodes `abstract` — des signatures sans corps, que chaque sous-classe DEVRA fournir. Deux règles forment la mécanique : on ne peut pas l\'instancier directement (un « Paiement » sans moyen précis n\'existe pas dans la réalité — le type le dit) ; et la sous-classe qui en hérite est maintenue d\'honneur par le compilateur — oublie `reference()` et ta classe est refusée. Ce schéma s\'appelle le « template method » : la base écrit le SQUELETTE du comportement (`recu()`), les variantes branchent leurs pièces. Avant de le généraliser partout, souviens-toi des unions discriminées : pour des ÉTATS purs sans logique partagée, elles restent plus simples ; les classes abstraites brillent quand le COMMUN contient de vraies méthodes.' },
            { t: 'h3', h: '#privé : le privé qui tient au runtime' },
            { t: 'code', lang: 'ts', label: 'prive-natif.ts', code:
'class Compte {\n  #solde = 0;                    // champ privé NATIF (ES2022)\n  private soldeVuParTS = 0;      // privé TS : compile-only\n\n  solde(): number { return this.#solde; }\n}\n\nconst c = new Compte();\n// (c as any).#solde;     // ✗ IMPOSSIBLE, même au runtime, même avec un cast\n// JSON.stringify(c)      // #solde N\'APPARAÎT PAS dans la sérialisation\n// Object.keys(c)         // idem : invisible à l\'introspection\n(c as any).soldeVuParTS;  // ✓ …par contre le private TS, lui, a disparu' },
            { t: 'table', head: ['Critère', 'private (TS)', '# (JS natif)'], rows: [
              ['Existe au runtime', '✗ effacé', '✓ mur physique'],
              ['Visible dans JSON.stringify', 'oui', 'non'],
              ['Contournable par cast / clé dynamique', 'oui', 'non'],
              ['Visible dans la sous-classe', 'non (protected existe)', 'non, jamais'],
              ['Cible de compilation minimale', 'toutes', 'ES2022+ (transpilable)'],
              ['À choisir pour…', 'verrous anti-erreur, partout', 'secrets, état critique, libs publiques']
            ]},
            { t: 'p', h: 'Le `#` est une syntaxe de JAVASCRIPT MODERNE (ES2022), pas une invention TypeScript : le champ devient physiquement inaccessible hors de la classe — ni par un cast, ni par une clé dynamique, ni en sérialisant. TypeScript le reconnaît, le type, et le laisse intact à l\'émission. Alors lequel choisir ? Le réflexe professionnel : `private` PAR DÉFAUT (partout, tout le temps, comme verrou anti-accident), et `#` pour les cas où la confidentialité est une EXIGENCE — données sensibles, état critique d\'une librairie publique, encapsulation qui doit survire à des consommateurs hostiles ou maladroits. À noter : les deux mondes se touchent à peine — un `#champ` n\'est pas visible même depuis les SOUS-CLASSES, là où TS offre `protected` pour l\'héritage. Choisir l\'un ou l\'autre, c\'est choisir DE QUEL acheteurs tu te protèges.' },
            { t: 'h3', h: 'Sous le capot : deux couches de privé, deux philosophies' },
            { t: 'p', h: 'Pourquoi ce doublon déroutant ? Historique et philosophie. TypeScript date de 2012 : il a inventé `private` alors que JavaScript ne connaissait AUCUN privé — son but était la relecture, pas la sécurité, d\'où un verrou purement statique, gratuit au runtime (« type erasure », toujours elle). Le `#`, arrivé dans JS en 2022, naît d\'un besoin différent : un privé D\'EXÉCUTION, géré par le moteur, avec un coût mémoire conçu pour. Les deux cohabitent dans le même fichier sans se gêner — et TS compile même le `#` vers des WeakMaps quand la cible est ancienne. La leçon architecturale à garder : TypeScript et JavaScript se partagent le travail comme un couple rodé — l\'un VÉRIFIE avant (« le code est-il correct ? »), l\'autre GARANTIT pendant (« cette donnée est-elle inaccessible ? »). Savoir qui fait quoi, c\'est déjà architecturer.' },
            { t: 'h3', h: 'Exemple : la hiérarchie des paiements de la Boutique Awa' },
            { t: 'code', lang: 'ts', label: 'paiements-hierarchie.ts', code:
'interface Tracable {\n  reference(): string;\n}\n\nabstract class Paiement implements Tracable {\n  constructor(public readonly montant: number) {}\n  abstract reference(): string;\n\n  journal(): string {\n    return this.reference() + \" : \" + this.montant + \" F\";\n  }\n}\n\nclass PaiementMoMo extends Paiement {\n  #transactionId: string;              // secret de traitement, mur natif\n\n  constructor(montant: number, transactionId: string) {\n    super(montant);\n    this.#transactionId = transactionId;\n  }\n  reference(): string { return \"MoMo/\" + this.#transactionId; }\n}\n\nclass PaiementEspeces extends Paiement {\n  reference(): string { return \"CASH/\" + Math.floor(this.montant / 100); }\n}\n\nfunction enregistrer(p: Tracable): string {\n  return p.reference();   // le contrat suffit : la classe derrière peut varier\n}\n\nenregistrer(new PaiementMoMo(21000, \"8F3K2\"));   // ✓\nenregistrer(new PaiementEspeces(1500));          // ✓ même porte d\'entrée' },
            { t: 'p', h: 'Relis cet exemple lentement, il condense la fiche entière : `Tracable` est le CONTRAT public (une interface minimale — la meilleure architecture publie le moins possible) ; la classe abstraite fournit le SQUELETTE commun (`journal`) tout en imposant la pièce variable (`reference`) ; `implements` fait signer la base au contrat, donc TOUTES ses filles avec ; `#transactionId` verrouille la donnée sensible au runtime ; et la fonction consommatrice n\'exige que `Tracable` — elle ignore les classes, les hiérarchies, les secrets. Chaque outil joue SEULEMENT son rôle. Quand ton code atteint ce découpage, les changements deviennent locaux : un nouveau moyen de paiement = une nouvelle fille, sans retoucher ni la base, ni les consommateurs.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« implements COPIE les membres de l\'interface »** — non : il les EXIGE. Rien n\'est importé ni généré ; la classe doit écrire chaque membre elle-même. C\'est une promesse vérifiée, pas un approvisionnement.',
              '**« private et # sont interchangeables »** — le premier est un verrou de COMPILATION (effacé, contournable, JSON-visible), le second un mur d\'EXÉCUTION (physique, sérialisation invisible, cast inefficace). Même apparence, deux mondes.',
              '**« abstract, c\'est une classe privée plus forte »** — non : abstrait signifie « incomplète PAR DESIGN » — elle manque volontairement des pièces (méthodes abstract) que les filles fournissent. L\'interdiction d\'instancier est la conséquence, pas le but.',
              '**« protected s\'ouvre aux classes voisines »** — jamais : `protected` ne concerne QUE la descendance (héritage). Deux classes sœurs ne partagent rien en coulisses — c\'est l\'une des raisons d\'être des compositions de fonctions.',
              '**« il faut abstract partout »** — l\'héritage se rationne : pour des données sans logique commune, l\'union discriminée est plus simple ; pour un contrat pur, l\'interface suffit. La classe abstraite se mérite : du code RÉEL à partager, plus des pièces à imposer.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Tout filait vers ici : les interfaces de `ts-interfaces` deviennent des signatures d\'usine via `implements` ; les modificateurs de `ts-classes` trouvent leur complément d\'exécution avec `#` ; et l\'alternative « états purs » est la fiche `ts-discriminated-unions` — sache choisir entre hiérarchie de classes (logique partagée) et union étiquetée (données pures). Côté JavaScript, `js-objets` a posé le prototype et la syntaxe classe ; tu n\'as rien réappris du runtime, tu as branché des verrous et des contrats sur une mécanique déjà connue.' },
            { t: 'callout', kind: 'tip', h: 'Question d\'architecture qui clarifie tout : « ce que je partage, est-ce du CONTRAT ou du CODE ? » Contrat pur → interface + implements. Code et contrat → classe abstraite. Ni l\'un ni l\'autre, juste des données qui varient → union discriminée. Trois besoins, trois outils, zéro hésitation.' }
          ],
          errors: [
            { title: 'Confondre private et # privé', lang: 'ts', bad:
'class Compte {\n  private solde = 0;\n}\nconst c = new Compte();\nJSON.stringify({ c });       // solde APPARAÎT dans le log…\n(c as unknown as { solde: number }).solde = -1;   // contourne en une ligne', good:
'class Compte {\n  #solde = 0;                // mur d\'exécution : sérialisation aveugle,\n  voir(): number { return this.#solde; }   // cast inopérant\n}', why: '`private` ne vit qu\'à la compilation : au runtime c\'est une propriété ordinaire — sérialisable, introspectable, ouvrable par cast. Quand la confidentialité est une EXIGENCE (secrets, état critique, API publique), seul le `#` natif tient la route — et TS le connaît et le protège pareillement à l\'écriture.' },
            { title: 'Instancier une classe abstraite', lang: 'ts', bad:
'const p = new Paiement(500, new Date());\n// ✗ « Cannot create an instance of an abstract class »\n// la base est un SQUELETTE, pas un produit fini', good:
'const p = new PaiementMoMo(500, \"8F3K2\");\n// ou une fonction de dispatch qui choisit la fille selon le cas,\\n// exactement comme le réel : on ne paie jamais « en général »', why: 'Abstraite signifie INCOMPLÈTE : il lui manque des pièces (les méthodes abstract) que seules les sous-classes fournissent. Autoriser son instanciation reviendrait à vendre un paiement sans moyen — le compilateur refuse pour les mêmes raisons que la boutique : ça ne veut rien dire dans le monde réel.' }
          ],
          related: ['ts-classes', 'ts-interfaces', 'ts-discriminated-unions', 'js-objets']
        }
      ]
    },
  {
      id: 'utility-types',
      name: 'Utility types',
      icon: 'construction',
      fiches: [
        {
          id: 'ts-utility-types',
          title: 'Partial, Pick, Omit, Required & Readonly',
          icon: 'construction',
          level: 'Intermédiaire',
          tagline: 'Transformer une forme existante au lieu de la recopier — Produit reste la source, le reste dérive.',
          intro: 'Tu as un type métier propre, `Produit` — et voilà que la vie en demande des VARIANTES : le brouillon du formulaire de création (tout optionnel), la fiche publique du catalogue (quelques champs), la création sans id (il sera généré), la configuration figée. Trois stratégies : recopier le type à la main (divergence garantie à la première évolution), crisser de `?` partout dans l\'original (le contrat principal fond), ou utiliser les **utility types** : des génériques FOURNIS par le langage qui DÉRIVENT une forme d\'une autre — `Partial`, `Required`, `Readonly`, `Pick`, `Omit`. Une seule source de vérité, des variantes qui la suivent automatiquement : c\'est le DRY appliqué aux types.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : une forme, des variantes, zéro copie' },
            { t: 'p', h: 'Regarde ce qui se passe dans un vrai projet, semaine après semaine. Lundi : `Produit` est parfait. Mardi : le formulaire de saisie de la Boutique Awa a besoin d\'un brouillon où chaque champ PEUT manquer — tu recopies `ProduitBrouillon` avec des `?` partout. Mercredi : la fiche publique du catalogue n\'expose que nom et prix — tu recopies `ProduitPublic`. Jeudi : un collègue ajoute `promo` à `Produit`… et les trois copies, silencieusement, sont FAUSSES. Tu viens de payer l\'impôt le plus courant des bases de code : la duplication structurelle. Les utility types existent pour le supprimer : `Partial<Produit>`, `Pick<Produit, \"nom\" | \"prix\">` — le type original demeure la SOURCE, les variantes sont CALCULÉES. Une seule retouche, partout à jour : le compilateur est ton assistant de synchronisation.' },
            { t: 'h3', h: 'Partial et Required : tout optionnel, tout exigé' },
            { t: 'code', lang: 'ts', label: 'partial-required.ts', code:
'interface Produit {\n  id: string;\n  nom: string;\n  prix: number;\n  stock: number;\n  promo?: number;\n}\n\n// Partial : TOUS les champs deviennent optionnels — le brouillon parfait\ntype BrouillonProduit = Partial<Produit>;\nconst brouillon: BrouillonProduit = { nom: \"Gari premium\" };   // ✓ le reste viendra\n\n// Required : TOUT devient obligatoire, même ce qui était optionnel\nfunction finaliser(b: BrouillonProduit): Required<BrouillonProduit> {\n  // le compilateur EXIGE maintenant promo et cie :\n  return { id: doit(b.id), nom: doit(b.nom), prix: doit(b.prix),\n           stock: doit(b.stock), promo: b.promo ?? 0 };\n}' },
            { t: 'p', h: '`Partial<T>` et `Required<T>` sont des miroirs exacts : l\'un ajoute un `?` à chaque clé de T, l\'autre les retire — y compris celles qui étaient optionnelles à l\'origine (`promo` devient exigible dans le `Required`). Le couple se marie superbement avec le cycle de vie d\'une SAISIE : `Partial` pour la phase brouillon (le formulaire se remplit champ par champ, tout peut manquer), `Required` pour la phase consolidation (« à l\'enregistrement, EXIGEONS tout ») — et une fonction de transition qui transforme l\'un en l\'autre en justifiant chaque champ. Là encore, note l\'économie : ni le brouillon ni la forme consolidée n\'ont été DECLARÉS ; ils se déduisent de `Produit` et suivront toute évolution de la fiche maîtresse.' },
            { t: 'h3', h: 'Pick et Omit : l\'essentiel et son complément' },
            { t: 'code', lang: 'ts', label: 'pick-omit.ts', code:
'// Pick : garder SEULEMENT les clés nommées\ntype FichePublique = Pick<Produit, \"nom\" | \"prix\">;\nconst vitrine: FichePublique = { nom: \"Gari premium 50 kg\", prix: 21000 };\n// ni id, ni stock, ni promo n\'existent plus sur ce type\n\n// Omit : retirer les clés nommées\ntype NouveauProduit = Omit<Produit, \"id\">;\nfunction creer(saisie: NouveauProduit): Produit {\n  return { ...saisie, id: \"P-\" + Date.now() };   // l\'id, LUI, est fourni par le système\n}' },
            { t: 'p', h: '`Pick<T, K>` et `Omit<T, K>` travaillent sur les CLÉS (d\'où l\'importance de `keyof`, qui se cache dessous — fiche `ts-keyof-typeof`) : le premier construit la SOUS-FORME des clés choisies, le second la forme MOINS les clés bannies. Deux battements de cœur du quotidien. Pick pour l\'EXPOSITION : la vitrine publique ne montre que ce qui doit l\'être — si demain `prixFournisseur` rejoint `Produit`, la fiche publique ne le laissera jamais fuiter. Omit pour la CRÉATION : « tout sauf l\'id, que le système génère » — l\'utilisateur fournit le reste, l\'application complète ; si un champ requis est ajouté à `Produit`, toutes les fonctions `creer` du projet signaleront les saisies incomplètes automatiquement. C\'est ça, un type qui TRAVAILLE pour toi.' },
            { t: 'h3', h: 'Readonly : figer une forme à la lecture seule' },
            { t: 'code', lang: 'ts', label: 'readonly.ts', code:
'interface Config {\n  tva: number;\n  devise: string;\n  boutique: { nom: string; quartier: string };\n}\n\nconst CONFIG: Readonly<Config> = {\n  tva: 0.18,\n  devise: \"FCFA\",\n  boutique: { nom: \"Boutique Awa\", quartier: \"Dantokpa\" }\n};\n\nCONFIG.tva = 0.20;                // ✗ lecture seule\nCONFIG.boutique.nom = \"Autre\";   // ✓ HÉLAS accepté : le gel est SUPERFICIEL\n// (Readonly gèle les clés de surface, pas le contenu des objets imbriqués)' },
            { t: 'p', h: '`Readonly<T>` préfixe chaque propriété de `readonly` — la config devient inaltérable EN SURFACE. Et la dernière ligne de l\'exemple porte tout le savoir de la fiche : le gel est À UN ÉTAGE. `CONFIG.boutique.nom` traverse le verrou parce que `boutique` est un objet IMBRIQUÉ dont les propres clés ne sont pas gelées (tout comme le readonly de tableau de la fiche Interfaces). Le remède maison existe — un `DeepReadonly` récursif, faisable en quelques lignes avec les mapped types — mais la règle pratique de la maison est plus sage : `Readonly` pour figer la SURFACE (réassignation des clés), conventions et `as const` pour les valeurs profondes vraiment immuables, `Object.freeze` quand le gel doit tenir au runtime. Choisis ta profondeur en connaissance de cause.' },
            { t: 'h3', h: 'Combinaisons : le vrai pouvoir, c\'est l\'empilage' },
            { t: 'code', lang: 'ts', label: 'combinaisons.ts', code:
'// « Le brouillon PUBLIC du produit » : optionnel ET restreint\ntype BrouillonPublic = Partial<Pick<Produit, \"nom\" | \"prix\" | \"promo\">>;\n\n// « La saisie complète, sans id, figée une fois validée »\ntype SaisieValidee = Readonly<Omit<Produit, \"id\">>;\n\n// « Juste le stock, obligatoire »\ntype PointStock = Required<Pick<Produit, \"id\" | \"stock\">>;\n\nfunction ajusterStock(p: PointStock, delta: number): number {\n  return p.stock + delta;   // id ET stock garantis, rien d\'autre promis\n}' },
            { t: 'p', h: 'Comme les fonctions, les utility types se COMPOSENT : chaque couche prend la sortie de la précédente. Deux repères pour garder les idées claires. L\'ordre compte peu pour les opérations indépendantes (`Partial<Pick<T, K>>` équivalent à `Pick<Partial<T>, K>`), mais se lit mieux « de la forme d\'abord, des modificateurs ensuite ». Et au-delà de deux étages, nomme le résultat : `BrouillonPublic` dit l\'intention, quand `Required<Pick<Omit<...>>>` dit seulement la mécanique. Un alias parlant est à la composition de types ce que la fonction nommée est au pipeline de valeurs : l\'endroit où le SENS habite.' },
            { t: 'h3', h: 'Sous le capot : les mapped types' },
            { t: 'code', lang: 'ts', label: 'mapped-types.ts', code:
'// Partial, réécrit « à la main » — c\'est EXACTEMENT ça, sous le capot :\ntype MonPartial<T> = {\n  [K in keyof T]?: T[K];   // pour chaque clé K de T : optionnelle, même type\n};\n\ntype MonReadonly<T> = {\n  readonly [K in keyof T]: T[K];   // même boucle, autre modificateur\n};\n\ntype MonPick<T, K extends keyof T> = {\n  [P in K]: T[P];          // boucle restreinte aux seules clés demandées\n};' },
            { t: 'p', h: 'Il n\'y a pas de magie dans la bibliothèque standard : chaque utility type est un **mapped type** — une boucle `for` sur les clés, écrite en position type. `[K in keyof T]?: T[K]` se lit mot à mot : « pour chaque K appartenant aux clés de T, génère la propriété K, optionnelle, du type T[K] ». Tu reconnais deux notations croisées en chemin : `keyof T` (l\'union des clés) et `T[K]` (l\'accès indexé : « le type de la clé K dans T »). La fiche `ts-keyof-typeof` dépliera tout ça tranquillement ; le message d\'aujourd\'hui est l\'ESSENTIEL : ces cinq utilitaires ne sont pas des reliques du langage à vénérer, ce sont des patrons réécrivables — et un jour tu écriras les tiens pour ta boutique.' },
            { t: 'h3', h: 'Quel utilitaire pour quel besoin ?' },
            { t: 'table', head: ['Besoin', 'Utility', 'Attention'], rows: [
              ['Brouillon / mise à jour partielle', 'Partial`<T>`', 'superficiel : pas les objets imbriqués'],
              ['Consolider un brouillon', 'Required`<T>`', 'retire aussi les ? d\'origine'],
              ['Exposer un sous-ensemble public', 'Pick`<T, K>`', 'les clés sont vérifiables — les typos refusées'],
              ['Créer sans les champs système', 'Omit`<T, K>`', 'préfère-le à « Partial de tout » pour la création'],
              ['Figer une config', 'Readonly`<T>`', 'gel de surface ; as const pour les valeurs littérales'],
              ['Composer des variantes', 'empilages nommés', 'nomme l\'intention, pas la mécanique']
            ]},
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« Partial est profond »** — NON : seul l\'étage de surface devient optionnel. `Partial<{ adresse: { ville: string } }>` exige toujours `ville` entière dès qu\'`adresse` est fournie. Le gel profond comme l\'option profonde demandent des mapped types récursifs maison.',
              '**« Omit et Partial sont proches »** — ils répondent à des questions différentes : « retirer CES clés » (Omit) contre « rendre TOUT possiblement absent » (Partial). Pour une création sans id, c\'est Omit — Partial laisserait aussi passer des saisies sans nom ni prix, « pour aller vite » jusqu\'au crash.',
              '**« ça coûte au runtime »** — zéro : ce sont des calculs de TYPES, tout est effacé à l\'émission. Le JavaScript produit ne connaîtra jamais l\'existence de ces variantes.',
              '**« je recopie à la main, c\'est plus lisible »** — lisible le jour J, dangereux à chaque évolution : la copie ne suit plus l\'original. La dérivation (`Pick`, `Omit`…) fait du COMPILATEUR ton synchroniseur : retouche la source, les variantes signalent elles-mêmes leurs problèmes.',
              '**« les utility types s\'instancient comme des objets »** — ce sont des génériques toujours évalués en position TYPE : jamais de valeur, jamais de `new`, jamais d\'appel. `Partial<Produit>` EST un type, pas une recette à exécuter.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Tout se ressemble, et c\'est normal : `Partial`/`Readonly` jouent avec le `?` et le `readonly` rencontrés dans `ts-interfaces` — simplement appliqués EN BOUCLE à toutes les clés. `Pick` et `Omit` manient les clés comme le faisait `K extends keyof T` dans `ts-generics-contraintes` ; la syntaxe des mapped types fera l\'objet de `ts-keyof-typeof`. Le principe « une source, des dérivées » prolonge `ts-interface-vs-type` (composer plutôt que recopier). Et le voisin de palier, `ts-record-returntype`, continue la tournée du magasin : dictionnaires typés et extraction de types depuis des fonctions.' },
            { t: 'callout', kind: 'tip', h: 'Règle d\'achat pour ta boutique de types : chaque fois que deux types « se ressemblent à 80 % », demande-toi lequel est la SOURCE et lequel est la DÉRIVÉE. La source s\'écrit à la main ; la dérivée s\'obtient par Pick/Omit/Partial — et le jour où la source évolue, tu n\'as RIEN à faire : les erreurs (ou leur absence) font la synchronisation.' }
          ],
          errors: [
            { title: 'Croire que Partial est profond', lang: 'ts', bad:
'interface Fournisseur { nom: string; contact: { tel: string; quartier: string } }\nconst f: Partial<Fournisseur> = { contact: {} };\n// ✗ ce brouillon ne compile PAS : dès que contact est fourni,\n//   ses champs sont exigés — Partial n\'a agi qu\'en surface', good:
'// Deux options honnêtes :\n// 1) Partial à chaque étage qui doit l\'être :\ninterface Fournisseur2 { nom: string; contact: Partial<{ tel: string; quartier: string }> }\n// 2) un Partial profond maison (mapped type récursif, fiche keyof/manipulation).', why: 'Les utility types de base agissent à UN étage : les clés directes de T. Un objet imbriqué traverse le filtre intact — ses propres champs restent obligatoires. C\'est voulu (la profondeur récursive coûte en vérification et en surprises), mais ça doit être un choix conscient, pas une découverte au milieu d\'un refactor.' },
            { title: 'Recopier un type à la main au lieu de le dériver', lang: 'ts', bad:
'interface ProduitPublic {      // copie manuelle du « vrai » Produit\n  nom: string;\n  prix: number;\n}\n// dans un mois : Produit gagne « promo »… ProduitPublic, lui, reste vieux\n// et personne ne signale la divergence — elle devient la norme', good:
'type ProduitPublic = Pick<Produit, \"nom\" | \"prix\" | \"promo\">;\n// ProduitPublic suit Produit : toute évolution de la source est répercutée\n// ou signalée — le compilateur EST la synchronisation', why: 'Une forme recopiée duplique la CONNAISSANCE du domaine : deux endroits à maintenir, et rien ne les relie quand ils se séparent. La dérivation par utility types tisse le lien : la variantes est CALCULÉE depuis la source et ne peut pas se tromper silencieusement. DRY s\'applique aux types autant qu\'aux valeurs.' }
          ],
          related: ['ts-record-returntype', 'ts-keyof-typeof', 'ts-generics-contraintes', 'ts-interface-vs-type']
        },
        {
          id: 'ts-record-returntype',
          title: 'Record, ReturnType, Parameters & cie',
          icon: 'keyboard_return',
          level: 'Intermédiaire',
          tagline: 'Dictionnaires fermés, types extraits de fonctions existantes : la boîte à outils se remplit.',
          intro: 'Deuxième tiroir de la caisse à outils, deux familles. **`Record<K, V>`** type les dictionnaires — ces objets utilisés comme tables de correspondance (quartier → tarif, statut → label) — en imposant clés ET valeurs, avec la possibilité radicale de FERMER la liste des clés. **`ReturnType` et `Parameters`** extraient les types d\'une fonction EXISTANTE : au lieu de redéclarer « ce que renvoie chargerCatalogue », tu le déduis — et tu ne peux plus diverger. Avec `Awaited` et `NonNullable` en compléments, cette fiche parachève le kit que tu utiliseras littéralement chaque jour.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : les dictionnaires méritent des clés en règle' },
            { t: 'p', h: 'Un dictionnaire, tu en écris toute la journée sans le nommer : les tarifs du zémidjan par quartier, les labels français des statuts de commande, les adresses des dépôts. L\'annotation « honnête mais faible » est `{ [cle: string]: number }` — index signature déjà vue — qui déclare « n\'importe quelle clé, valeurs numériques ». Elle fonctionne… et elle AVALE tout : la faute de frappe `\"akpapka\"` y trouve sa place sans prévenir, le quartier manquant n\'est pas signalé, et la lecture `tarifs[\"x\"]` n\'est pas sûre. `Record` fait deux choses en une : écrire la signature d\'index en clair, et — c\'est le bouton d\'or — FERMER les clés sur une union : `Record<Zone, number>` exige EXACTEMENT les zones connues, ni plus, ni moins. Le dictionnaire devient une table de correspondance vérifiée.' },
            { t: 'h3', h: '`Record<K, V>` : le dictionnaire typé' },
            { t: 'code', lang: 'ts', label: 'record.ts', code:
'type Zone = \"centre\" | \"calavi\" | \"godomey\" | \"akpakpa\";\n\n// 1) Clés OUVERTES : équivalent de l\'index signature, en plus clair\nconst cache: Record<string, number> = {};\n\n// 2) Clés FERMÉES : la table de correspondance complète et contrôlée\nconst TARIFS: Record<Zone, number> = {\n  centre: 300,\n  calavi: 700,\n  godomey: 400,\n  akpakpa: 300\n  // oublie une zone → erreur ; ajoute \"dantokpa\" → erreur aussi\n};\n\n// 3) Les labels d\'une union : exhaustivité garantie, GRATUITE\nconst STATUT_LABEL: Record<StatutCommande, string> = {\n  en_attente: \"En attente de paiement\",\n  payee: \"Payée (MoMo confirmé)\",\n  livree: \"Livrée au client\"\n};' },
            { t: 'p', h: 'Le troisième usage est celui que ta future équipe te disputera : le couple `Record<Union, string>` pour les LABELS. Demain, `StatutCommande` gagne le statut `\"annulee\"` : TOUTES les tables de labels du projet deviennent des erreurs de compilation ciblées — « il manque annulee » — exactement comme `assertNever` sécurisait tes `switch`. Tu obtiens l\'exhaustivité coté DONNÉES : aucun statut sans libellé affichable dans l\'interface. Et la lecture est verrouillée dans l\'autre sens : `STATUT_LABEL[\"anulee\"]` (une seule consonne) est refusée à l\'écriture, fini l\'affichage vide du ticket client. Comparer à l\'objet libre de JavaScript, c\'est comparer un carnet de comptes tenu au crayon et un registre à pages numérotées.' },
            { t: 'h3', h: 'ReturnType et Parameters : extraire au lieu de redéclarer' },
            { t: 'code', lang: 'ts', label: 'extraction.ts', code:
'async function chargerCatalogue(page: number, filtre?: string): Promise<Produit[]> {\n  /* … */\n}\n\n// Redéclarer la signature à la main → divergence garantie.\n// Extraire, c\'est suivre AUTOMATIQUEMENT :\ntype Catalogue = Awaited<ReturnType<typeof chargerCatalogue>>;   // Produit[]\ntype Args = Parameters<typeof chargerCatalogue>;                 // [page: number, filtre?: string]\n\nfunction relancer(page: number, filtre?: string): Catalogue {\n  return [] as Catalogue;   // ou la logique de relance réelle\n}\n\n// Cas d\'école : typer un wrapper générique\ndeclare function mesure<F extends (...args: any[]) => any>(\n  fn: F, ...args: Parameters<F>\n): ReturnType<F>;' },
            { t: 'p', h: 'Le trio de cette famille est à retenir comme UNE formule. `typeof chargerCatalogue` récupère le TYPE de la fonction (le `typeof` de l\'espace des types — précisé dans `ts-keyof-typeof`). `ReturnType<…>` lui retire le type de RETOUR ; `Parameters<…>` extrait le TUPLE des paramètres, dans l\'ordre. Et comme la fonction retourne souvent une `Promise`, `Awaited<…>` la déballe une fois — d\'où la chaîne `Awaited<ReturnType<typeof f>>` qui donne le type FINAL rendu à l\'appelant. Le bénéfice n\'est pas la paresse, c\'est la FIABILITÉ : la signature de la fonction bouge ? Tous les extraits bougent avec elle — wrappers, relances, tests et mocks ne peuvent plus être « presque juste ». (Et regarde `mesure<F>` en bas de l\'exemple : c\'est exactement ainsi que les grands utilitaires eux-mêmes sont écrits.)' },
            { t: 'h3', h: 'NonNullable et les autres à connaître' },
            { t: 'code', lang: 'ts', label: 'complements.ts', code:
'type Recherche = Produit | null | undefined;\ntype Trouve = NonNullable<Recherche>;          // Produit — les vides retirés\n\n// La famille de la fiche précédente se complète :\ntype SansNull<T> = Exclude<T, null | undefined>;     // le mécanisme SOUS NonNullable\ntype SeulementTexte = Extract<string | number | Date, string>;   // \"string\"\n\n// Et deux petits utilitaires très employés :\ntype NomClasse = InstanceType<typeof Date>;          // Date — pour les constructeurs\ntype Ctor = ConstructorParameters<typeof Date>;      // [] | [value: string|number] | …' },
            { t: 'p', h: '`NonNullable<T>` retire `null` et `undefined` d\'une union — utile chaque fois qu\'un type « à trous » arrive d\'une API libérale et que TON code, lui, a déjà vérifié. Les deux autres méritent une mention stratégique : `Exclude` et `Extract` filtrent les unions (retirer / garder des membres) — ce sont les mêmes mécaniques que les conditional types de la fiche suivante ; et le couple `InstanceType`/`ConstructorParameters` fait pour les CLASSES ce que `ReturnType`/`Parameters` font pour les fonctions. Tu n\'as pas à mémoriser le catalogue complet : ce qui compte, c\'est le RÉFLEXE — « avant d\'écrire un type dérivé à la main, chercher si la bibliothèque standard le calcule déjà ». Neuf fois sur dix, la réponse est oui.' },
            { t: 'h3', h: 'Record ou Map ? Le tableau qui tranche' },
            { t: 'table', head: ['Critère', 'Record / objet dictionnaire', 'Map'], rows: [
              ['Clés', 'string / number / symbol (ou unions de littéraux)', 'TOUT type (objets compris)'],
              ['Exhaustivité des clés vérifiable', 'oui (`Record<Zone, V>`)', 'non'],
              ['Sérialisation JSON', 'directe', 'à convertir'],
              ['Itération ordonnée garantie', 'ordre d\'insertion (objets)', 'garanti par spécification'],
              ['Fréquence d\'ajout/suppression', 'plutôt stable', 'souple, optimisée'],
              ['À choisir quand…', 'tables de correspondance, labels, config, API JSON', 'caches dynamiques, clés riches, gros volumes']
            ]},
            { t: 'h3', h: 'Sous le capot : extraction = types calculés' },
            { t: 'p', h: 'Comment `ReturnType` lit-il la signature d\'une fonction sans l\'appeler ? Par les **conditional types + infer**, le duo vedette de la prochaine fiche : « si T est une fonction `(…args: infer A) => infer R`, alors réponds R ». Le `infer` est une variable de type que le compilateur RÉSOUT par pattern-matching — il regarde la forme de la fonction et DÉDUIT ce qu\'il doit remplir. Tu n\'as pas besoin de la mécanique pour UTILISER l\'outil, mais tu la reconnaîtras désormais dans la nature : toute la bibliothèque standard de TypeScript est écrite exactement comme ça — des questions en forme de types, résolues par correspondance. Et l\'autre sous-le-capot concerne `Record` : c\'est un mapped type sur l\'union des clés — même moteur que `Partial`, boucle différente. Tout se tient ; les fiches restantes ne font que t\'ouvrir le capot plus grand.' },
            { t: 'h3', h: 'Exemple : le back-office de la Boutique Awa, sans duplication' },
            { t: 'code', lang: 'ts', label: 'back-office.ts', code:
'type ZoneLivraison = \"dantokpa\" | \"calavi\" | \"godomey\";\n\n// 1) Table exhaustive : chaque zone a son tarif, prouvé à la compilation\nconst TARIFS_ZEM: Record<ZoneLivraison, number> = {\n  dantokpa: 300,\n  calavi: 700,\n  godomey: 400\n};\n\nfunction facturer(zone: ZoneLivraison, panier: number): number {\n  return panier + TARIFS_ZEM[zone];   // lecture sûre : la clé est forcément là\n}\n\n// 2) La NOTIFICATION dépend de l\'existant, jamais d\'une copie\ndeclare function envoyerAuClient(clientId: string, sujet: string, corps: string): Promise<boolean>;\n\ntype ArgsNotif = Parameters<typeof envoyerAuClient>;      // [string, string, string]\ntype ResultatNotif = Awaited<ReturnType<typeof envoyerAuClient>>;   // boolean\n\nasync function relancerFacture(...args: ArgsNotif): Promise<boolean> {\n  return envoyerAuClient(...args);   // wrapper aligné, pour toujours\n}' },
            { t: 'p', h: 'Les deux moitiés de la fiche s\'y rencontrent dans leur usage naturel : le `Record` à clés fermées rend la table de tarifs À JOUR PAR CONSTRUCTION (ajoute un quartier au type, le compilateur réclame son tarif dans chaque table), et l\'extraction aligne le wrapper sur l\'API sans copier sa signature — quand `envoyerAuClient` évoluera, `relancerFacture` évoluera dans la foulée, sans que tu touches à cette ligne. C\'est le même principe que la fiche précédente, appliqué aux fonctions : une SOURCE de vérité, des dérivées calculées, et la duplication reléguée au musée des mauvaises habitudes.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« Record avec des clés string, c\'est assez »** — c\'est l\'index signature déguisée : pratique pour un cache, aveugle pour une table métier. Dès que la liste des clés est CONNUE, ferme-la sur une union : la machine vérifiera complétude ET typos.',
              '**« ReturnType exécute la fonction »** — jamais : c\'est un calcul de TYPE, à la compilation uniquement. Aucune fonction n\'est appelée, aucun effet produit — le compilateur LIT la signature et en extrait le poste demandé.',
              '**« typeof ici est le typeof de JS »** — non : en position type, `typeof f` interroge le TYPE de la valeur `f` (« sa signature »), pas sa catégorie runtime (\"function\"). Même mot, deux espaces — `ts-keyof-typeof` tranche ce malentendu définitivement.',
              '**« Awaited n\'est qu\'un détail d\'async »** — c\'est LE moyen propre de déplier une Promise en position type ; et il déballe récursivement (`Promise<Promise<T>>` → T), là où un bidouillage manuel s\'arrêterait au premier étage.',
              '**« Record et Map font doublon »** — ils répondent à des formes de problèmes différentes : tables stables + clés connues + JSON → Record ; ajouts fréquents + clés objets + gros volumes → Map. Le tableau de cette fiche te tranche le cas en une ligne.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Tu prolonges la promesse de `ts-utility-types` — une source, des dérivées — en l\'appliquant aux fonctions et aux clés. Les unions fermées de `ts-unions` deviennent des listes de clés contrôlées ; l\'index signature croisée dans `ts-interfaces` reçoit son habillage propre ; et le `switch` exhaustif de `ts-discriminated-unions` trouve son frère côté données avec `Record<Union, string>`. La fiche suivante, `ts-keyof-typeof`, ouvre la mécanique que tous ces outils partagent — clés comme unions, accès indexés, `typeof` d\'espace type — avant que `ts-conditional-types` n\'explique `infer` et la logique d\'extraction.' },
            { t: 'callout', kind: 'tip', h: 'Heuristique d\'équipe : interdisez-vous de redéclarer à la main la signature d\'une fonction TIERS (ou d\'un collègue) pour wrapper, mocker ou relayer. `Parameters`/`ReturnType`/`Awaited` existent pour que le wrapper ne puisse pas mentir sur l\'enveloppe — une divergence oubliée de mock est un trou noir à bugs en tests.' }
          ],
          errors: [
            { title: 'Un Record à clés ouvertes pour une liste fermée', lang: 'ts', bad:
'const TARIFS: Record<string, number> = {\n  centre: 300,\n  calvai: 700,        // faute de frappe : PERSONNE ne voit rien\n};\nTARIFS[\"calavi\"];     // undefined au runtime — et la course est mal facturée', good:
'type Zone = \"centre\" | \"calavi\" | \"godomey\";\nconst TARIFS: Record<Zone, number> = {\n  centre: 300,\n  calavi: 700,        // \"calvai\" serait REFUSÉ ; une zone manquante aussi\n  godomey: 400\n};', why: 'Avec des clés `string`, le compilateur ne connaît pas la liste des clés ATTENDUES : ni typo ni oubli ne se signalent, et chaque lecture est potentiellement `undefined`. L\'union de littéraux + `Record` verrouille les deux sens : écriture contrôlée, lecture garantie.' },
            { title: 'Redéclarer la signature à la main', lang: 'ts', bad:
'// envoyerAuClient évolue dans api.ts, mais ICI on avait copié :\nfunction relancer(clientId: string, sujet: string) {           // corps oublié !\n  return envoyerAuClient(clientId, sujet, \"\");                // mock silencieusement faux\n}', good:
'type ArgsNotif = Parameters<typeof envoyerAuClient>;\nasync function relancerFacture(...args: ArgsNotif) {\n  return envoyerAuClient(...args);   // aligné par construction, pour toujours\n}', why: 'Une signature recopiée est une BOMBE : exacte le jour de la copie, fausse en silence dès que l\'originale évolue — et les wrappers/mocks sont les derniers fichiers qu\'on relit. L\'extraction ne peut pas diverger : elle est CALCULÉE depuis la source à chaque vérification.' }
          ],
          related: ['ts-utility-types', 'ts-keyof-typeof', 'ts-conditional-types', 'ts-generics']
        }
      ]
    },
  {
      id: 'manipulation-types',
      name: 'Manipulation de types',
      icon: 'transform',
      fiches: [
        {
          id: 'ts-keyof-typeof',
          title: 'keyof, typeof & indexed access',
          icon: 'key',
          level: 'Avancé',
          tagline: 'Les objets ont déjà les réponses : extrais les clés, les valeurs, les formes — sans rien recopier.',
          intro: 'Trois opérateurs de types, un même principe : **dériver les types de structures existantes** au lieu de les réécrire. `keyof T` produit l\'union des clés d\'un objet (`\"id\" | \"nom\" | \"prix\"`). `typeof valeur`, en position type, récupère le type d\'une VALEUR concrète — le pont entre le monde des valeurs et celui des types. Et l\'**accès indexé** `T[K]` lit le type d\'une clé donnée, comme `objet[cle]` lit sa valeur. Ensemble, ils alimentent tout ce que tu as déjà croisé (`Pick`, `Record`, `K extends keyof T`) — et un patron d\'architecture redoutable : l\'objet littéral comme SOURCE UNIQUE de vérité, pour ses labels ET ses types.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : la structure existe déjà, ne la réécris jamais' },
            { t: 'p', h: 'Fais le compte de ce que tu sais déjà dériver : `Partial<Produit>`, `Record<Zone, number>`, `Parameters<typeof f>` — tous PARTENT d\'une structure existante pour en calculer une autre. Cette fiche te donne les trois outils BRUTS dont ces utilitaires sont faits : l\'union des clés, le type d\'une valeur, la lecture d\'une clé. Le bénéfice n\'est pas académique : chaque type « recopié à la main » — la liste des clés d\'un objet, le type d\'un champ précis, la forme d\'une constante de config — est une future DIVERGENCE silencieuse. `keyof`, `typeof` et `T[K]` te garantissent que le type demandé restera aligné avec la structure source, pour toujours, gratuitement. C\'est le dernier étage de la fusée « une seule source de vérité ».' },
            { t: 'h3', h: 'keyof : l\'union des clés' },
            { t: 'code', lang: 'ts', label: 'keyof.ts', code:
'interface Produit {\n  id: string;\n  nom: string;\n  prix: number;\n  stock: number;\n}\n\ntype CleProduit = keyof Produit;   // \"id\" | \"nom\" | \"prix\" | \"stock\"\n\nfunction lireChamp(p: Produit, cle: keyof Produit) {\n  return p[cle];   // ✓ la clé est FORCÉMENT valide\n}\nlireChamp(gari, \"nom\");    // ✓\nlireChamp(gari, \"preix\");  // ✗ \"preix\" n\'est pas une clé de Produit\n\n// Sur un dictionnaire, keyof épouse la signature d\'index :\ntype CleTarifs = keyof Record<string, number>;   // string | number' },
            { t: 'p', h: '`keyof T` ne regarde pas les VALEURS : il extrait la LISTE DES CLÉS comme une union de littéraux (et sur un dictionnaire à signature `string`, il produit `string | number` — parce qu\'en JavaScript, les clés numériques sont converties en texte). Deux usages dominent. Contrôler un ARGUMENT : `cle: keyof Produit` interdit la faute de frappe à l\'appel — le même réflexe que `K extends keyof T` de la fiche Contraintes. Composer des types : `keyof` est le carburant des mapped types (`[K in keyof T]`) qui font fonctionner `Partial` et `Record`. À retenir durablement : les clés d\'un objet forment elles-mêmes un TYPE, aussi manipulable que n\'importe quel autre — une union qu\'on peut filtrer, exclure, parcourir.' },
            { t: 'h3', h: 'typeof : le pont entre valeurs et types' },
            { t: 'code', lang: 'ts', label: 'typeof-type.ts', code:
'// En POSITION TYPE, typeof lit le type d\'une VALEUR existante :\nconst CONFIG = { tva: 0.18, devise: \"FCFA\", boutique: \"Awa\" } as const;\n\ntype Config = typeof CONFIG;\n// { readonly tva: 0.18; readonly devise: \"FCFA\"; readonly boutique: \"Awa\" }\n\nfunction appliquer(c: Config) { /* c.tva est littéralement 0.18 */ }\n\n// Le malentendu classique, tranché :\nconst t1 = typeof CONFIG;      // position VALEUR → \"object\" (le typeof de JS)\ntype T2 = typeof CONFIG;      // position TYPE → la forme complète de l\'objet' },
            { t: 'p', h: 'Le même mot vit dans DEUX ESPACES : devant une expression (code qui tourne), c\'est l\'opérateur JavaScript qui répond `\"object\"`, `\"string\"`… ; en position de TYPE (annotation, alias, générique), c\'est l\'opérateur TypeScript qui répond « le type de cette valeur ». La deuxième ligne de l\'exemple montre la combinaison mortelle : `as const` fige chaque valeur en littéral (`tva` vaut EXACTEMENT `0.18` pour le compilateur), et `typeof CONFIG` capture le tout — le TYPE suit la constante, sans qu\'aucune interface ait été écrite. C\'est la technique reine pour les configurations, tables de correspondance et constantes métier : le code EST la documentation, le type est sa photo.' },
            { t: 'h3', h: 'L\'accès indexé `T[K]` : lire le type d\'une clé' },
            { t: 'code', lang: 'ts', label: 'acces-indexe.ts', code:
'interface Produit {\n  id: string;\n  nom: string;\n  prix: number;\n  images: string[];\n}\n\ntype PrixProduit = Produit[\"prix\"];           // number — lit la clé, point\ntype ImageProduit = Produit[\"images\"][number]; // string — [number] = l\'élément du tableau\ntype ChampsTextuels = Produit[\"id\" | \"nom\"];  // string — une union de clés s\'additionne\n\ntype TouteValeur = Produit[keyof Produit];\n// string | number | string[] — l\'union de TOUTES les valeurs possibles' },
            { t: 'p', h: 'L\'analogie avec JavaScript est exacte : `objet[cle]` lit la VALEUR d\'une clé à l\'exécution ; `Type[cle]` lit le TYPE d\'une clé à la compilation. Trois réflexes de lecture à prendre. Une clé directe : `Produit[\"prix\"]` — précis, suivi dans les refactors (renomme `prix`, cette ligne signalera). Un NOMBRE pour les tableaux : `T[number]` donne le type de l\'élément — inestimable pour extraire \"ce qu\'il y a DANS le tableau\" sans connaître la liste. Unions en entrée : accéder par une union (`[\"id\" | \"nom\"]`) produit l\'union des types de valeur — et la forme extrême `T[keyof T]` donne « toutes les valeurs possibles de T », dont tu verras l\'usage juste après, dans le patron à source unique.' },
            { t: 'h3', h: 'Le patron « objet source de vérité »' },
            { t: 'code', lang: 'ts', label: 'source-unique.ts', code:
'// UN objet littéral concentre : les clés = les statuts, les valeurs = les labels\nconst STATUT_LABEL = {\n  en_attente: \"En attente de paiement\",\n  payee: \"Payée (MoMo confirmé)\",\n  en_retard: \"En retard\",\n  livree: \"Livrée au client\"\n} as const;\n\n// TOUT dérive de l\'objet — aucun type n\'est écrit à la main :\ntype Statut = keyof typeof STATUT_LABEL;            // \"en_attente\" | \"payee\" | …\ntype Label = (typeof STATUT_LABEL)[keyof typeof STATUT_LABEL];\n// \"En attente de paiement\" | \"Payée (MoMo confirmé)\" | …  — les LITTÉRAUX exacts\n\nfunction afficher(statut: Statut): Label {\n  return STATUT_LABEL[statut];   // ✓ clé ET valeur dérivent du même objet\n}\n\n// Ajoute un statut DANS L\'OBJET : le type Statut, la fonction afficher,\n// et le Record<Statut, …> de tous les écrans suivent — ou protestent.' },
            { t: 'p', h: 'Étudie cette construction, elle vaut la fiche à elle seule. L\'objet `STATUT_LABEL` est UNE réalité exécutable (les labels existent au runtime, prêts à afficher) ; `keyof typeof` en extrait le type des STATUTS ; l\'accès indexé en extrait le type des LABELS — et ces deux types, obtenus sans écrire la moindre union à la main, ne peuvent JAMAIS diverger de la donnée. Ajoute `\"annulee\": \"Annulée\"` dans l\'objet : `Statut` grandit automatiquement, les `switch` avec `assertNever` réclament leur branche nouvelle, les tables locales se mettent à jour. C\'est l\'aboutissement de toute la philosophie TypeScript : la DONNÉE comme source unique, le type comme projection calculée, la maintenance comme conséquence. Le trio se lit à voix haute : `keyof typeof X` = « les clés de l\'objet X », `(typeof X)[keyof typeof X]` = « les valeurs de l\'objet X ».' },
            { t: 'h3', h: 'Les trois outils en regard' },
            { t: 'table', head: ['Opérateur', 'Répond à', 'Exemple'], rows: [
              ['keyof T', '« quelles sont les clés ? »', 'keyof Produit → \"id\" | \"nom\" | …'],
              ['typeof x (position type)', '« quel est le type de cette valeur ? »', 'typeof CONFIG → forme de l\'objet'],
              ['T[K]', '« quel type porte cette clé ? »', 'Produit[\"prix\"] → number'],
              ['T[keyof T]', '« quelles valeurs possibles ? »', 'Produit[keyof Produit] → union des champs'],
              ['A[number]', '« que contient ce tableau ? »', 'string[][number] → string']
            ]},
            { t: 'h3', h: 'Sous le capot : les deux espaces, et comment ils communiquent' },
            { t: 'p', h: 'TypeScript maintient DEUX univers parallèles : l\'espace des VALEURS (ce qui tourne : variables, fonctions, classes comme constructeurs) et l\'espace des TYPES (ce qui vérifie : interfaces, alias, et aussi les classes comme formes). Certains noms habitent les deux — une classe est à la fois une valeur (le constructeur) et un type (la forme des instances) ; un `interface` n\'habite que les types ; une `const` n\'habite que les valeurs. `typeof` est LA PASSERELLE officielle : appliqué en position type, il amène un nom de l\'espace des valeurs vers l\'espace des types (`Date` est une valeur ; `typeof Date` est « la signature du constructeur », quand `InstanceType<typeof Date>` en extrait la forme des instances). `keyof` et l\'accès indexé, eux, restent dans l\'espace des types — ils ne fabriquent et ne coûtent rien au runtime. Visualiser ces deux espaces, c\'est cesser de « subir » les erreurs et commencer à les LIRE : « X refers to a value, but is being used as a type » est précisément le message du pont.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« keyof donne les valeurs »** — non : les CLÉS, en union de littéraux. Les valeurs s\'obtiennent par accès indexé : `T[keyof T]`. Clés et valeurs sont deux projections distinctes du même objet.',
              '**« typeof x est le typeof de JavaScript »** — que EN POSITION VALEUR. En position type, c\'est une extraction de forme (delta complet). L\'erreur d\'aiguillage entre les deux espaces est LE blocage classique de cette fiche.',
              '**« Object.keys renvoie (keyof T)[] »** — non : pour le compilateur, c\'est `string[]` — la réalité JS est plus riche que la forme déclarée (un objet peut TRAÎNER des clés supplémentaires). Pour itérer en sécurité sur des clés connues, entre par un `Record<K, V>` ou un cast explicite et assumé.',
              '**« T[K] avec K inconnu marche »** — l\'accès indexé exige que K soit une clé de T (ou une union de clés) : `Produit[\"toto\"]` est une erreur de TYPE, immédiatement — ce qui est précisément la protection recherchée.',
              '**« as const avale les performances »** — const assertions sont des types : effacés à l\'émission, zéro coût. Le seul « prix » est au moment de la vérification, et il est minuscule face à la précision achetée.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Tu as déjà EMPRUNTÉ ces outils en sentinelle : `K extends keyof T` dans `ts-generics-contraintes`, `keyof` sous les mapped types de `ts-utility-types`, `typeof` dans `Awaited<ReturnType<typeof f>>` de `ts-record-returntype`. Le pattern à source unique noue aussi `as const` (fiche `ts-inference`) et l\'exhaustivité (`ts-discriminated-unions`). L\'étage au-dessus arrive tout de suite : `ts-conditional-types` t\'apprendra à choisir des types par TEST — la logique qui, combinée à `infer`, a littéralement écrit les utilitaires que tu utilises déjà.' },
            { t: 'callout', kind: 'tip', h: 'Prochaine fois qu\'un type et une donnée « doivent rester synchronisés » (statuts/labels, permissions/descriptions, codes/postes) : écris la DONNÉE seule, en objet `as const` — puis dérive le type avec `keyof typeof`. La synchronisation devient structurelle ; la revue de code, plus courte ; les oublis, impossibles.' }
          ],
          errors: [
            { title: 'Confondre typeof valeur et typeof type', lang: 'ts', bad:
'function saluer(c: typeof CONFIG) {}\n// idée juste, mauvaise surprise si CONFIG est en fait dans un AUTRE espace :\ntype X = typeof Produit;   // ✗ « Produit » est une interface : espace des TYPES\n// typeof n\'amène que des VALEURS vers les types, jamais l\'inverse', good:
'type Config = typeof CONFIG;     // ✓ CONFIG est une valeur (const) : bridge ok\ntype P = Produit;                // ✓ Produit est déjà un type : inutile de typeof', why: '`typeof` (position type) prend une VALEUR et rend son TYPE. Sur un nom qui habite déjà l\'espace des types (interface, alias), il n\'y a rien à « amener » — l\'opérateur bute. La question à te poser est spatiale : « ce nom vit-il du côté valeurs (const, let, function, classe) ou du côté types ? ».' },
            { title: 'Attendre de Object.keys des clés sûres', lang: 'ts', bad:
'const tarifs = { centre: 300, calavi: 700 } as const;\nfor (const z of Object.keys(tarifs)) {\n  tarifs[z];   // ✗ z est string, pas \"centre\" | \"calavi\" : indexation refusée\n}', good:
'// Option 1 — l\'objet porte des clés FERMÉES, on itère un tableau typé :\nfor (const z of [\"centre\", \"calavi\"] as const) tarifs[z];\n// Option 2 — cast explicite et assumé :\nfor (const z of Object.keys(tarifs) as (keyof typeof tarifs)[]) tarifs[z];', why: 'Le contrat de TypeScript est prudent : un objet peut au runtime posséder des clés EN PLUS de sa forme déclarée (typage structurel !), donc `Object.keys` ne promet que `string[]`. Itérer sur les clés CONNUES exige soit un tableau littéral de clés, soit un cast assumé — jamais la confiance aveugle.' }
          ],
          related: ['ts-conditional-types', 'ts-utility-types', 'ts-generics-contraintes', 'ts-record-returntype']
        },
        {
          id: 'ts-conditional-types',
          title: 'Conditional types & infer',
          icon: 'fork_right',
          level: 'Avancé',
          tagline: 'Poser des questions sur les types : « si T est un tableau, réponds son élément » — et résoudre avec infer.',
          intro: 'Tout ce que tu as déjà fait choisissait des types par NOM (`Pick<T, \"prix\">`) ou par structure (`keyof`, `T[K]`). Les **conditional types** choisissent par TEST : `T extends U ? X : Y` — « si T est assignable à U, réponds X, sinon Y ». Ajoute `infer`, la variable de type que le compilateur résout par correspondance de motifs, et tu obtiens une véritable logique de programmation côté types : extraire l\'élément d\'un tableau, déballer une Promise, filtrer une union. C\'est la mécanique qui écrit `ReturnType`, `Exclude`, `Awaited` — la fiche où tu cesses de consommer les utilitaires et commences à les comprendre, voire à en écrire.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : les types aussi méritent un « si »' },
            { t: 'p', h: 'Imagine la question : « cette fonction de recherche accepte soit un `Produit`, soit une `Promise<Produit>` — quel est le type RÉELLEMENT obtenu par l\'appelant ? » Aucun opérateur vu jusqu\'ici ne répond : il faut un TEST sur la forme (« est-ce une Promise ? ») suivi d\'une EXTRACTION (« alors prends ce qu\'elle contient »). Les conditional types sont exactement ces deux mouvements : un `extends` en guise de question (« T est-il assignable à … ? »), deux réponses possibles, et `infer` pour attraper au passage l\'inconnue cherchée. Pourquoi y consacrer une fiche ? Parce que c\'est la frontière entre « utiliser TypeScript » et « programmer DANS TypeScript » : les librairies que tu installes, les utilitaires standard, les types magiques de ton framework — tous parlent ce langage. Lire un rapport de bug, contribuer à une API typée, écrire LE type qui rend ton outil agréable : tout commence ici.' },
            { t: 'h3', h: 'La syntaxe : un ternaire aux sommets' },
            { t: 'code', lang: 'ts', label: 'conditionnel.ts', code:
'type EstTexte<T> = T extends string ? \"oui\" : \"non\";\n\ntype A = EstTexte<string>;    // \"oui\"\ntype B = EstTexte<42>;        // \"non\"\n\n// Le extends, ici, est un TEST D\'ASSIGNABILITÉ, pas un héritage :\n// « T peut-il être mis dans un string ? »\n\n// Une vraie machine : déballer une Promise\ntype Unwrap<T> = T extends Promise<infer R> ? R : T;\n\ntype C = Unwrap<Promise<Produit[]>>;   // Produit[]\ntype D = Unwrap<Produit[]>;            // Produit[] — pas une Promise : on rend T tel quel' },
            { t: 'p', h: 'Lecture en trois battements. 1) `T extends U ? X : Y` ressemble au ternaire JavaScript parce que la LOGIQUE est la même — question, deux branches — mais la question porte sur les TYPES : « T est-il assignable à U ? ». 2) `infer R` introduit une variable muette que le compilateur RÉSOUT par correspondance : `T extends Promise<infer R>` se lit « si T a la forme `Promise<quelque-chose>`, appelle ce quelque-chose R et sers-le ». 3) Le `else` est souvent l\'identité (`T`) ou le vide (`never`) — choisis selon que l\'outil doit être TOLÉRANT (« rends tel quel si pas concerné ») ou STRICT (« rien du tout si pas conforme »). Tout est toujours évalué à la COMPILATION : zéro test au runtime, zéro code émis.' },
            { t: 'h3', h: 'Déballer proprement : infer, le détective' },
            { t: 'code', lang: 'ts', label: 'infer.ts', code:
'// L\'élément d\'un tableau\ntype ElementDe<T> = T extends (infer E)[] ? E : never;\n\ntype E1 = ElementDe<Produit[]>;   // Produit\ntype E2 = ElementDe<string[]>;    // string\ntype E3 = ElementDe<number>;      // never — pas un tableau du tout\n\n// Le type de retour d\'une fonction MAISON (comme ReturnType)\ntype RetourDe<F> = F extends (...args: any[]) => infer R ? R : never;\ntype R1 = RetourDe<typeof chargerCatalogue>;   // Promise<Produit[]>\n\n// infer peut apparaître PLUSIEURS fois :\ntype PremierEtDernier<T> = T extends [infer P, ...any[], infer D] ? [P, D] : never;\ntype PD = PremierEtDernier<[string, number, boolean]>;   // [string, boolean]' },
            { t: 'p', h: '`infer` transforme le test en extraction : la partie de la forme que tu marques `infer X` devient DISPONIBLE dans la branche positive, comme un paramètre capturé. Les positions possibles sont précises : à l\'intérieur d\'une structure attendue (générique instancié `Promise<infer R>`, tableau `(infer E)[]`, tuple avec variadique `[infer P, ...any[], infer D]`, signature de fonction). Et la deuxième ligne du deuxième exemple dévoile le PÈRE de tous les utilitaires : ce `RetourDe<F>` maison, c\'est MOT POUR MOT le `ReturnType` de la bibliothèque standard. Quand tu lis `lib.d.ts` — les définitions officielles — tu reconnais maintenant chaque ligne : rien n\'est magique, tout est test d\'assignabilité + extraction.' },
            { t: 'h3', h: 'La distributivité : le piège à connaître absolument' },
            { t: 'code', lang: 'ts', label: 'distributivite.ts', code:
'type ExclureTexte<T> = T extends string ? never : T;\n\n// Surprise (la BONNE) : l\'union est DISTRIBUÉE membre par membre\ntype X = ExclureTexte<string | number | Date>;\n// = (string extends string ? never : string)\n// | (number extends string ? never : number)\n// | (Date    extends string ? never : Date)\n// = never | number | Date  =  number | Date\n\n// Pour tester l\'union EN BLOC, emballe dans un tuple :\ntype EstTexteGlobal<T> = [T] extends [string] ? \"oui\" : \"non\";\ntype Y = EstTexteGlobal<string | number>;   // \"non\" — l\'union entière n\'est pas un string' },
            { t: 'p', h: 'Voici LA règle de la fiche, celle qui explique 90 % des comportements « incompréhensibles » : quand le paramètre testé est NU (un simple `T`, pas emballé) et que T est une UNION, le conditional se DISTRIBUE — la question est posée à chaque membre SÉPARÉMENT, et les réponses forment une nouvelle union. C\'est exactement ce qui fait fonctionner `Exclude<T, U>` : « pour chaque T, s\'il est assignable à U, rayer ; sinon garder ». Et c\'est aussi ce qui piège l\'inattentif : pour tester l\'union ENTIÈRE d\'un bloc, il faut l\'EMBALLER — `[T] extends [U]` — ce qui désactive la distribution. Réflexe à prendre : en écrivant un conditional sur un paramètre libre, demande-toi « membre par membre, ou d\'un bloc ? » — et emballe dès que c\'est d\'un bloc.' },
            { t: 'h3', h: 'Recréer les classiques : la bibliothèque standard à mains nues' },
            { t: 'code', lang: 'ts', label: 'classiques-maison.ts', code:
'// Exclude : rayer les membres assignables à U (distributivité à l\'œuvre)\ntype MonExclude<T, U> = T extends U ? never : T;\ntype StatutRestant = MonExclude<\"attente\" | \"payee\" | \"annulee\", \"annulee\">;\n\n// NonNullable : exclure les vides\ntype MonNonNullable<T> = MonExclude<T, null | undefined>;\n\n// Extract : garder SEULEMENT les assignables à U\ntype MonExtract<T, U> = T extends U ? T : never;\n\n// Awaited : déballer (récursivement dans la vraie implémentation)\ntype MonAwaited<T> = T extends Promise<infer R> ? MonAwaited<R> : T;\ntype Pret = MonAwaited<Promise<Promise<Produit>>>;   // Produit — déballe jusqu\'au bout' },
            { t: 'p', h: 'Remarque ce qui vient d\'arriver : tu as RÉÉCRIT quatre utilitaires « intouchables » de la bibliothèque standard, en dix lignes. C\'est la démonstration de la fiche : ces outils ne sont pas des privilèges du langage, ils sont ÉCRITS en TypeScript-de-types, et le tien vaut le leur. Deux enseignements pratiques. La récursion fonctionne (`MonAwaited` se rappelle tant que la valeur est une Promise — le type « pense » en boucles comme le code). Et `never` garde son comportement d\'ensemble vide : absorbé dans les unions, annulé en test nu (`MonExclude<never, X>` vaut `never` — le cas sans valeur n\'a pas de réponse à donner, ce qui explique quelques disparitions mystérieuses de types en production).' },
            { t: 'h3', h: 'Exemple : parser une réponse d\'API, proprement' },
            { t: 'code', lang: 'ts', label: 'parser-api.ts', code:
'interface ApiResponse<T> { ok: boolean; data: T }\n\n// « Toute donnée encapsulée dans ApiResponse devient la donnée nue »\ntype DataDe<T> = T extends ApiResponse<infer D> ? D : T;\n\ntype Brute = DataDe<ApiResponse<Produit[]>>;   // Produit[]\ntype Directe = DataDe<Produit[]>;              // Produit[] — tolérant\n\n// Chaîne complète : d\'une fonction au contenu final de sa réponse\ntype Final<F> = F extends (...args: any[]) => Promise<infer R>\n  ? DataDe<R>\n  : never;\n\ntype Contenu = Final<typeof chargerCatalogue>;   // Produit[] — à JOUR pour toujours' },
            { t: 'p', h: 'Le dernier type mérite une minute de silence : `Final<F>` questionne la signature (« retournes-tu une Promise ? »), extrait ce qu\'elle porte, et re-déballe l\'enveloppe d\'API — le tout SANS toucher à `chargerCatalogue`, dont le simple nom suffit. Si la fonction change demain (nouvelle enveloppe, type de page, filtre), `Contenu` se recalculera seul. C\'est la quintessence de la programmation de types : des DÉRIVÉES qui ne peuvent pas vieillir, parce qu\'elles ne sont jamais écrites — seulement calculées. À doser avec goût : au-delà de deux ou trois étages de conditionnels, le rapport d\'erreur devient cryptique pour l\'équipe ; nomme chaque étage, teste chaque étage avec une ligne `type Test = Machine<Connue> // doit valoir Attendu`.' },
            { t: 'h3', h: 'Sous le capot : une évaluation paresseuse, à la demande' },
            { t: 'p', h: 'Comment le vérificateur traite-t-il ces machines ? PARESSEUSEMENT : un conditional type n\'est résolu que quand ses paramètres sont CONNUS — tant que `T` est un paramètre libre (dans une fonction générique), la question reste en suspens, « différée », et le compilateur vérifie les COHÉRENCES (les branches renvoient-elles des formes compatibles avec le contexte ?) plutôt que la décision. À l\'appel, quand `T` se concrétise, le rideau tombe : substitution, distribution éventuelle, résolution des `infer`, et le type final apparaît. C\'est pourquoi l\'éditeur affiche parfois `Unwrap<Promise<X>>` non évalué dans un fichier générique puis « plie » la formule dès l\'appel : ce n\'est pas un bug d\'affichage, c\'est la paresse qui travaille. Et comme toujours : TOUT disparaît à l\'émission — ces machines coûtent de la réflexion au vérificateur, jamais un octet au runtime.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« extends ici, c\'est l\'héritage »** — non : c\'est un TEST D\'ASSIGNABILITÉ (« peut-être mis dans… »). `42 extends number ? X : Y` répond X parce qu\'un littéral numérique rentre dans number — aucune généalogie de classes n\'intervient.',
              '**« le conditional s\'exécute au runtime »** — jamais : évalué à la compilation, effacé à l\'émission. Aucun test réel ne se produit dans le programme livré ; toute la logique meurt avec les types.',
              '**« infer marche n\'importe où »** — non : uniquement dans la BRANCHE CONDITION d\'un extends, aux positions de capture (intérieur d\'un générique, d\'un tuple, d\'une signature). Hors de là, la syntaxe est invalide.',
              '**« la distributivité est un bug »** — c\'est un CHOIX de conception, et le bon : elle fait fonctionner `Exclude`/`Extract` naturellement. Le contre-coup — tester une union d\'un bloc — se paie d\'une paire de crochets : `[T] extends [U]`.',
              '**« il faut des conditional types partout »** — non : l\'usage sain est d\'écrire les OUTILS dont ton projet a vraiment besoin (DataDe, Final…), nommés et testés. Le conditional est un marteau de précision : au-delà de deux niveaux d\'imbrication, demande-toi si une union discriminée ou un overload ne dirait pas la même chose plus lisiblement.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Cette fiche est le capot GRAND OUVERT des deux précédentes : `ReturnType`, `Awaited`, `Exclude`/`Extract` (`ts-record-returntype`) et tous les mapped types (`ts-utility-types`) s\'écrivent exactement ainsi — test d\'assignabilité, distribution sur les unions, capture par `infer`. Le « T extends » des contraintes de `ts-generics-contraintes` y retrouve son frère jumeau : même mot, rôle différent (borne contre question). Tu touches ici le plafond du système de types « courant » ; au-delà, il n\'y a plus que de l\'ingénierie — templates literals types, recursion contrôlée — dont la substance reste celle-ci.' },
            { t: 'callout', kind: 'tip', h: 'Méthode de pro pour écrire un conditional sans souffrir : 1) écris DEUX exemples résolus à la main (entrée connue → sortie attendue) ; 2) écris la formule ; 3) valide avec une ligne-test `type Verif = MaMachine<Connue>` sans quitter l\'éditeur. Tester les types comme tu testes le code — c\'est le même sport, un étage plus haut.' }
          ],
          errors: [
            { title: 'Oublier la distributivité sur les unions', lang: 'ts', bad:
'type EstTexte<T> = T extends string ? T : never;\ntype R = EstTexte<string | number | Date>;\n// Surprise : R = string — la condition a été posée MEMBRE PAR MEMBRE,\n// et number/Date sont devenus never (absorbés par l\'union)', good:
'// Tester l\'union D\'UN BLOC : emballer dans un tuple\ntype EstTexteBloc<T> = [T] extends [string] ? T : never;\ntype R = EstTexteBloc<string | number>;   // never — l\'union entière n\'est pas un string', why: 'Un paramètre nu (`T extends …`) DISTRIBUE le test à chaque membre de l\'union — chacun répond pour soi, les réponses s\'unissent. C\'est le moteur de Exclude/Extract, mais un piège si tu voulais tester l\'union entière. Crochets autour de T et de U : la distribution s\'éteint, l\'union est jugée en bloc.' },
            { title: 'Croire que le conditional s\'exécute', lang: 'ts', bad:
'function lire<T>(x: T extends Promise<infer R> ? R : T) {\n  if (x instanceof Promise) {          // ✗ \'Promise\' ici vise encore… un souhait\n    return x;                          // le type calculé n\'aide pas à l\'EXÉCUTION\n  }\n}', good:
'// Les types décident à la COMPILE ; au runtime, teste de vraies valeurs :\nasync function lire<T>(x: T | Promise<T>): Promise<T> {\n  return x instanceof Promise ? x : x;   // ou plus simplement : return await x;\n}', why: 'Conditional types et infer vivent dans l\'espace des TYPES : ils disparaissent avant l\'exécution. À l\'intérieur d\'une fonction, seuls les tests d\'EXÉCUTION (`instanceof`, `typeof`, gardes maison) séparent les cas ; les conditional types servent à CALCULER la signature qui promet le bon résultat — jamais à le produire.' }
          ],
          related: ['ts-keyof-typeof', 'ts-generics-contraintes', 'ts-utility-types', 'ts-record-returntype']
        }
      ]
    },
  {
      id: 'modules',
      name: 'Modules & déclarations',
      icon: 'widgets',
      fiches: [
        {
          id: 'ts-modules',
          title: 'import/export & import type',
          icon: 'widgets',
          level: 'Débutant',
          tagline: 'Même mécanique qu\'en JS, une question en plus : « cette importation tourne-t-elle au runtime, ou ne sert-elle qu\'aux types ? »',
          intro: 'Tu connais déjà `import`/`export` du module JavaScript : chaque fichier est un module, tout y est privé sauf ce qu\'il exporte. TypeScript ne change RIEN à cette mécanique d\'exécution — mais ajoute une couche que JS n\'a pas : **les types aussi voyagent entre fichiers** (interfaces, alias, unions). Et comme les types s\'effacent à la compilation, une question nouvelle apparaît à chaque import : faut-il garder cette importation dans le JavaScript émis, ou l\'effacer avec lui ? Le mot-clé `type` des imports — `import type`, `import { type X }` — est la réponse, et cette fiche te rendra son usage naturel, avec les deux pièges d\'écosystème qui bloquent 9 débutants sur 10 : `verbatimModuleSyntax` et les extensions `.js` de Node.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : chaque fichier est un module, les types aussi' },
            { t: 'p', h: 'Dès que la Boutique Awa dépasse un fichier — très vite, donc — la question devient : « où vivent les contrats ? » La réponse de la maison tient en UN fichier : `types.ts`, qui exporte `Produit`, `Commande`, `MoyenPaiement` — le vocabulaire du métier — pendant que `catalogue.ts`, `commandes.ts`, `paiements.ts` l\'importent chacun de leur côté. Jusque-là, pur JavaScript de modules. La nouveauté TypeScript : `types.ts` ne contient peut-être AUCUNE valeur exécutable — que des interfaces et alias. Quand tu l\'importes, le compilateur doit donc décider Ce qu\'il en fait à l\'émission : un import de types peut être EFFACÉ (et c\'est en général ce que tu veux — rien à charger !), un import de valeurs doit rester (il y a du code à exécuter). Dire la différence explicitement, c\'est tout le job de `import type`.' },
            { t: 'h3', h: 'La base : export nommé, export par défaut — en TS comme en JS' },
            { t: 'code', lang: 'ts', label: 'src/types.ts et src/catalogue.ts', code:
'// types.ts — le vocabulaire partagé de la boutique\nexport interface Produit { id: string; nom: string; prix: number; }\nexport type MoyenPaiement = \"momo\" | \"moov\" | \"especes\";\nexport type Zone = \"centre\" | \"calavi\" | \"godomey\";\n\n// catalogue.ts — valeurs + contrats importés\nimport type { Produit, Zone } from \"./types\";\nimport { TARIFS_ZEM } from \"./tarifs\";   // une VALEUR : le tableau des prix\n\nexport function livraison(p: Produit, zone: Zone): number {\n  return p.prix + TARIFS_ZEM[zone];\n}' },
            { t: 'p', h: 'Trois précisions avant d\'aller plus loin. Les EXPORTS NOMMÉS (`export interface Produit`) restent la recommandation générale — renommage suivi par l\'éditeur, imports explicites, arbres plus faciles à élaguer (« tree-shaking ») ; l\'export par défaut se réserve à « ce fichier EST cette chose » (rare en bibliothèque de code métier). Les types s\'exportent EXACTEMENT comme les valeurs : `export interface`, `export type` — et les deux peuvent cohabiter tranquillement dans un même fichier (rire des grands : `types.ts` finit souvent par exporter une ou deux constantes, et tout le monde s\'en porte mieux). Et tu remarques la syntaxe un peu spéciale à l\'import : `import type { Produit }` — c\'est le prochain chapitre.' },
            { t: 'h3', h: 'import type : dire « efface-moi ça à la compilation »' },
            { t: 'code', lang: 'ts', label: 'trois-ecritures.ts', code:
'// 1) Import de TYPES pur : effacé à 100 % à l\'émission\nimport type { Produit, MoyenPaiement } from \"./types\";\n\n// 2) Import de VALEURS : conservé dans le JS émis (il y a du code derrière)\nimport { TARIFS_ZEM, livraison } from \"./catalogue\";\n\n// 3) Mixte, marqué élément par élément — le réflexe moderne\nimport { type Produit, type Zone, TARIFS_ZEM } from \"./catalogue\";\n\n// À l\'émission, l\'exemple 3 devient littéralement :\n// import { TARIFS_ZEM } from \"./catalogue\";\n// (les deux mentions type ont disparu : elles n\'existaient que pour vérifier)' },
            { t: 'p', h: 'Pourquoi marquer ce qui va s\'effacer, alors que « le compilateur le sait déjà » ? Trois raisons très concrètes. D\'abord l\'OUTILLAGE : les transpileurs rapides (esbuild, swc — ceux de Vite, Next…) traitent chaque fichier ISOLÉMENT (`isolatedModules`) : sans le mot `type`, ils ne peuvent pas savoir si `Produit` est une valeur à conserver ou un type à effacer — et génèrent un import vers un module qui n\'exportera rien, ce qui a fait planter plus d\'un build en production. Ensuite la LISIBILITÉ : relire un import marqué `type`, c\'est savoir immédiatement qu\'il ne crée aucune liaison runtime — aucun effet de bord au chargement, aucune dépendance circulaire possible. Enfin la SÉCURITÉ STRte : avec `verbatimModuleSyntax` activé (de plus en plus la norme dans les gabarits récents), TS REFUSE tout import de type non marqué — autant prendre le pli proprement tout de suite.' },
            { t: 'h3', h: 'Le point qui déroute tout le monde : les extensions .js dans les imports' },
            { t: 'code', lang: 'ts', label: 'module et moduleResolution (tsconfig)', code:
'// ── Projet BUNDLER (Vite/Next/esbuild) : moduleResolution \"bundler\"\nimport { livraison } from \"./catalogue\";      // ✓ pas d\'extension : le bundler résout\n\n// ── Projet NODE PUR (module/moduleResolution \"NodeNext\")\nimport { livraison } from \"./catalogue.js\";   // ✓ .js alors que le fichier est .ts !\nimport { livraison } from \"./catalogue\";      // ✗ le runtime Node ne devine rien\n\n// Pourquoi ce « .js » absurde ? Parce que l\'import parle au FICHIER ÉMIS,\n// pas à la source : dist/catalogue.js arrivera, et Node l\'exige par son nom.' },
            { t: 'p', h: 'Retiens la règle sans perdre le nord : en TypeScript, l\'import cible le fichier COMME IL EXISTERA APRÈS ÉMISSION. Avec un bundler, l\'outil résout l\'ambiguïté — extension optionnelle, chemins d\'alias `@/…` possibles. Avec Node natif (modules ESM), Node est strict : le chemin doit être EXACT, extension comprise — d\'où ce `./catalogue.js` qui semble désigner un inexistant mais dit la vérité SUR LA SORTIE. La bonne nouvelle : tu n\'as pas à deviner. `moduleResolution\"` + `module` règlent le comportement attendu dans `tsconfig.json` (fiche `ts-tsc-config`), le compilateur signale les incohérences, et ton gabarit de départ (Vite, Next, tsoa…) livre le couple déjà assorti. Le piège ne touche que celui qui mélange les deux mondes sans le savoir — tu sais.' },
            { t: 'h3', h: 'Les barrels et les alias de chemins : deux commodités, deux mesures' },
            { t: 'code', lang: 'ts', label: 'confort.ts (+ tsconfig paths)', code:
'// src/model/index.ts — le « barrel » : réexporte l\'essentiel public\nexport type { Produit, Commande } from \"./types\";\nexport { CatalogueProduits } from \"./catalogue\";\n// → ailleurs : import { type Produit, CatalogueProduits } from \"./model\";\n\n// tsconfig.json — un alias de chemins pour cesser les ../../../\n{ \"compilerOptions\": {\n    \"baseUrl\": \".\",\n    \"paths\": { \"@/*\": [\"src/*\"] }\n} }\n// → import type { Produit } from \"@/types\";   // stable quel que soit le fichier' },
            { t: 'p', h: 'Deux commodités d\'équipe, à utiliser avec dosages connus. Le BARREL (un `index.ts` qui réexporte le visage public d\'un dossier) clarifie les importations des consommateurs — mais méfie-toi des barrels qui TOUT réexportent à la chaîne : ils recréent des liaisons runtime entre modules que rien ne nécessitait, ralentissent le démarrage, et favorisent les cycles d\'imports. L\'ALIAS `@/` régularise les chemins (fini les `../../../../types` cassés à chaque déplacement de fichier) — note seulement qu\'il est résolu PAR TON OUTIL (le bundler doit connaître le même alias ; Node pur demande son propre mécanisme `imports` de package.json). Dans les deux cas : marque `type` dans les réexports aussi (`export type { Produit }`), la mécanique d\'effacement traverse les barrels sans broncher.' },
            { t: 'h3', h: 'Ce que les modules TS changent — ou pas — à tes habitudes' },
            { t: 'table', head: ['Question', 'Réponse'], rows: [
              ['Une interface exportée existe-t-elle au runtime ?', 'non — effacée, n\'importez-la qu\'en `type`'],
              ['import type empêche-t-il les effets de bord du module ?', 'oui : le module n\'est pas chargé DU TOUT depuis cet import'],
              ['Peut-on exporter types et valeurs du même fichier ?', 'oui — marquez les imports granulairement'],
              ['Faut-il l\'extension .js ?', 'NodeNext : obligatoire ; bundler : facultative'],
              ['Le barrel est-il obligatoire ?', 'non — une commodité, jamais une obligation'],
              ['verbatimModuleSyntax change quoi ?', 'marquage type obligatoire, import/export conservés tels quels']
            ]},
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« import type est juste un commentaire »** — non : c\'est une instruction d\'ÉMISSION. Le module visé ne sera pas chargé par cette importation — si ce module exécute des effets de bord au chargement, ils n\'arriveront pas « par hasard » via un import de types.',
              '**« le compilateur peut toujours deviner à ma place »** — dans un projet tsc monolithique, souvent oui ; dans la chaîne moderne (esbuild/swc/isolatedModules), NON : chaque fichier est transpilé seul, et seul le mot `type` dit ce qui s\'efface. D\'où `verbatimModuleSyntax` qui rend l\'intention obligatoire.',
              '**« ./catalogue.js est une faute de frappe »** — c\'est la réalité de l\'APRÈS-compilation avec NodeNext : les imports visent les fichiers émis. Si ça te surprend, c\'est que ton projet est en mode bundler — vérifie `moduleResolution` avant de « corriger ».',
              '**« les interfaces n\'ont pas besoin d\'être exportées »** — si : comme les valeurs, un type vit dans SON module tant qu\'il n\'est pas exporté. `export interface Produit` rend le contrat disponible ; `types.ts` centralise le vocabulaire.',
              '**« default export + types pour tout »** — les types ne connaissent pas d\'export par défaut ; et côté valeurs, la recommandation générale reste les exports nommés : renommages suivis, imports auto-complétés, dépendances lisibles.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Tu réutilises ici la mécanique JS de modules (imports/exports, strict mode implicite, hoisting des imports) en y greffant la spécificité TS : la frontière valeur/type, et l\'effacement. Le couple `module` + `moduleResolution` renvoie à `ts-tsc-config` où le tsconfig de départ a été posé ; les interfaces de `ts-interfaces` sont précisément ce que tu déplaces dorénavant dans `types.ts` ; et le prochain voisin, `ts-dts`, montre l\'autre face de la médaille : comment des types voyagent entre PROJETS (paquets npm, fichiers de déclaration) sans qu\'une ligne de leurs implémentations ne te concerne.' },
            { t: 'callout', kind: 'tip', h: 'Règle de la maison qui rend serein : dans chaque import, CHAQUE identifiant qui ne désigne pas du code exécutable porte `type` — soit via un `import type` dédié au-dessus des imports de valeurs, soit en ligne (`import { type Produit, tarifs }`). La section imports devient une carte des dépendances RÉELLES du fichier : ce qui tourne d\'un côté, ce qui vérifie de l\'autre.' }
          ],
          errors: [
            { title: 'Importer un type comme une valeur sous verbatimModuleSyntax', lang: 'ts', bad:
'import { Produit } from \"./types\";\n// ✗ verbatimModuleSyntax / isolatedModules : Produit est un TYPE —\n//   conservé tel quel, l\'import survit à l\'émission… vers rien', good:
'import type { Produit } from \"./types\";          // ✓ effacé, clairement\nimport { type Produit } from \"./types\";          // ✓ variante en ligne (mélangée)\nimport { TARIFS } from \"./tarifs\";               // ✓ les VALEURS, elles, restent', why: 'Les transpileurs fichier-par-fichier ne voient pas `./types.ts` : sans le mot `type`, impossible de savoir que `Produit` s\'efface — l\'import est conservé et pointe vers un export inexistant. Le marquage explicite rend l\'intention certaine dans TOUTES les chaînes de build, pas seulement la tienne ce matin.' },
            { title: 'Oublier l\'extension .js avec NodeNext', lang: 'ts', bad:
'// moduleResolution: \"NodeNext\"  (Node natif, ESM)\nimport { livraison } from \"./catalogue\";   // ✗ compile peut-être,\n// mais en production : ERR_MODULE_NOT_FOUND — Node ne devine pas', good:
'import { livraison } from \"./catalogue.js\";   // ✓ tu parles au fichier ÉMIS\n// (dist/catalogue.js), exactement comme Node l\'entend au runtime', why: 'Node ESM est strict : un import relatif sans extension n\'existe pas. Le `.js` dans un fichier `.ts` semble absurde jusqu\'à ce qu\'on comprenne le principe : l\'import décrit la SORTIE (le fichier qui existera), pas la source. Les bundlers sont tolérants, Node non — ton `moduleResolution` annonce dans quel monde tu joues.' }
          ],
          related: ['ts-dts', 'ts-tsc-config', 'ts-interfaces', 'ts-types-speciaux']
        },
        {
          id: 'ts-dts',
          title: 'Fichiers .d.ts & @types/',
          icon: 'description',
          level: 'Intermédiaire',
          tagline: 'Typer du JavaScript sans le réécrire : déclarations, @types, et le contrat des bibliothèques.',
          intro: 'Le monde du JavaScript est VASTE et ancien : des millions de bibliothèques utiles écrites sans une seule annotation de type. Les **fichiers de déclaration** (`.d.ts`) résolvent l\'équation : y coller du TS serait tout réécrire — à la place, on écrit SÉPARÉMENT « ce que ce code promet ». Un `.d.ts` ne contient que des TYPES (interfaces, signatures de fonctions, classes sans corps) : il dit au compilateur « fais comme si tu savais » — et le compilateur vérifie TON usage contre ces promesses. Couvre le tout avec l\'écosystème `@types/` (le magasin communautaire de déclarations), ajoute la capacité d\'en ÉCRIRE pour tes propres modules JS ou globaux, et tu sauras typer tout code que tu n\'as pas écrit — c\'est-à-dire l\'essentiel du monde.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : typer sans réécrire' },
            { t: 'p', h: 'Situation vécue par toute équipe, tôt ou tard. Le projet embarque une bibliothèque JS formidable — un vieux module de calcul de tontines maison, un SDK de paiement, `lodash` — écrite sans TypeScript. À l\'import, la sentence tombe : `Could not find a declaration file`. Trois réponses. La première : utiliser `any` et renoncer à la vérification sur ce pan entier — vraiment dommage. La deuxième : réécrire la librairie en TS — excessif, et personne ne te paiera pour. La troisième, celle du monde réel : DÉCLARER ses types dans un fichier `.d.ts` à côté. Le code JS continue de tourner tel quel ; le fichier de déclaration instruit le compilateur — et dès lors, TON usage de la librairie est vérifié ligne par ligne, autocomplété, refactorisé. C\'est ainsi que tout l\'écosystème JS est devenu typable sans bouger : par ACCUMULATION de promesses écrites en marge.' },
            { t: 'h3', h: 'Un .d.ts minimal : des promesses, jamais du code' },
            { t: 'code', lang: 'ts', label: 'tontine.d.ts (pour tontine.js, module JS existant)', code:
'// PAS d\'implémentation ici : uniquement des formes et des signatures.\nexport interface Cotisation {\n  membreId: string;\n  montant: number;\n  date: string;\n}\n\nexport declare function calculePenalite(joursRetard: number, montant: number): number;\nexport declare function prochaineEcheance(mois: number): string;\n\n// Dans ton TS, aussitôt :\n// import { calculePenalite } from \"./tontine\";   // ← vérifié, autocomplété\ncalculePenalite(14, 5000);      // ✓\ncalculePenalite(\"14\", 5000);    // ✗ la promesse dit number' },
            { t: 'p', h: 'Les règles du jeu sont simples et strictes. Un `.d.ts` ne contient QUE des déclarations : `interface`, `type`, et des signatures marquées `declare` (`declare function`, `declare const`, `declare class`) — aucune implémentation, aucune valeur initialisée, aucun corps de fonction (le compilateur refuse si tu essayes : ce fichier ne S\'EXÉCUTERA jamais, il n\'émet rien). Le mot-clé `declare` se lit : « ce symbole EXISTE quelque part au runtime — dans le .js voisin, dans le navigateur, dans le global — je ne fais que le décrire ». Dès que le fichier vit à côté du module JS (ou à un endroit connu du projet), l\'import côté TS trouve les types, et tout redevient civilisé : signatures vérifiées, complétion, refactors suivis.' },
            { t: 'h3', h: 'declare module, declare global : décrire ce qui existe ailleurs' },
            { t: 'code', lang: 'ts', label: 'environnement.d.ts', code:
'// 1) Typer un paquet npm SANS types officiels :\ndeclare module \"ancien-sdk-paiement\" {\n  export function initierPaiement(montant: number, tel: string): Promise<{ ref: string }>;\n  export function verifier(ref: string): Promise<\"succes\" | \"echec\" | \"attente\">;\n}\n\n// 2) Déclarer un global posé par un script tiers (ou ton index.html) :\ndeclare global {\n  interface Window {\n    boutiqueVersion: string;\n  }\n}\nexport {};   // nécessaire pour que le fichier reste un MODULE\n\n// 3) Typer des imports qui ne sont PAS du code (images, css… avec Vite) :\ndeclare module \"*.png\" { const src: string; export default src; }' },
            { t: 'p', h: 'Trois situations, trois formes de `declare`. `declare module \"nom\"` décrit un paquet dont TS ignore tout — l\'équivalent d\'écrire sa carte d\'identité ; à l\'import du paquet, les types s\'appliquent. `declare global` élargit l\'objet global (avec le `export {}` qui force le fichier à être un module, sinon ses déclarations restent locales) — utile pour les scripts chargés par balise, les variables posées par le back-office, ou la fameuse fusion d\'interface sur `Window` vue dans la fiche Interfaces. Et la troisième forme, quotidienne avec Vite ou webpack : les « modules sauvages » (`*.png`, `*.css`, `?url`) — on déclare leur TYPE d\'export (souvent une string — le chemin), et l\'import `import logo from \"./logo.png\"` devient typé. Chacune de ces trois écritures est une PROMESSE : si le vrai paquet, la vraie globale ou la vraie image ne correspond pas, le mensonge éclatera au runtime — d\'où La règle d\'or : n\'écris que ce que tu as VÉRIFIÉ dans le code réel.' },
            { t: 'h3', h: 'Le magasin commun : installer les types via @types/' },
            { t: 'code', lang: 'bash', label: 'terminal', code:
'# Pour une librairie JS pure, les types arrivent en devDependency :\nnpm install --save-dev @types/lodash\n\n# La convention, partout pareille :\n#   paquet « lodash »        → types « @types/lodash »\n#   paquet « node »          → types « @types/node » (le classique des classiques)\n#   paquet express installé  → types à installer : @types/express\n\n# Et le fichier de déclaration ARRIVE TOUT SEUL :\n# TS regarde automatiquement node_modules/@types/* (typeRoots par défaut)' },
            { t: 'p', h: 'L\'écosystème `@types` (projet DefinitelyTyped) est la plus grande collection de déclarations au monde : des milliers de bibliothèques JS pures y ont leurs cartes d\'identité, maintenues par la communauté. Deux règles de conduite professionnelle. Un paquet @types s\'installe en DEV-dependency : les types ne servent qu\'à la VÉRIFICATION — rien à livrer en production. Et avant d\'installer, vérifie que la librairie n\'est pas DÉJÀ typée : de plus en plus de projets embarquent leurs propres déclarations (le champ `"types"` de leur package.json) — un double approvisionnement fait des conflits (deuxième carte d\'erreur). Savoir où TS cherche est le dernier morceau : `node_modules/@types` par défaut (option `typeRoots`), les champs `types`/`typings` des packages, puis tes propres fichiers inclus par `include` — l\'option `skipLibCheck`, quand elle est activée, dit aussi de fermer les yeux sur les imperfections INTERNES de ces fichiers tiers (tu n\'es pas leur relecteur).' },
            { t: 'h3', h: 'Publier tes propres déclarations : declaration: true' },
            { t: 'code', lang: 'json', label: 'tsconfig d\'une librairie, puis package.json', code:
'// tsconfig.json\n{ \"compilerOptions\": {\n    \"declaration\": true,        // tsc GÉNÈRE les .d.ts à partir de ton .ts\n    \"emitDeclarationOnly\": true,\n    \"outDir\": \"dist\"\n} }\n\n// package.json\n{\n  \"main\": \"dist/index.js\",\n  \"types\": \"dist/index.d.ts\",   // ← le point d\'entrée des types pour tes consommateurs\n  \"files\": [\"dist\"]\n}\n\n// tsc fait la moitié du travail : TON code TS devient la SOURCE des .d.ts —\n// ils ne peuvent plus mentir, puisqu\'ils sont calculés.' },
            { t: 'p', h: 'Tiens le raisonnement jusqu\'au bout et tu boucles le module entier : quand TU publies une bibliothèque écrite en TypeScript, tu n\'écris pas deux fois — le compilateur PRODUIT les `.d.ts` depuis ton code source (`declaration: true`). Le type vérifie le code ET publie le contrat : la promesse et l\'implémentation ne peuvent pas diverger, parce que l\'une est calculée depuis l\'autre. C\'est la boucle vertueuse que TypeScript a offerte à tout l\'écosystème JS : même ceux qui n\'utilisent pas TS dans LEUR projet consomment les types des librairies qu\'ils installent, parce que ces librairies, elles, en ont généré. Les types sont devenus une monnaie d\'échange universelle — et le `.d.ts` est son format de billets.' },
            { t: 'h3', h: 'Sous le capot : l\'ordre de recherche des types' },
            { t: 'p', h: 'Quand tu écris `import { x } from \"paquet\"`, le compilateur résout en couches : 1) le paquet expose-t-il ses propres types ? (champ `types`/`typings` de SON package.json, ou `.d.ts` livré à côté du `.js`) — le cas moderne, prioritaire ; 2) existe-t-il un `@types/paquet` installé ? (lu automatiquement dans `node_modules/@types`) ; 3) existe-t-il un `declare module \"paquet\"` dans TON projet ? Et si tout échoue : l\'erreur fameuse `Could not find a declaration file`, avec en options la création d\'une déclaration minimale ou le contournement temporaire (un `declare module \"paquet\";` nu, équivalent avoué d\'un `any` centralisé — au moins, il est LOCALISÉ dans un seul fichier, au lieu de fuiter partout). L\'option `skipLibCheck` agit en coulisses de tout ça : elle dit au vérificateur de ne pas examiner la cohérence INTERNE des fichiers tiers — activée presque partout en production, pour des raisons de vitesse et de tolérance aux librairies imparfaites.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« un .d.ts s\'exécute quelque part »** — jamais : il n\'émet RIEN. C\'est pure spécification, relue à la compil, effacée avant l\'émission. Si une déclaration promet une fonction absente au runtime, c\'est TON import qui plantera.',
              '**« @types change le comportement du paquet »** — non : `@types/x` ne modifie pas `x`, n\'ajoute aucun code, ne tourne pas. C\'est un paquet de PROMESSES installé en devDependency — exactement comme une notice technique rangée à côté de la machine.',
              '**« declare signifie \'à implémenter plus tard\' »** — tout le contraire : `declare` signifie « EXISTE déjà, ailleurs ». Le corps n\'arrivera jamais dans ce fichier — il est déjà livré par le JS, le navigateur ou le paquet.',
              '**« les librairies sans types sont inutilisables en TS »** — non : entre `@types/`, un `declare module` local de dix lignes, et la déclaration sauvage temporaire, tout code JS devient consommable. La seule question est « combien de promesses vais-je écrire », jamais « est-ce possible ».',
              '**« je peux implémenter DANS le .d.ts »** — non : aucun corps, aucune valeur initialisée (hors `const enum` historique). Une déclaration, c\'est une signature — l\'implémentation vit dans le `.js`/`.ts` correspondant, ou chez un tiers.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Cette fiche ferme naturellement le chapitre des modules : `ts-modules` faisait voyager les types dans TON projet, celle-ci les fait voyager entre PROJETS — le même format, à l\'échelle de l\'écosystème. La fusion d\'interfaces (`ts-interfaces`) est le carburant des augmentations globales (`declare global`, `Window`, prototypes étendus) ; le couple `declaration`/`skipLibCheck` renvoie au `tsconfig` posé dans `ts-tsc-config` ; et si tu publies un jour la librairie de gestion de tontines de la Boutique Awa, tu généreras ses types automatiquement — preuve vivante que les déclarations sont des types qui ont pris le train.' },
            { t: 'callout', kind: 'tip', h: 'Avant d\'écrire la moindre ligne d\'un `.d.ts` maison, pose-toi ces trois questions dans l\'ordre : la librairie embarque-t-elle ses types ? (regarde son package.json) — existe-t-il un `@types/` ? (une recherche npm suffit) — la promesse que je m\'apprête à écrire, l\'ai-je VÉRIFIÉE dans le code source réel ? Trois « oui » t\'épargnent des heures ; un « je suppose » t\'en coûtera davantage.' }
          ],
          errors: [
            { title: 'Écrire l\'implémentation dans un .d.ts', lang: 'ts', bad:
'// tontine.d.ts\ndeclare function calculePenalite(j: number, m: number): number {\n  return j * m * 0.01;   // ✗ CORPS INTERDIT : un .d.ts n\'exécute rien\n}', good:
'// tontine.d.ts : la SIGNATURE seule\ndeclare function calculePenalite(joursRetard: number, montant: number): number;\n\n// tontine.js : le CODE, inchangé, qui tourne déjà\nexports.calculePenalite = (j, m) => j * m * 0.01;', why: 'Un fichier de déclaration est un CONTRAT, pas une usine : il n\'émet aucun code, ne s\'exécute pas, n\'autorise aucun corps de fonction ni valeur initialisée. Le `declare` promet que le symbole existe AILLEURS au runtime — si tu écris le corps dans le .d.ts, tu mélanges la notice et la machine.' },
            { title: 'Installer @types pour un paquet déjà typé', lang: 'bash', bad:
'npm install date-fns @types/date-fns\n# ✗ @types/date-fns est obsolète : date-fns embarque SES types.\n# Les deux déclarations entrent en conflit : erreurs inexplicables en cascade', good:
'npm install date-fns\n# → vérifier le package.json de la librairie : un champ \"types\"/\"typings\"\n#   ou des .d.ts livrés ? Alors RIEN d\'autre à installer.\n# @types/ = uniquement pour le JS SANS types embarqués.', why: 'Deux sources de types pour le même paquet, c\'est un ménage à trois que le compilateur paie en diagnostics bizarres (déclarations dupliquées, versions décalées). La règle actuelle est simple : types embarqués PRIORITAIRES et suffisants ; `@types/` en secours des bibliothèques JS pures — jamais les deux à la fois.' }
          ],
          related: ['ts-modules', 'ts-tsc-config', 'ts-generics', 'ts-interfaces']
        }
      ]
    },
  {
      id: 'decorateurs',
      name: 'Décorateurs',
      icon: 'celebration',
      fiches: [
        {
          id: 'ts-decorateurs',
          title: 'Décorateurs : le pouvoir expérimental',
          icon: 'auto_fix_high',
          level: 'Avancé',
          tagline: 'Attacher un comportement transversal — log, cache, validation — à une déclaration sans la réécrire.',
          intro: 'Certaines préoccupations traversent tout un programme sans appartenir à aucune classe : journaliser, mesurer, mettre en cache, valider, tracer. Les **décorateurs** attachent ces comportements DIRECTEMENT aux déclarations — une méthode, une classe — via une syntaxe aérienne, `@log`, `@memoize`, apposée au-dessus de la cible. Historiquement expérimentaux (`experimentalDecorators`, le drapeau des anciens projets Angular/NestJS), ils sont depuis TypeScript 5 alignés sur le **standard TC39** : le pouvoir reste, la stabilité en plus. Mais attention — rupture avec tout ce module : les décorateurs sont du CODE ÉMIS, pas des types effacés. Ils coûtent au runtime, s\'exécutent au chargement, et doivent se mériter. Le meilleur projet n\'en écrit presque jamais ; il en CONSOMME via ses frameworks — et comprend exactement ce qu\'il paie.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : les préoccupations qui traversent tout' },
            { t: 'p', h: 'À la Boutique Awa, tu veux journaliser chaque encaissement — « qui, combien, quand » — sans recopier trois lignes de log dans vingt méthodes. Le wrapper de fonction classique marche, mais chaque méthode doit se SOUVENIR de passer par lui : à la vingt-et-unième méthode, quelqu\'un oubliera. Le décorateur inverse la charge : le comportement est ATTACHÉ à la déclaration elle-même, visible, impossible à oublier. C\'est le même geste qu\'au marché : plutôt que dire à chaque caissier « n\'oublie pas de tamponner le reçu », on pose le tampon SUR le reçu. Deux mondes se partagent l\'usage aujourd\'hui : les frameworks (Angular, NestJS, Lit — qui t\'en font CONSOMMER : `@Component`, `@Injectable`, `@Get`) et les bibliothèques d\'outillage (qui en vendent quelques-uns réutilisables). Le premier réflexe reste d\'en LIRE, pas d\'en écrire — mais un jour, cette lecture te servira à déboguer ce que ton framework fait derrière ton dos.' },
            { t: 'h3', h: 'Le décorateur de méthode, version standard (TS 5)' },
            { t: 'code', lang: 'ts', label: 'log.ts — standard TC39, TS 5+', code:
'// Un décorateur EST une fonction : elle reçoit la valeur décorée\n// et un contexte, et peut rendre un REMPLAÇANT.\nfunction log(\n  methode: (...args: unknown[]) => unknown,\n  contexte: ClassMethodDecoratorContext\n) {\n  const nom = String(contexte.name);\n  return function (this: unknown, ...args: unknown[]) {\n    const avant = performance.now();\n    const resultat = methode.apply(this, args);\n    console.log(\"[Boutique] \" + nom + \" : \" +\n      (performance.now() - avant).toFixed(1) + \" ms\");\n    return resultat;\n  };\n}\n\nclass Caisse {\n  @log\n  encaisser(montant: number): number {\n    return montant;   // + journalisation automatique, attachée à la déclaration\n  }\n}' },
            { t: 'p', h: 'Trois idées à décoller de cette mécanique. 1) Le décorateur est une FONCTION ORDINAIRE appliquée à la déclaration : il reçoit la méthode originale et un objet `contexte` (nom, genre de membre, visibilité…) et rend une NOUVELLE fonction qui l\'enveloppe. 2) `@log` n\'est que du sucre : sous la capuche, la classe appelle `log(encaisser, contexte)` au chargement, et remplace la méthode par le retour. 3) La syntaxe standard (TS 5+) — `(value, context) => replacement` — diffère de l\'ANCIENNE (`experimentalDecorators`, celle d\'Angular classique : `(target, propertyKey, descriptor)`) : si tu lis du code d\'il y a quelques années, reconnais le dialecte avant de juger. Les deux existent dans la nature ; ta version de TS et ton `tsconfig` décident du dialecte émis.' },
            { t: 'h3', h: 'memoize : un vrai cas d\'école, mesuré' },
            { t: 'code', lang: 'ts', label: 'memoize.ts', code:
'function memoize<This, Args extends unknown[], R>(\n  calcul: (this: This, ...args: Args) => R,\n  _contexte: ClassMethodDecoratorContext\n) {\n  const cache = new Map<string, R>();\n  return function (this: This, ...args: Args): R {\n    const cle = JSON.stringify(args);\n    if (!cache.has(cle)) cache.set(cle, calcul.apply(this, args));\n    return cache.get(cle) as R;   // .get sur Map : R | undefined → garanti par has\n  };\n}\n\nclass Tarificateur {\n  @memoize\n  prixCourse(zone: string, poidsKg: number): number {\n    // calcul lourd (grille, majorations, promotion en cours…)\n    return zone.length * 100 + poidsKg * 50;\n  }\n}\nnew Tarificateur().prixCourse(\"calavi\", 50);   // calculé UNE fois, puis servi du cache' },
            { t: 'p', h: 'Retiens la leçon avant la syntaxe : `@memoize` est un comportement TRANSVERSAL (n\'importe quel calcul pur pourrait l\'utiliser) attaché PROPREMENT à la déclaration — la méthode elle-même reste pure, sans une ligne de gestion de cache. C\'est exactement le genre de décorateur que les bibliothèques vendent tout prêt ; la tienne prouve qu\'il n\'y a pas de magie : une enveloppe, une Map, un test `has`. Deux mises en garde d\'honnête marchand. Les génériques rendent le décorateur RÉUTILISABLE sur n\'importe quelle signature — `<This, Args, R>` capture la forme et la vérifie à chaque application. Et le cache a des RÈGLES : invalidation, taille, clés — un `JSON.stringify(args)` n\'est pas gratuit et peut mentir sur les objets complexes ; en production, on calibre, on borne, on mesure. Le décorateur rend le SÉDUISANT facile ; à toi de garder le CORRECT.' },
            { t: 'h3', h: 'Décorateurs de classe : enregistrer au passage' },
            { t: 'code', lang: 'ts', label: 'registre.ts', code:
'// Le registre central des moyens de paiement disponibles\nconst registrePaiements: Record<string, unknown> = {};\n\nfunction enregistrerPaiement<T extends new (...args: any[]) => object>(\n  classe: T,\n  contexte: ClassDecoratorContext\n) {\n  registrePaiements[String(contexte.name)] = classe;\n  return classe;   // inchangée — juste DÉCLARÉE au registre\n}\n\n@enregistrerPaiement\nclass PaiementMoMo { /* … */ }\n\n@enregistrerPaiement\nclass PaiementEspeces { /* … */ }\n\n// registrePaiements : { PaiementMoMo: class, PaiementEspeces: class }\n// → la boutique découvre ses moyens de paiement SANS liste à maintenir' },
            { t: 'p', h: 'Le décorateur de CLASSE reçoit le constructeur lui-même — et son usage le plus propre est l\'ENREGISTREMENT AUTOMATIQUE : la classe se déclare dans un registre central au simple fait d\'exister, sans qu\'aucune liste d\'imports ne soit tenue à jour (« ajoute un moyen de paiement » devient : écrire la classe, apposer le décorateur, terminé). C\'est la mécanique exacte des `@Component`, `@Entity` et autres gardiens de frameworks : le décorateur COLLECTE la métadonnée au chargement, et le framework la consomme au démarrage. Note la discipline qui rend tout cela sain : le décorateur RETOURNE la classe INCHANGÉE — enregistrer sans modifier. Dès qu\'un décorateur de classe commence à REMPLACER le constructeur (injection, proxies), il déplace de la logique invisible dans une zone que peu de reliseurs inspectent — pouvoir réel, dette réelle.' },
            { t: 'h3', h: 'Sous le capot : du code, pas des types' },
            { t: 'p', h: 'Voici la rupture à comprendre, en un paragraphe qui casse dix fiches d\'habitude. Jusqu\'ici dans ce module, TOUT ce que TypeScript ajoutait s\'effaçait (types, interfaces, génériques). Le décorateur, lui, produit du CODE ÉMIS : la fonction décoratrice est appelée au CHARGEMENT du module (historiquement via un helper `__decorate` généré par tsc ; en mode standard, via l\'évaluation de la classe elle-même). Trois conséquences. Un COÛT d\'exécution existe — import, appels, enveloppe à chaque appel de méthode décorée (minimes ici, mais mesurables sur des chemins chauds). L\'ordre d\'application compte : plusieurs décorateurs sur la même cible s\'appliquent de bas en haut, comme des poupées gigognes. Et les décorateurs ne peuvent PAS décorer ce qui n\'a pas de déclaration au bon niveau : fonctions flèches isolées, paramètres libres — la syntaxe vise les classes et leurs membres. Si la magie te démange, relis ce capot : c\'est une application de fonctions au moment du chargement, rien de plus — rien de moins.' },
            { t: 'h3', h: 'Décorateur ou simple fonction ? Le tableau d\'arbitrage' },
            { t: 'table', head: ['Critère', 'Fonction wrapper', 'Décorateur'], rows: [
              ['Visibilité à la déclaration', 'faible (au point d\'appel)', 'forte (AU-DESSUS de la méthode)'],
              ['Coût au runtime', 'un appel de plus', 'enveloppe + évaluation au chargement'],
              ['Attache automatique', 'non (risque d\'oubli)', 'oui, impossible à oublier'],
              ['Testabilité unitaire', 'excellente', 'moyenne (la cible est remplacée)'],
              ['Terrain naturel', '1-2 endroits précis', 'membre transversal, par dizaines'],
              ['À choisir si…', 'défaut de la maison', 'framework le réclame ou répétition massive']
            ]},
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« le décorateur s\'efface comme les types »** — NON : c\'est la grande rupture du module. Le décorateur ÉMET du code, s\'exécute au chargement, enveloppe à chaque appel. C\'est du JavaScript réel avec une syntaxe en sucre — pas de la spécification.',
              '**« experimentalDecorators et standard, même chose »** — deux DIALECTES : l\'ancien `(target, key, descriptor)` d\'Angular historique, et le standard TC39 `(value, context)` de TS 5+. Connais le tien avant de copier un exemple trouvé en ligne.',
              '**« je peux décorer n\'importe quoi »** — non : classes et membres de classes (méthodes, champs, accesseurs). Une fonction flèche isolée, un type, une interface : hors de portée, par construction.',
              '**« décorer change this tout seul »** — non : l\'enveloppe reçoit `this` comme paramètre et doit le RELAYER (`calcul.apply(this, args)`) — un décorateur qui perd `this` est LA panne classique (première carte d\'erreur).',
              '**« tous les projets devraient en avoir »** — pas du tout : les décorateurs bien utilisés sont RARES et transversaux. Pour 90 % des besoins, une fonction wrapper explicite est plus lisible, plus testable, plus honnête. Le décorateur se mérite à la répétition.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Tu décores ici les classes des fiches `ts-classes` et `ts-classes-avancees` (membres, privés, `this`) ; les signatures génériques de `ts-generics-contraintes` rendent les `memoize` réutilisables ; et le contraste avec l\'effacement de types qui régissait TOUT le module jusqu\'ici (`ts-pourquoi` : « zéro coût au runtime ») est LA leçon architecturale de cette fiche — TypeScript a deux natures : la vérification qui s\'efface, et désormais, avec les décorateurs, l\'instrumentation qui reste. Si ton projet n\'utilise ni Angular ni Nest, classe cette fiche en « culture stratégique » : tu sais désormais ce que tes outils paient en ton nom.' },
            { t: 'callout', kind: 'tip', h: 'Test de maturité avant d\'écrire TON premier décorateur : « ce comportement va-t-il s\'appliquer à plus de cinq membres, sans logique propre à chacun ? ». Non → fonction wrapper, point. Oui → décorateur documenté, testé comme du code réel (il l\'est !), et relu en revue EXACTEMENT comme une méthode, pas comme une annotation.' }
          ],
          errors: [
            { title: 'Perdre this dans l\'enveloppe', lang: 'ts', bad:
'return function (this: unknown, ...args: unknown[]) {\n  return methode(args);       // ✗ this AVALE : la méthode originale\n}                              //   s\'exécute sans son instance → crash au premier this.xxx', good:
'return function (this: unknown, ...args: unknown[]) {\n  return methode.apply(this, args);   // ✓ relaye l\'instance, tout survit\n};', why: 'La méthode décorée est remplacée par ton enveloppe ; quand le code appelle `caisse.encaisser(…)`, c\'est TON fonction qui reçoit `this`. Ne pas le relayer à l\'originale (`apply`/`call`), c\'est exécuter le corps de la méthode sans son objet — le premier `this.solde` explose. `apply` est le gardien de la relève.' },
            { title: 'Croire que le décorateur est gratuit au runtime', lang: 'ts', bad:
'// « C\'est TypeScript, ça s\'efface à la compilation, non ? »\n// → le bundle gagne une enveloppe PAR méthode décorée,\n//   un callback au chargement de CHAQUE classe concernée,\n//   et un appel supplémentaire à CHAQUE invocation.', good:
'// Mesure AVANT de décorer un chemin chaud (boucle de rendu, tri) ;\n// décore les chemins ADMINISTRATIFS (log, audit, auth) sans scrupule ;\n// et garde une version wrapper explicite pour les cas limites.', why: 'Les décorateurs sont la GRANDE EXCEPTION du module : ils émettent du code réel — zéro effacement. Sur un chemin froid (journalisation d\'encaissement), le coût est invisible ; sur un chemin chaud (appelée 10 000 fois/seconde), il est réel : mesure, profile, puis décide. La syntaxe sucrée ne change pas la facture.' }
          ],
          related: ['ts-classes-avancees', 'ts-generics-contraintes', 'ts-modules', 'js-fonctions']
        }
      ]
    },
  {
      id: 'dom',
      name: 'DOM & événements',
      icon: 'web',
      fiches: [
        {
          id: 'ts-dom-elements',
          title: 'DOM : typer les éléments',
          icon: 'account_tree',
          level: 'Intermédiaire',
          tagline: 'querySelector répond « Element ou null » — à toi de dire lequel, et de gérer l\'absence.',
          intro: 'Le DOM de JavaScript que tu connais (`querySelector`, `getElementById`, `value`, `src`…) fonctionne pareil sous TypeScript — mais le compilateur pose deux QUESTIONS que JS évacuait en silence : « quel GENRE d\'élément est-ce ? » (un bouton n\'a pas de `.value`, une image a un `.src`) et « es-tu sûr qu\'il EXISTE ? » (`null` si le sélecteur ne matche rien). La mécanique de réponse est d\'une élégance rare : les méthodes de sélection sont **génériques** — `querySelector<HTMLInputElement>` — tu affirmes le type attendu, l\'autocomplétion et la vérification suivent, et le `null` reste à traiter honnêtement. Fiche après fiche, le module t\'a donné les types du métier ; celle-ci te les branche sur la page, là où tes utilisateurs cliquent vraiment.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : le DOM est une forêt d\'éléments DIFFÉRENTS' },
            { t: 'p', h: 'En JavaScript pur, `document.querySelector(\"#recherche\")` renvoie « un élément » — flou, interchangeable, et chaque erreur attend le runtime : lire `.value` sur un `div` ? undefined, silencieux ; appeler `.play()` sur un élément qui n\'est pas une vidéo ? crash. Le DOM TypeScript fait la chose juste : chaque balise a SON interface (`HTMLInputElement` avec `.value`, `HTMLImageElement` avec `.src`, `HTMLButtonElement` avec `.disabled`…), toutes rassemblées dans une immense généalogie dont la racine commune est `Element`. Le contrat du sélecteur est alors limpide : DIS au compilateur ce que tu cherches (le générique), PROUVE que tu l\'as trouvé (le null-check), et la suite — méthodes autorisées, propriétés présentes, autocomplétion — fonctionne comme dans le reste du module. Le DOM n\'est plus une boîte noire : c\'est un arbre typé.' },
            { t: 'h3', h: 'Le générique du sélecteur : affirmer le type attendu' },
            { t: 'code', lang: 'ts', label: 'selection.ts', code:
'// Sans générique : Element | null — presque inutilisable tel quel\nconst el = document.querySelector(\"#recherche\");\n\n// AVEC : tu affirmes la balise visée, tu traites l\'absence — PUIS tu travailles\nconst champ = document.querySelector<HTMLInputElement>(\"#recherche\");\nif (champ) {\n  champ.value = \"gari\";          // ✓ value existe sur HTMLInputElement\n  champ.disabled = false;        // ✓ disabled aussi\n  // champ.play();                // ✗ refusé : ce n\'est pas une vidéo\n}\n\n// Le duo classique en une ligne élégante :\nconst bouton = document.querySelector<HTMLButtonElement>(\"#panier-btn\");\nbouton?.addEventListener(\"click\", ouvrirPanier);   // ?. absorbe l\'absence' },
            { t: 'p', h: 'Le générique est une AFFIRMATION, pas une vérification : le compilateur ne peut pas lire ton HTML via le sélecteur — il te CROIT sur le type d\'élément. Ce marché est assumé et excellent : l\'id `#recherche` et la balise `input` vivent côte à côte dans TON template, relus ensemble ; si un jour la balise change de nature, la ligne te hurlera dessus au premier usage incompatible. Ce qui n\'est PAS négociable, en revanche, c\'est le `null` : sélecteur fautif, élément pas encore rendu, page incomplète — le type `HTMLInputElement | null` t\'oblige à trancher. Deux styles : le `if (champ)` explicite (quand l\'absence est un vrai cas à traiter) et le `?.` (quand l\'absence est simplement « rien à faire »). Jamais — vraiment jamais — le `as HTMLInputElement` direct qui saute les deux questions (première carte d\'erreur).' },
            { t: 'h3', h: 'La ménagerie des types d\'éléments usuels' },
            { t: 'table', head: ['Élément', 'Type TS', 'Membres qui changent la vie'], rows: [
              ['input', 'HTMLInputElement', 'value, checked, placeholder, disabled'],
              ['form', 'HTMLFormElement', 'elements, requestSubmit(), reset()'],
              ['button', 'HTMLButtonElement', 'disabled, type'],
              ['select', 'HTMLSelectElement', 'value, options, selectedIndex'],
              ['textarea', 'HTMLTextAreaElement', 'value, rows'],
              ['img', 'HTMLImageElement', 'src, alt, naturalWidth'],
              ['a', 'HTMLAnchorElement', 'href, target'],
              ['video / audio', 'HTMLVideoElement / HTMLAudioElement', 'play(), pause(), currentTime'],
              ['div & cie génériques', 'HTMLElement', 'dataset, classList, style, textContent'],
              ['inconnu / général', 'Element', 'querySelector, getAttribute, classList']
            ]},
            { t: 'p', h: 'Note la hiérarchie, utile en lecture d\'erreurs : `HTMLInputElement extends HTMLElement extends Element`. Vers le HAUT, tout le monde partage (`classList`, `dataset`, `textContent`, `querySelector` sur un sous-arbre) ; vers le BAS, les spécialités (`value`, `src`, `checked`). Le réflexe qui t\'économisera des heures : typer au NIVEAU DONT TU AS BESOIN — ni `Element` (trop pauvre) ni `HTMLInputElement` quand un simple `textContent` suffit. Et quand plusieurs balises partagent un trait, l\'union fait le job : `HTMLInputElement | HTMLTextAreaElement` pour « n\'importe quel champ à `value` ». Deux mentions à connaître : `SVGElement` existe pour le SVG inline, et `HTMLCanvasElement` cache le `getContext(\"2d\")` qui mène à tout un monde à part — tu ne manques de rien, la forêt est cartographiée.' },
            { t: 'h3', h: 'Collections, dataset et boucles' },
            { t: 'code', lang: 'ts', label: 'collections.ts', code:
'// querySelectorAll : NodeListOf<T> affiné pareil\nconst lignes = document.querySelectorAll<HTMLTableRowElement>(\"#inventaire tr\");\nfor (const l of lignes) {                 // itérable directement\n  const produit = l.dataset.produit ?? \"\";\n  const prix = Number(l.dataset.prix);    // dataset : TOUT est string\n  console.log(produit, prix);\n}\n\n// getElementById : déjà précis, mais null pareil\nconst T1 = document.getElementById(\"total\");   // HTMLElement | null\n\n// dataset est typé DOMStringMap : string | undefined par clé —\n// le « data-produit » ABSENT renvoie undefined, ?? pose le défaut honnêtement' },
            { t: 'p', h: 'Trois verbes, trois comportements à connaître par cœur. `querySelectorAll<T>` retourne un `NodeListOf<T>` — directement itérable au `for…of` (et `Array.from(nodes)` si tu veux filter/map). `getElementById`, souvent oublié, a l\'honnêteté de rester précis : `HTMLElement | null` — le générique n\'y est pas nécessaire, un simple test de présence suffit antes de typer à la demande. Et `dataset` — les attributs `data-*` — est un `DOMStringMap` : chaque clé vaut `string | undefined`, TOUJOURS du texte (`data-prix=\"21000\"` arrive en string — à convertir via `Number`), et l\'absence donne `undefined`, pas du `null` : traiter avec `??`. C\'est un des derniers endroits du module où une DONNÉE EXTERNE rencontre ton monde typé ; les règles sont les mêmes que partout — vérifier, convertir, puis seulement calculer.' },
            { t: 'h3', h: 'Exemple : la recherche du catalogue de la Boutique Awa' },
            { t: 'code', lang: 'ts', label: 'recherche-catalogue.ts', code:
'interface Produit { nom: string; prix: number; stock: number }\nconst catalogue: Produit[] = [\n  { nom: \"Gari premium 50 kg\", prix: 21000, stock: 12 },\n  { nom: \"Huile rouge 5 L\", prix: 6500, stock: 30 }\n];\n\nconst champ = document.querySelector<HTMLInputElement>(\"#recherche\");\nconst zone = document.querySelector<HTMLUListElement>(\"#resultats\");\n\nfunction afficher(liste: Produit[]) {\n  if (!zone) return;\n  zone.innerHTML = \"\";\n  for (const p of liste) {\n    const li = document.createElement(\"li\");\n    li.textContent = p.nom + \" — \" + p.prix + \" F (\" + p.stock + \" en rayon)\";\n    zone.appendChild(li);\n  }\n}\n\nchamp?.addEventListener(\"input\", () => {\n  if (!champ) return;\n  const q = champ.value.trim().toLowerCase();\n  afficher(catalogue.filter((p) => p.nom.toLowerCase().includes(q)));\n});\n\nafficher(catalogue);   // premier rendu : tout le catalogue' },
            { t: 'p', h: 'Tout le module se rejoint dans cet exemple banal : les interfaces du catalogue, les tableaux et leur `filter`, l\'affirmation `HTMLInputElement`/`HTMLUListElement` au sélecteur, l\'écoute `input` qui déclenchera la fiche suivante, et même `?.` comme parapluie du montage. Et surtout : PAS UNE SEULE donnée n\'a été devinée — le DOM fournit des chaînes (`value`), ton catalogue fournit des types, et chaque conversion est visible. C\'est ça qu\'on appelle « le DOM sous contrôle » : pas plus de code qu\'en JS, mais chaque accès est justifié, chaque absence traitée, et le jour où tu renommes `#recherche`, la suite ne tombe plus en marche par grâce.' },
            { t: 'h3', h: 'Sous le capot : lib.dom.d.ts, la carte du territoire' },
            { t: 'p', h: 'D\'où viennent ces types magiques ? D\'un des plus gros fichiers de déclaration au monde : `lib.dom.d.ts`, livré avec TypeScript, qui modélise TOUTE la plateforme web — éléments, événements, `fetch`, `localStorage`, WebSocket… C\'est un `.d.ts` comme ceux de la fiche précédente, simplement officiel et colossal (des dizaines de milliers de lignes générées depuis les spécifications). Corollaires utiles à connaître : dans tsconfig, l\'option `lib: [\"ES2022\", \"DOM\"]` choisit quelles plates-formes sont déclarées — un script Node sans `DOM` ne connaîtra pas `document`, et c\'est une protection ; et quand ton éditeur affiche au survol la documentation d\'un `HTMLInputElement`, il ne fait que lire ce fichier. Les types de la plateforme ne sont pas du flan : c\'est la SPECIFICATION du web, transformée en promesses vérifiables.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« le générique vérifie le sélecteur »** — non : `querySelector<HTMLInputElement>(\"#video-principale\")` compile parfaitement. Le générique est une AFFIRMATION que tu relis avec le template ; sa force est l\'éditeur, pas lagarde-fou réel. La nullité, elle, est bien imposée — c\'est LE vrai garde-fou.',
              '**« getElementById devine la balise »** — non : il renvoie `HTMLElement | null`. Le générique `querySelector<T>` est l\'outil d\'affirmation ; sinon, c\'est le null-check puis un cast assumé au niveau requis.',
              '**« value est un nombre si l\'input est de type number »** — non : `.value` est TOUJOURS `string` (le HTML ne connaît que du texte). `champ.valueAsNumber` existe sur les inputs numériques — sinon `Number(champ.value)` et validation, comme pour toute donnée externe.',
              '**« NodeListOf est un tableau »** — presque : itérable, doté de `forEach`, indexable — mais ni `filter` ni `map`. `Array.from(nodes)` opère la conversion, une fois, franchement.',
              '**« data-* renvoie des types forts »** — tout est `string | undefined` : `data-prix` arrive en texte, `data-actif` pareil. Convertis explicitement, et traite l\'absence avec `??` — le DOM ne fait aucun typage pour toi, il n\'en a jamais eu vocation.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Tu fais ici se rejoindre deux mondes que le cours a tenus séparés jusqu\'ici : le DOM de `js-dom` (sélection, création, manipulation — rien n\'a changé d\'exécution) et TOUT l\'édifice TypeScript — génériques (`ts-generics`), nullité maîtrisée (`ts-types-speciaux`), interfaces par millier (`ts-interfaces`, ici `lib.dom.d.ts` = la fiche `ts-dts` géante). L\'affirmation générique + null-check est le même mouvement que `unknown` + garde : AFFIRMER moins que vérifier. La fiche qui suit immédiatement, `ts-dom-evenements`, branche la deuxième moitié du DOM : ce qui se passe quand l\'utilisateur agit — et dont les objets sont, eux aussi, remarquablement typés.' },
            { t: 'callout', kind: 'tip', h: 'Rituel de montage sain dans chaque composant : 1) sélections affinées AU SOMMET du fichier, avec `?.` pour les attaches optionnelles ou `if (!x) throw` pour les vitales ; 2) conversions string→number visibles et localisées ; 3) aucun `as` non justifié dans le code de page. La page devient lisible comme une facture : chaque ligne prouvée, aucune devinée.' }
          ],
          errors: [
            { title: 'Caster sans vérifier l\'existence', lang: 'ts', bad:
'const champ = document.querySelector(\"#recherche\") as HTMLInputElement;\nchamp.value = \"gari\";   // CRASH si l\'id est renommé : crash au chargement,\n                        //    le cast a escamoté le null-check', good:
'const champ = document.querySelector<HTMLInputElement>(\"#recherche\");\nif (!champ) throw new Error(\"#recherche introuvable : le template a changé ?\");\nchamp.value = \"gari\";   // ✓ présence prouvée, type affirmé — les deux questions traitées', why: 'Le cast ne change RIEN au runtime : il fait taire le compilateur sur la nullité, puis la réalité facture. Le générique + le test d\'existence posent les deux questions honnêtes (« est-ce là ? » « de quel genre ? ») — et l\'erreur explicite au montage vaut mille crashes silencieux en production.' },
            { title: 'Croire que dataset et value sont déjà typés', lang: 'ts', bad:
'const qte = ligne.dataset.quantite;          // string | undefined\nconst total = qte * prix;                    // ✗ arithmétique sur undefined/string\nconst remise = champ.value + 10;             // \"10\" + 10 = \"1010\", le classique', good:
'const qte = Number(ligne.dataset.quantite ?? \"0\");   // conversion + absence traitées\nconst total = qte * prix;\nconst remise = Number(champ.value) + 10;             // la chaîne devient nombre, évidemment', why: 'Le DOM ne connaît que DU TEXTE : attributs data-*, value de champs, textContent. Toute arithmétique, toute comparaison exige d\'abord la conversion — et `undefined` en cas d\'attribut absent. C\'est la même loi que pour `unknown` et le réseau : ce qui vient DEHORS se convertit à la frontière, jamais au milieu du calcul.' }
          ],
          related: ['ts-dom-evenements', 'js-dom', 'ts-narrowing', 'ts-dts']
        },
        {
          id: 'ts-dom-evenements',
          title: 'Événements : MouseEvent, FormEvent & cie',
          icon: 'touch_app',
          level: 'Intermédiaire',
          tagline: 'Le bon objet événement, typé selon l\'écoute — click donne MouseEvent, submit donne SubmitEvent, automatiquement.',
          intro: 'Côté événements, TypeScript brille d\'un confort discret mais quotidien : **`addEventListener` sait quel objet événement correspond à quel type d\'écoute**. Écoute `\"click\"` → le paramètre est un `MouseEvent` (coordonnées, bouton pressé) ; écoute `\"keydown\"` → `KeyboardEvent` (touches, modificateurs) ; écoute `\"submit\"` → `SubmitEvent`. C\'est la carte des événements (`HTMLElementEventMap`) qui orchestre tout — une immense interface du `lib.dom.d.ts` qui associe chaque nom d\'écoute à son objet. Ajoute la distinction `target` vs `currentTarget` (le cas classique du clic délégué) et `FormData` pour les formulaires, et tu obtiens des pages où chaque interaction est vérifiée jusqu\'au bout du `preventDefault`.',
          blocks: [
            { t: 'h3', h: 'Le pourquoi : les événements ne sont pas interchangeables' },
            { t: 'p', h: 'En JavaScript, le gestionnaire reçoit « un événement » — et chacun improvise : `e.clientX` sur un `input` (undefined, silencieux), `e.key` dans un clic (pareil), et l\'on ne s\'en rend compte qu\'en production. Le DOM TypeScript a fait le bureaucrate utile : il possède une CARTE des correspondances — tel nom d\'écoute, tel objet événement, telle cible — et `addEventListener` est générique DESSUS. En clair : tu choisis `\"click\"`, le compilateur sait que ton paramètre est un `MouseEvent` avec `clientX/clientY/button` ; tu choisis `\"submit\"`, c\'est un `SubmitEvent` avec son `submitter`. Rien à déclarer, rien à caster : le type du gestionnaire suit l\'écoute, naturellement. C\'est probablement l\'expérience la plus « magique » du TS quotidien — et elle repose sur une simple interface géante que tu peux lire.' },
            { t: 'h3', h: 'La carte en action : l\'écoute détermine l\'objet' },
            { t: 'code', lang: 'ts', label: 'eventmap.ts', code:
'const bouton = document.querySelector<HTMLButtonElement>(\"#valider\");\n\nbouton?.addEventListener(\"click\", (e) => {\n  // e : MouseEvent — le compilateur le SAIT via la carte\n  console.log(e.clientX, e.clientY, e.button);\n});\n\nchamp?.addEventListener(\"keydown\", (e) => {\n  // e : KeyboardEvent — jamais clientX/plus de méthodes d\'input\n  if (e.key === \"Enter\") valider();\n  if (e.ctrlKey && e.key === \"k\") ouvrirPalette();\n});// e.clientX serait REFUSÉ ici : pas de souris dans un clavier\n\nformulaire?.addEventListener(\"submit\", (e) => {\n  // e : SubmitEvent — e.submitter = le bouton qui a déclenché\n  e.preventDefault();\n});' },
            { t: 'p', h: 'Arrête-toi sur la deuxième ligne du premier exemple : tu n\'as RIEN écrit de typé — `e` est inféré, et il est EXACT. La mécanique : `addEventListener` est déclarée générique sur le NOM de l\'événement (`K extends keyof HTMLElementEventMap`) ; la carte associe `\"click\": MouseEvent`, `\"keydown\": KeyboardEvent`, `\"input\": Event` (question technique : `input` légal est `Event`, le contenu vit sur la cible) ; et le paramètre du callback prend le type correspondant. Deux conséquences pratiques : l\'AUTOCOMPLÉTION sur `e.` devient chirurgicale (seules les propriétés du bon événement), et les impostures (`e.key` dans un clic) sont refusées à l\'écriture. Si un jour tu sors du DOM officiel (événements custom), une `CustomEvent` + un cast assumé ou l\'augmentation de la carte via `declare global` fait le lien — même mécanique, mêmes règles.' },
            { t: 'h3', h: 'target vs currentTarget : l\'affaire du clic délégué' },
            { t: 'code', lang: 'ts', label: 'delegation.ts', code:
'// Délégation : UN écouteur sur la liste, tri des clics au dataset\nliste?.addEventListener(\"click\", (e) => {\n  // currentTarget : l\'élément QUI ÉCOUTE (la liste) — toujours juste\n  const zone = e.currentTarget as HTMLUListElement;\n\n  // target : l\'élément RÉELLEMENT CLIQUÉ — EventTarget générique !\n  const li = (e.target as HTMLElement).closest(\"li\");\n  if (!li) return;\n  const produit = (li as HTMLElement).dataset.produit;\n  console.log(\"Ajout au panier :\", produit);  // dataset : string | undefined — traité\n});' },
            { t: 'p', h: 'La distinction est le point douloureux n° 1 des événements, alors posons-la clairement. `currentTarget` est l\'élément SUR LEQUEL tu as posé l\'écouteur — fiable, typé précisément par le compilateur quand tu l\'as sélectionné (et son type suit le générique de l\'écoute). `target` est l\'élément où l\'événement est RÉELLEMENT né — et sa propriété fondamentale est l\'imprévisibilité : un clic sur le `span` DANS un `li` DANS la liste… Le type officiel est donc volontairement flou (`EventTarget | null`) — et le modèle sain consiste à le REQUALIFIER honnêtement (`as HTMLElement`, ou mieux : un garde maison minimal) puis à remonter au conteneur qui t\'intéresse (@closest). La leçon à méditer : TypeScript ne « résout » pas la délégation — il rend EXPLICITE ce que JS laissait sous silence, à savoir que `target` n\'est jamais garanti. Et comme toujours : vérifier, qualifier, travailler.' },
            { t: 'h3', h: 'Les formulaires : SubmitEvent, FormData et la validation typée' },
            { t: 'code', lang: 'ts', label: 'form-commande.ts', code:
'const form = document.querySelector<HTMLFormElement>(\"#commande\");\n\nform?.addEventListener(\"submit\", (e) => {\n  e.preventDefault();                       // on pilote l\'envoi nous-mêmes\n  const f = e.currentTarget as HTMLFormElement;\n  const donnees = new FormData(f);\n\n  const qte = Number(donnees.get(\"quantite\"));      // get : FormDataEntryValue | null\n  const moyen = String(donnees.get(\"moyen\") ?? \"\");\n\n  if (!Number.isFinite(qte) || qte <= 0) {\n    afficherErreur(\"Quantité invalide\");\n    return;\n  }\n  enregistrerCommande({ quantite: qte, moyen });   // Produit typé, enfin\n});' },
            { t: 'p', h: 'Le formulaire est LA frontière DOM par excellence — le point où l\'utilisateur fournit de la DONNÉE, comme le réseau le fait pour `fetch`. La mécanique complète, une fois pour toutes : `preventDefault` pour reprendre la main ; `FormData` transforme le formulaire en dictionnaire (et son `.get()` rend `FormDataEntryValue | null` — string ou File, et null si absent : convertis la string, traite le null, exactement comme `dataset`) ; validation explicite (tel est le rôle de `Number.isFinite` qui refuse à la fois NaN et l\'infini) ; et SEULEMENT ENSUITE, construction de l\'objet métier TYPÉ — la `Commande` de ta fiche Interfaces. Tu reconnais le rituel qui clôt ce module : vérifier à la frontière, puis vivre en paix. Le bonus de la maison : `e.submitter` (SubmitEvent) dit QUEL bouton a déclenché l\'envoi quand le formulaire en a plusieurs (« Acheter » vs « Panier ») — un détail qui évite les `form.querySelector` aléatoires.' },
            { t: 'h3', h: 'Extraire les gestionnaires : typer les fonctions à la main' },
            { t: 'code', lang: 'ts', label: 'handlers.ts', code:
'// Sortir le handler de l\'addEventListener = le typer SOI-MÊME :\nfunction onRecherche(e: Event) {\n  const champ = e.currentTarget as HTMLInputElement;   // requalification assumée\n  rechercher(champ.value);\n}\nchamp?.addEventListener(\"input\", onRecherche);   // ✓ signature compatible\n\n// Version bouton, avec l\'événement précis pour la propreté :\nfunction onValider(e: MouseEvent) {\n  if (e.shiftKey) ouvrirOptionsAvancees();   // detail standard de MouseEvent\n  valider();\n}\nbouton?.addEventListener(\"click\", onValider);\n\n// Bonus consistence : un alias d\'équipe pour les gestionnaires courants\ntype Handler<E extends Event> = (e: E) => void;' },
            { t: 'p', h: 'Dès que le gestionnaire SORT de l\'`addEventListener` (lisibilité, tests, réutilisation entre deux éléments), l\'inférence magique se retire : tu dois annoter TOI-MÊME le paramètre — `Event` pour les génériques (`input`, `change`), `MouseEvent`, `KeyboardEvent`, `SubmitEvent` pour les précis. Deux règles de bonne tenue. Correspondance honnête : le type annoncé doit pouvoir RECEVOIR ce que l\'écoute enverra — si `input` envoie `Event`, n\'affirme pas `InputEvent` (les écoutes légales l\'emportent toujours sur les spécialisations rêvées). Et comme la cible redevient floue hors du contexte de l\'appel, `currentTarget as HTMLInputElement` est la requalification ASSUMÉE — à commenter d\'un mot quand le handler survit à l\'écoute initiale. À l\'usage, ce pattern de handlers extraits et typés fait des pages testables : passe un faux `{ currentTarget: fakeInput } as unknown as Event`, et le gestionnaire s\'exécute hors du navigateur.' },
            { t: 'h3', h: 'Sous le capot : l\'EventMap, une interface comme une autre' },
            { t: 'p', h: 'Pas de sorcellerie dans la correspondance : `HTMLElementEventMap`, la grande interface de `lib.dom.d.ts`, énumère nom par nom chaque écoute standard — `\"click\": MouseEvent`, `\"dblclick\": MouseEvent`, `\"keydown\": KeyboardEvent`, `\"submit\": SubmitEvent`, `\"input\": Event`… — et la signature GÉNÉRIQUE d\'`addEventListener` (`addEventListener\<K extends keyof HTMLElementEventMap>(type: K, listener: (ev: HTMLElementEventMap[K]) => …)`) lit la valeur correspondante. C\'est l\'accès indexé `T[K]` de la fiche `ts-keyof-typeof`, appliqué à la plateforme web ! Conséquences concrètes à retenir : un NOM d\'écoute non standard (`\"mon-event\"`) tombe sur la surcharge générique et donne un `Event` de base — rien de cassé, juste moins de magie ; et comme cette map est… une interface, tu peux l\'AUGMENTER par fusion (`declare global`) pour TES événements maison si tu émets via `dispatchEvent`. Sous chaque confort TypeScript se cache un mécanisme que tu as déjà appris — cette phrase pourrait clore le module.' },
            { t: 'h3', h: 'Exemple complet : le formulaire de commande de la Boutique Awa' },
            { t: 'code', lang: 'ts', label: 'commande-complete.ts', code:
'interface Commande {\n  produitId: string;\n  quantite: number;\n  moyen: \"momo\" | \"moov\" | \"especes\";\n}\n\nconst form = document.querySelector<HTMLFormElement>(\"#commande-form\");\nconst champQte = document.querySelector<HTMLInputElement>(\"#qte\");\nconst feedback = document.querySelector<HTMLParagraphElement>(\"#feedback\");\n\n// Prévisualisation du total TANT que l\'utilisateur tape — input = temps réel\nchampQte?.addEventListener(\"input\", (e) => {\n  const q = Number((e.currentTarget as HTMLInputElement).value);\n  if (feedback) {\n    feedback.textContent = Number.isFinite(q) && q > 0\n      ? \"Total estimé : \" + q * 21000 + \" F\"\n      : \"Saisir une quantité valide\";\n  }\n});\n\nform?.addEventListener(\"submit\", (e) => {\n  e.preventDefault();\n  const d = new FormData(e.currentTarget as HTMLFormElement);\n  const C: Commande = {\n    produitId: String(d.get(\"produit\") ?? \"\"),\n    quantite: Number(d.get(\"quantite\")),\n    moyen: String(d.get(\"moyen\") ?? \"especes\") as Commande[\"moyen\"]\n  };\n  enregistrerCommande(C);   // objet métier propre, forme prouvée\n});' },
            { t: 'p', h: 'Relis ce dernier exemple comme l\'examinateur du module : TOUT ce qu\'il contient a été enseigné. Les éléments affirmés au sélecteur (`ts-dom-elements`) ; les écouteurs avec objets exacts (`input`/`submit`) ; la cible requalifiée honnêtement post-callback ; `FormData` converti à la frontière ; et l\'objet `Commande` métier — l\'interface de la fiche 4 — construit en dernier, une fois chaque champ prouvé. Le module TypeScript entier tient dans ces quinze lignes d\'une page de boutique à Cotonou : affirmer peu, vérifier aux frontières, laisser le compilateur relire le reste. C\'est la promesse du hero de ce module, tenue au niveau de TON interface utisateur.' },
            { t: 'h3', h: 'Ce que les débutants comprennent mal' },
            { t: 'ul', items: [
              '**« e.target est toujours l\'élément écouté »** — non : `target` = élément RÉEL où l\'événement est né (potentiellement un enfant, un descendant lointain) ; `currentTarget` = élément porteur de l\'écoute. La délégation vit entière de cette distinction — et le type de `target` est volontairement flou pour te le rappeler.',
              '**« l\'événement \"input\" me donne un InputEvent typé »** — la carte officielle l\'associe à `Event` (historiquement : les détails spécifiques ne sont pas garantis sur toutes les cibles). Pour le contenu, va chercher `value` sur `currentTarget` requalifié — la source fiable, toujours.',
              '**« preventDefault est automatique avec TypeScript »** — non : rien de TS ne change le comportement du navigateur. TS type l\'événement ; la BOMBE de navigation classique (le submit qui recharge la page au beau milieu de ton fetch) reste à désamorcer explicitement, comme en JS.',
              '**« je peux écouter n\'importe quel nom »** — oui : `addEventListener(\"toto\", …)` compile (surcharge générique, événement `Event`). La CARTE donne les types précis pour les noms standard ; au-delà, tu es en terrain événementiel personnalisé (`CustomEvent`) avec conventions maison à expliciter.',
              '**« FormData.get renvoie ce qui est dans le champ »** — pas tout à fait : `FormDataEntryValue | null` — string ou File selon le type d\'input, et null si l\'attribut `name` est absent ou non matché. Convertis la string vers ton type métier AU MÊME endroit, une fois.'
            ]},
            { t: 'h3', h: 'Lien avec les notions déjà vues' },
            { t: 'p', h: 'Ceci est la dernière fiche — et volontairement la plus SYNTHÉTIQUE : les événements JS de `js-evenements` (boucle de dispatch, délégation, phases) typés ; les éléments de `ts-dom-elements` comme cibles ; l\'accès indexé et les unions de `ts-keyof-typeof`/`ts-unions` cachés dans la carte ; les objets métier de `ts-interfaces` comme destination finale. Si tu repenses au premier sujet du module — `ts-pourquoi` (« attraper les erreurs AVANT le vendredi soir du client ») — tu vois le trajet : de la variable `let` à l\'utilisateur qui clique, CHAQUE frontière a reçu son garde. Le JavaScript qui tourne est identique ; tu n\'es plus le seul à l\'avoir relu.' },
            { t: 'callout', kind: 'tip', h: 'Prochaine étape naturelle, quand tu es prêt : reprendre l\'UN des projets DOM du module JavaScript (panier, todo, calculatrice de tontine) et le porter en TypeScript — sélecteurs affirmés, handlers extraits et typés, FormData converti à la frontière. Compter les erreurs que tsc attrape dans le portage est le meilleur bilan pédagogique qui soit : ce sont exactement les bugs qui t\'attendaient à la prochaine évolution.' }
          ],
          errors: [
            { title: 'Confondre target et currentTarget', lang: 'ts', bad:
'liste.addEventListener(\"click\", (e) => {\n  const li = e.target as HTMLLIElement;\n  li.dataset.produit;      // ✗ fragilité : clic sur le <span> DANS le li !\n  // e.target est l\'élément SOUS la souris, pas forcément le li visé\n});', good:
'liste.addEventListener(\"click\", (e) => {\n  const brut = e.target as HTMLElement;\n  const li = brut.closest(\"li\");              // ✓ remonte au conteneur utile\n  if (!li) return;                            // clic hors ligne : rien à faire\n  console.log((li as HTMLElement).dataset.produit);\n});', why: '`target` désigne l\'élément RÉELLEMENT touché par l\'événement — avec un `span` imbriqué, ce n\'est JAMAIS garanti d\'être ton conteneur. `closest` remonte l\'arborescence jusqu\'au candidat utile (ou null hors zone) ; et `currentTarget` reste la référence stable quand c\'est le PORTEUR d\'écoute qu\'il te faut. Le typage volontairement vague de EventTarget est un garde-fou, pas une négligence.' },
            { title: 'Attribuer à l\'événement des propriétés qu\'il n\'a pas', lang: 'ts', bad:
'champ?.addEventListener(\"keydown\", (e) => {\n  console.log(e.clientX);   // ✗ KeyboardEvent n\'a pas clientX :\n                            //   refusé à l\'écriture — et tant mieux\n});', good:
'champ?.addEventListener(\"keydown\", (e) => {\n  if (e.key === \"Enter\") valider();        // ✓ propriétés DE KeyboardEvent\n});\n// Besoin de coordonnées ? C\'est un événement SOURIS qu\'il te faut :\nzone?.addEventListener(\"click\", (e) => console.log(e.clientX, e.clientY));', why: 'Chaque objet événement porte SES données : clavier → touches et modificateurs, souris → coordonnées et bouton, submit → submitter. Écrire clientX dans un handler clavier est un bug de conception (que se passerait-il à l\'exécution ? undefined permanent) : la carte des événements le refuse AVANT le premier test — relis le refus du compilateur comme un conseil d\'architecture, pas comme une broutille.' }
          ],
          related: ['ts-dom-elements', 'js-evenements', 'ts-type-guards', 'html-formulaires']
        }
      ]
    }
  ]
};
